data "azurestack_storage_container" "example" {
    id = "{{ id }}"
    metadata = {
        key = "{{ metadata_value }}"
      }
    name = "{{ name }}"
    storage_account_name = "{{ storage_account_name }}"
    timeouts {
      read = "{{ read }}"
    }
}
