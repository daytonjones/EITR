data "azurestack_image" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    name_regex = "{{ name_regex }}"
    resource_group_name = "{{ resource_group_name }}"
    sort_descending = {{ sort_descending }}
    timeouts {
      read = "{{ read }}"
    }
}
