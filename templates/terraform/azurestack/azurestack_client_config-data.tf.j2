data "azurestack_client_config" "example" {
    client_id = "{{ client_id }}"
    id = "{{ id }}"
    object_id = "{{ object_id }}"
    service_principal_application_id = "{{ service_principal_application_id }}"
    service_principal_object_id = "{{ service_principal_object_id }}"
    subscription_id = "{{ subscription_id }}"
    tenant_id = "{{ tenant_id }}"
}