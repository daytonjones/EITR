data "azurestack_key_vault_secret" "example" {
    content_type = "{{ content_type }}"
    id = "{{ id }}"
    key_vault_id = "{{ key_vault_id }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    value = "{{ value }}"
    version = "{{ version }}"
}