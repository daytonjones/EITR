data "azurestack_public_ips" "example" {
    allocation_type = "{{ allocation_type }}"
    attached = {{ attached }}
    attachment_status = "{{ attachment_status }}"
    id = "{{ id }}"
    name_prefix = "{{ name_prefix }}"
    resource_group_name = "{{ resource_group_name }}"
    timeouts {
      read = "{{ read }}"
    }
}
