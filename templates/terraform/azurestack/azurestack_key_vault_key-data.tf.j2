data "azurestack_key_vault_key" "example" {
    e = "{{ e }}"
    id = "{{ id }}"
    key_opts = "{{ key_opts }}"
    key_size = "{{ key_size }}"
    key_type = "{{ key_type }}"
    key_vault_id = "{{ key_vault_id }}"
    n = "{{ n }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    version = "{{ version }}"
}