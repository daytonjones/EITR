data "awscc_lex_bot_version" "example" {
    bot_id = "{{ bot_id }}"
    bot_version = "{{ bot_version }}"
    bot_version_locale_specification = "{
    bot_version_locale_details = {
    source_bot_version = {{ source_bot_version }}
}
    locale_id = {{ locale_id }}
}"
    description = "{{ description }}"
    id = "{{ id }}"
}