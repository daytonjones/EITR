data "awscc_wisdom_ai_prompt_version" "example" {
    ai_prompt_arn = "{{ ai_prompt_arn }}"
    ai_prompt_id = "{{ ai_prompt_id }}"
    ai_prompt_version_id = "{{ ai_prompt_version_id }}"
    assistant_arn = "{{ assistant_arn }}"
    assistant_id = "{{ assistant_id }}"
    id = "{{ id }}"
    modified_time_seconds = "{{ modified_time_seconds }}"
    version_number = "{{ version_number }}"
}