resource "awscc_ec2_transit_gateway_peering_attachment" "example" {
    peer_account_id = "{{ peer_account_id }}"
    peer_region = "{{ peer_region }}"
    peer_transit_gateway_id = "{{ peer_transit_gateway_id }}"
    tags = "{{ tags }}"
    transit_gateway_id = "{{ transit_gateway_id }}"
}
