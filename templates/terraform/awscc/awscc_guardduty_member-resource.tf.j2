resource "awscc_guardduty_member" "example" {
    detector_id = "{{ detector_id }}"
    disable_email_notification = {{ disable_email_notification }}
    email = "{{ email }}"
    member_id = "{{ member_id }}"
    message = "{{ message }}"
    status = "{{ status }}"
}
