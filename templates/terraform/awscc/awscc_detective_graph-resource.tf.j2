resource "awscc_detective_graph" "example" {
    arn = "{{ arn }}"
    auto_enable_members = "{{ auto_enable_members }}"
    id = "{{ id }}"
    tags = "{
    key = {{ key }}
    value = {{ value }}
}"
}