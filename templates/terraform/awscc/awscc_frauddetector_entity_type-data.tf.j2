data "awscc_frauddetector_entity_type" "example" {
    arn = "{{ arn }}"
    created_time = "{{ created_time }}"
    description = "{{ description }}"
    id = "{{ id }}"
    last_updated_time = "{{ last_updated_time }}"
    name = "{{ name }}"
    tags = "{
    key = {{ key }}
    value = {{ value }}
}"
}