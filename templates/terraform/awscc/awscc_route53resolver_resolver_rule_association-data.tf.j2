data "awscc_route53resolver_resolver_rule_association" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    resolver_rule_association_id = "{{ resolver_rule_association_id }}"
    resolver_rule_id = "{{ resolver_rule_id }}"
    vpc_id = "{{ vpc_id }}"
}