resource "awscc_iotfleetwise_signal_catalog" "example" {
    description = "{{ description }}"
    name = "{{ name }}"
    node_counts = "{{ node_counts }}"
    nodes = "{{ nodes }}"
    tags = "{{ tags }}"
}
