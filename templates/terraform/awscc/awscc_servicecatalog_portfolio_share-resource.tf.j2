resource "awscc_servicecatalog_portfolio_share" "example" {
    accept_language = "{{ accept_language }}"
    account_id = "{{ account_id }}"
    portfolio_id = "{{ portfolio_id }}"
    share_tag_options = {{ share_tag_options }}
}
