resource "awscc_connectcampaigns_campaign" "example" {
    connect_instance_arn = "{{ connect_instance_arn }}"
    dialer_config = "{{ dialer_config }}"
    name = "{{ name }}"
    outbound_call_config = "{{ outbound_call_config }}"
    tags = "{{ tags }}"
}
