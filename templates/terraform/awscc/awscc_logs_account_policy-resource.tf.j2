resource "awscc_logs_account_policy" "example" {
    account_id = "{{ account_id }}"
    id = "{{ id }}"
    policy_document = "{{ policy_document }}"
    policy_name = "{{ policy_name }}"
    policy_type = "{{ policy_type }}"
    scope = "{{ scope }}"
    selection_criteria = "{{ selection_criteria }}"
}