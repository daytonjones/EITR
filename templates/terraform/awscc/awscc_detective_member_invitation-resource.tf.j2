resource "awscc_detective_member_invitation" "example" {
    disable_email_notification = "{{ disable_email_notification }}"
    graph_arn = "{{ graph_arn }}"
    id = "{{ id }}"
    member_email_address = "{{ member_email_address }}"
    member_id = "{{ member_id }}"
    message = "{{ message }}"
}