resource "awscc_b2bi_partnership" "example" {
    capabilities = ["{{ capabilities }}"]
    capability_options = "{{ capability_options }}"
    email = "{{ email }}"
    name = "{{ name }}"
    phone = "{{ phone }}"
    profile_id = "{{ profile_id }}"
    tags = "{{ tags }}"
}
