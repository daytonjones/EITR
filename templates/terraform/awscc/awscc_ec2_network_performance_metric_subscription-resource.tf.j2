resource "awscc_ec2_network_performance_metric_subscription" "example" {
    destination = "{{ destination }}"
    metric = "{{ metric }}"
    source = "{{ source }}"
    statistic = "{{ statistic }}"
}
