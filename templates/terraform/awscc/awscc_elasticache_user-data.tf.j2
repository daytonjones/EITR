data "awscc_elasticache_user" "example" {
    access_string = "{{ access_string }}"
    arn = "{{ arn }}"
    authentication_mode = "{
    passwords = {{ passwords }}
    type = {{ type }}
}"
    engine = "{{ engine }}"
    id = "{{ id }}"
    no_password_required = "{{ no_password_required }}"
    passwords = "{{ passwords }}"
    status = "{{ status }}"
    tags = "{
    key = {{ key }}
    value = {{ value }}
}"
    user_id = "{{ user_id }}"
    user_name = "{{ user_name }}"
}