resource "awscc_lookoutmetrics_alert" "example" {
    action = "{{ action }}"
    alert_description = "{{ alert_description }}"
    alert_name = "{{ alert_name }}"
    alert_sensitivity_threshold = {{ alert_sensitivity_threshold }}
    anomaly_detector_arn = "{{ anomaly_detector_arn }}"
}
