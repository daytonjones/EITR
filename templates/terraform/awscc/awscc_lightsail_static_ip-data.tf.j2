data "awscc_lightsail_static_ip" "example" {
    attached_to = "{{ attached_to }}"
    id = "{{ id }}"
    ip_address = "{{ ip_address }}"
    is_attached = "{{ is_attached }}"
    static_ip_arn = "{{ static_ip_arn }}"
    static_ip_name = "{{ static_ip_name }}"
}