resource "awscc_connect_contact_flow" "example" {
    content = "{{ content }}"
    description = "{{ description }}"
    instance_arn = "{{ instance_arn }}"
    name = "{{ name }}"
    state = "{{ state }}"
    tags = "{{ tags }}"
    type = "{{ type }}"
}
