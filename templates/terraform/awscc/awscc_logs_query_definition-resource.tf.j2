resource "awscc_logs_query_definition" "example" {
    log_group_names = ["{{ log_group_names }}"]
    name = "{{ name }}"
    parameters = "{{ parameters }}"
    query_language = "{{ query_language }}"
    query_string = "{{ query_string }}"
}
