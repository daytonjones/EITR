resource "awscc_datazone_policy_grant" "example" {
    detail = "{{ detail }}"
    domain_identifier = "{{ domain_identifier }}"
    entity_identifier = "{{ entity_identifier }}"
    entity_type = "{{ entity_type }}"
    policy_type = "{{ policy_type }}"
    principal = "{{ principal }}"
}
