resource "awscc_m2_deployment" "example" {
    application_id = "{{ application_id }}"
    application_version = "{{ application_version }}"
    deployment_id = "{{ deployment_id }}"
    environment_id = "{{ environment_id }}"
    id = "{{ id }}"
    status = "{{ status }}"
}