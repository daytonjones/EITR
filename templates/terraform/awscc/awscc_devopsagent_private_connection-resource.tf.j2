resource "awscc_devopsagent_private_connection" "example" {
    certificate = "{{ certificate }}"
    connection_configuration = "{{ connection_configuration }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
}
