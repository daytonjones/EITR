resource "awscc_athena_data_catalog" "example" {
    connection_type = "{{ connection_type }}"
    description = "{{ description }}"
    error = "{{ error }}"
    name = "{{ name }}"
    parameters = {
        key = "{{ parameters_value }}"
      }
    status = "{{ status }}"
    tags = "{{ tags }}"
    type = "{{ type }}"
}
