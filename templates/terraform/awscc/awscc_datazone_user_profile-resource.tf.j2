resource "awscc_datazone_user_profile" "example" {
    domain_identifier = "{{ domain_identifier }}"
    status = "{{ status }}"
    user_identifier = "{{ user_identifier }}"
    user_type = "{{ user_type }}"
}
