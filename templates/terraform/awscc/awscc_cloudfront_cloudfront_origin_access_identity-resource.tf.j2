resource "awscc_cloudfront_cloudfront_origin_access_identity" "example" {
    cloudfront_origin_access_identity_config = "{
    comment = {{ comment }}
}"
    cloudfront_origin_access_identity_id = "{{ cloudfront_origin_access_identity_id }}"
    id = "{{ id }}"
    s3_canonical_user_id = "{{ s3_canonical_user_id }}"
}