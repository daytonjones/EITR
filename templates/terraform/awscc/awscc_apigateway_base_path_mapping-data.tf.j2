data "awscc_apigateway_base_path_mapping" "example" {
    base_path = "{{ base_path }}"
    domain_name = "{{ domain_name }}"
    id = "{{ id }}"
    rest_api_id = "{{ rest_api_id }}"
    stage = "{{ stage }}"
}