data "awscc_securitylake_aws_log_source" "example" {
    accounts = "{{ accounts }}"
    data_lake_arn = "{{ data_lake_arn }}"
    id = "{{ id }}"
    source_name = "{{ source_name }}"
    source_version = "{{ source_version }}"
}