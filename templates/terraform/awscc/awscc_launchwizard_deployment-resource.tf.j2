resource "awscc_launchwizard_deployment" "example" {
    deployment_pattern_name = "{{ deployment_pattern_name }}"
    name = "{{ name }}"
    specifications = {
        key = "{{ specifications_value }}"
      }
    tags = "{{ tags }}"
    workload_name = "{{ workload_name }}"
}
