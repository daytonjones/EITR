data "awscc_elasticbeanstalk_application_version" "example" {
    application_name = "{{ application_name }}"
    application_version_id = "{{ application_version_id }}"
    description = "{{ description }}"
    id = "{{ id }}"
    source_bundle = "{
    s3_bucket = {{ s3_bucket }}
    s3_key = {{ s3_key }}
}"
}