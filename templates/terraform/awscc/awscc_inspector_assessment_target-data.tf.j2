data "awscc_inspector_assessment_target" "example" {
    arn = "{{ arn }}"
    assessment_target_name = "{{ assessment_target_name }}"
    id = "{{ id }}"
    resource_group_arn = "{{ resource_group_arn }}"
}