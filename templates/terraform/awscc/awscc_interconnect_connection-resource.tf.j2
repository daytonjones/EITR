resource "awscc_interconnect_connection" "example" {
    activation_key = "{{ activation_key }}"
    attach_point = "{{ attach_point }}"
    bandwidth = "{{ bandwidth }}"
    description = "{{ description }}"
    environment_id = "{{ environment_id }}"
    remote_owner_account = "{{ remote_owner_account }}"
    tags = "{{ tags }}"
}
