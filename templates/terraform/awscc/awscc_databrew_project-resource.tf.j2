resource "awscc_databrew_project" "example" {
    dataset_name = "{{ dataset_name }}"
    name = "{{ name }}"
    recipe_name = "{{ recipe_name }}"
    role_arn = "{{ role_arn }}"
    sample = "{{ sample }}"
    tags = "{{ tags }}"
}
