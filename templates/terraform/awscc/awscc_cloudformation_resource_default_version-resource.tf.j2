resource "awscc_cloudformation_resource_default_version" "example" {
    arn = "{{ arn }}"
    id = "{{ id }}"
    type_name = "{{ type_name }}"
    type_version_arn = "{{ type_version_arn }}"
    version_id = "{{ version_id }}"
}