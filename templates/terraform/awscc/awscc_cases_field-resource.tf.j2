resource "awscc_cases_field" "example" {
    attributes = "{{ attributes }}"
    description = "{{ description }}"
    domain_id = "{{ domain_id }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    type = "{{ type }}"
}
