resource "awscc_iot_resource_specific_logging" "example" {
    id = "{{ id }}"
    log_level = "{{ log_level }}"
    target_id = "{{ target_id }}"
    target_name = "{{ target_name }}"
    target_type = "{{ target_type }}"
}