data "awscc_servicecatalogappregistry_attribute_group" "example" {
    arn = "{{ arn }}"
    attribute_group_id = "{{ attribute_group_id }}"
    attributes = "{{ attributes }}"
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
}