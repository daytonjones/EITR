resource "awscc_route53globalresolver_access_token" "example" {
    client_token = "{{ client_token }}"
    dns_view_id = "{{ dns_view_id }}"
    expires_at = "{{ expires_at }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
}
