data "awscc_iam_saml_provider" "example" {
    arn = "{{ arn }}"
    id = "{{ id }}"
    name = "{{ name }}"
    saml_metadata_document = "{{ saml_metadata_document }}"
    tags = "{
    key = {{ key }}
    value = {{ value }}
}"
}