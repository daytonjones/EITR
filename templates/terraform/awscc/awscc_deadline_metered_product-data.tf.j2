data "awscc_deadline_metered_product" "example" {
    arn = "{{ arn }}"
    family = "{{ family }}"
    id = "{{ id }}"
    license_endpoint_id = "{{ license_endpoint_id }}"
    port = "{{ port }}"
    product_id = "{{ product_id }}"
    vendor = "{{ vendor }}"
}