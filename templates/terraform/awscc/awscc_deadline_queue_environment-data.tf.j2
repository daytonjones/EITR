data "awscc_deadline_queue_environment" "example" {
    farm_id = "{{ farm_id }}"
    id = "{{ id }}"
    name = "{{ name }}"
    priority = "{{ priority }}"
    queue_environment_id = "{{ queue_environment_id }}"
    queue_id = "{{ queue_id }}"
    template = "{{ template }}"
    template_type = "{{ template_type }}"
}