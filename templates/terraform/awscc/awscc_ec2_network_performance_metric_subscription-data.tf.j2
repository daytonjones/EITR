data "awscc_ec2_network_performance_metric_subscription" "example" {
    destination = "{{ destination }}"
    id = "{{ id }}"
    metric = "{{ metric }}"
    source = "{{ source }}"
    statistic = "{{ statistic }}"
}