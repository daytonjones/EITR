resource "awscc_datazone_environment_actions" "example" {
    description = "{{ description }}"
    domain_identifier = "{{ domain_identifier }}"
    environment_identifier = "{{ environment_identifier }}"
    identifier = "{{ identifier }}"
    name = "{{ name }}"
    parameters = "{{ parameters }}"
}
