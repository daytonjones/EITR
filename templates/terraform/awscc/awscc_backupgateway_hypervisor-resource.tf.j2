resource "awscc_backupgateway_hypervisor" "example" {
    host = "{{ host }}"
    kms_key_arn = "{{ kms_key_arn }}"
    log_group_arn = "{{ log_group_arn }}"
    name = "{{ name }}"
    password = "{{ password }}"
    tags = "{{ tags }}"
    username = "{{ username }}"
}
