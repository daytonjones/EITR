data "awscc_eventschemas_registry_policy" "example" {
    id = "{{ id }}"
    policy = "{{ policy }}"
    registry_name = "{{ registry_name }}"
    registry_policy_id = "{{ registry_policy_id }}"
    revision_id = "{{ revision_id }}"
}