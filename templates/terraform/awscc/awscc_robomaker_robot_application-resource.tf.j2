resource "awscc_robomaker_robot_application" "example" {
    arn = "{{ arn }}"
    current_revision_id = "{{ current_revision_id }}"
    environment = "{{ environment }}"
    id = "{{ id }}"
    name = "{{ name }}"
    robot_software_suite = "{
    name = {{ name }}
    version = {{ version }}
}"
    sources = "{
    architecture = {{ architecture }}
    s3_bucket = {{ s3_bucket }}
    s3_key = {{ s3_key }}
}"
    tags = "{{ tags }}"
}