resource "awscc_iottwinmaker_sync_job" "example" {
    sync_role = "{{ sync_role }}"
    sync_source = "{{ sync_source }}"
    tags = {
        key = "{{ tags_value }}"
      }
    workspace_id = "{{ workspace_id }}"
}
