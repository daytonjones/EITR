resource "awscc_inspectorv2_cis_scan_configuration" "example" {
    scan_name = "{{ scan_name }}"
    schedule = "{{ schedule }}"
    security_level = "{{ security_level }}"
    tags = {
        key = "{{ tags_value }}"
      }
    targets = "{{ targets }}"
}
