data "awscc_secretsmanager_resource_policy" "example" {
    block_public_policy = "{{ block_public_policy }}"
    id = "{{ id }}"
    resource_policy = "{{ resource_policy }}"
    resource_policy_id = "{{ resource_policy_id }}"
    secret_id = "{{ secret_id }}"
}