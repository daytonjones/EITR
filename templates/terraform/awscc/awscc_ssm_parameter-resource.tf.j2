resource "awscc_ssm_parameter" "example" {
    allowed_pattern = "{{ allowed_pattern }}"
    data_type = "{{ data_type }}"
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    policies = "{{ policies }}"
    tags = "{{ tags }}"
    tier = "{{ tier }}"
    type = "{{ type }}"
    value = "{{ value }}"
}