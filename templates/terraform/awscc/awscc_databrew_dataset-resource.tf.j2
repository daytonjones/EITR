resource "awscc_databrew_dataset" "example" {
    format = "{{ format }}"
    format_options = "{{ format_options }}"
    input = "{{ input }}"
    name = "{{ name }}"
    path_options = "{{ path_options }}"
    source = "{{ source }}"
    tags = "{{ tags }}"
}
