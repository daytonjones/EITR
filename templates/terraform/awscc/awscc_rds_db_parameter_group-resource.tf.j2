resource "awscc_rds_db_parameter_group" "example" {
    db_parameter_group_name = "{{ db_parameter_group_name }}"
    description = "{{ description }}"
    family = "{{ family }}"
    parameters = "{{ parameters }}"
    tags = "{{ tags }}"
}
