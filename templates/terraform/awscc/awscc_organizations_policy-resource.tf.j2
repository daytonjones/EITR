resource "awscc_organizations_policy" "example" {
    content = "{{ content }}"
    description = "{{ description }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    target_ids = ["{{ target_ids }}"]
    type = "{{ type }}"
}
