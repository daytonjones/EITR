resource "awscc_servicecatalog_service_action" "example" {
    accept_language = "{{ accept_language }}"
    definition = "{{ definition }}"
    definition_type = "{{ definition_type }}"
    description = "{{ description }}"
    name = "{{ name }}"
}
