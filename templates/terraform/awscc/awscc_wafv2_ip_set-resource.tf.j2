resource "awscc_wafv2_ip_set" "example" {
    addresses = ["{{ addresses }}"]
    description = "{{ description }}"
    ip_address_version = "{{ ip_address_version }}"
    name = "{{ name }}"
    scope = "{{ scope }}"
    tags = "{{ tags }}"
}
