resource "awscc_memorydb_user" "example" {
    access_string = "{{ access_string }}"
    arn = "{{ arn }}"
    authentication_mode = "{
    passwords = {{ passwords }}
    type = {{ type }}
}"
    id = "{{ id }}"
    status = "{{ status }}"
    tags = "{
    key = {{ key }}
    value = {{ value }}
}"
    user_name = "{{ user_name }}"
}