resource "awscc_ec2_key_pair" "example" {
    key_format = "{{ key_format }}"
    key_name = "{{ key_name }}"
    key_type = "{{ key_type }}"
    public_key_material = "{{ public_key_material }}"
    tags = "{{ tags }}"
}
