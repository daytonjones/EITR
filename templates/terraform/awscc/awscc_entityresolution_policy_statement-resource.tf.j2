resource "awscc_entityresolution_policy_statement" "example" {
    action = ["{{ action }}"]
    arn = "{{ arn }}"
    condition = "{{ condition }}"
    effect = "{{ effect }}"
    principal = ["{{ principal }}"]
    statement_id = "{{ statement_id }}"
}
