resource "awscc_resourceexplorer2_view" "example" {
    filters = "{{ filters }}"
    included_properties = "{{ included_properties }}"
    scope = "{{ scope }}"
    tags = {
        key = "{{ tags_value }}"
      }
    view_name = "{{ view_name }}"
}
