data "awscc_athena_named_query" "example" {
    database = "{{ database }}"
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    named_query_id = "{{ named_query_id }}"
    query_string = "{{ query_string }}"
    work_group = "{{ work_group }}"
}