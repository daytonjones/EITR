data "awscc_msk_serverless_cluster" "example" {
    arn = "{{ arn }}"
    client_authentication = "{
    sasl = {
    iam = {
    enabled = {{ enabled }}
}
}
}"
    cluster_name = "{{ cluster_name }}"
    id = "{{ id }}"
    tags = "{{ tags }}"
    vpc_configs = "{
    security_groups = {{ security_groups }}
    subnet_ids = {{ subnet_ids }}
}"
}