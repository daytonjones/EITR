data "awscc_guardduty_member" "example" {
    detector_id = "{{ detector_id }}"
    disable_email_notification = "{{ disable_email_notification }}"
    email = "{{ email }}"
    id = "{{ id }}"
    member_id = "{{ member_id }}"
    message = "{{ message }}"
    status = "{{ status }}"
}