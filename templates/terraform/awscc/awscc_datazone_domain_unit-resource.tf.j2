resource "awscc_datazone_domain_unit" "example" {
    description = "{{ description }}"
    domain_identifier = "{{ domain_identifier }}"
    name = "{{ name }}"
    parent_domain_unit_identifier = "{{ parent_domain_unit_identifier }}"
}
