data "awscc_ec2_subnet_network_acl_association" "example" {
    association_id = "{{ association_id }}"
    id = "{{ id }}"
    network_acl_id = "{{ network_acl_id }}"
    subnet_id = "{{ subnet_id }}"
}