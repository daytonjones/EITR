resource "awscc_organizations_account" "example" {
    account_name = "{{ account_name }}"
    email = "{{ email }}"
    parent_ids = ["{{ parent_ids }}"]
    role_name = "{{ role_name }}"
    tags = "{{ tags }}"
}
