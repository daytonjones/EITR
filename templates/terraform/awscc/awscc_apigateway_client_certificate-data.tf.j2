data "awscc_apigateway_client_certificate" "example" {
    client_certificate_id = "{{ client_certificate_id }}"
    description = "{{ description }}"
    id = "{{ id }}"
    tags = "{
    key = {{ key }}
    value = {{ value }}
}"
}