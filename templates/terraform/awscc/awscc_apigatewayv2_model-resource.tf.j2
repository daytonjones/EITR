resource "awscc_apigatewayv2_model" "example" {
    api_id = "{{ api_id }}"
    content_type = "{{ content_type }}"
    description = "{{ description }}"
    id = "{{ id }}"
    model_id = "{{ model_id }}"
    name = "{{ name }}"
    schema = "{{ schema }}"
}