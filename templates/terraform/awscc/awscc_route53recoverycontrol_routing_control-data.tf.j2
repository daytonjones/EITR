data "awscc_route53recoverycontrol_routing_control" "example" {
    cluster_arn = "{{ cluster_arn }}"
    control_panel_arn = "{{ control_panel_arn }}"
    id = "{{ id }}"
    name = "{{ name }}"
    routing_control_arn = "{{ routing_control_arn }}"
    status = "{{ status }}"
}