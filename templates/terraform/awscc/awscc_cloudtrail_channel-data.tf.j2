data "awscc_cloudtrail_channel" "example" {
    channel_arn = "{{ channel_arn }}"
    destinations = "{
    location = {{ location }}
    type = {{ type }}
}"
    id = "{{ id }}"
    name = "{{ name }}"
    source = "{{ source }}"
    tags = "{
    key = {{ key }}
    value = {{ value }}
}"
}