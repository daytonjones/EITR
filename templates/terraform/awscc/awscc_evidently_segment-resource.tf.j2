resource "awscc_evidently_segment" "example" {
    arn = "{{ arn }}"
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    pattern = "{{ pattern }}"
    tags = "{
    key = {{ key }}
    value = {{ value }}
}"
}