resource "awscc_apigateway_resource" "example" {
    id = "{{ id }}"
    parent_id = "{{ parent_id }}"
    path_part = "{{ path_part }}"
    resource_id = "{{ resource_id }}"
    rest_api_id = "{{ rest_api_id }}"
}