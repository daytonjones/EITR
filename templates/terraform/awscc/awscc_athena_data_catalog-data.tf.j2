data "awscc_athena_data_catalog" "example" {
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    parameters = "{{ parameters }}"
    tags = "{
    key = {{ key }}
    value = {{ value }}
}"
    type = "{{ type }}"
}