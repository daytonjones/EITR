resource "awscc_lightsail_disk" "example" {
    add_ons = "{{ add_ons }}"
    availability_zone = "{{ availability_zone }}"
    disk_name = "{{ disk_name }}"
    location = "{{ location }}"
    size_in_gb = {{ size_in_gb }}
    tags = "{{ tags }}"
}
