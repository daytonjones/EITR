resource "awscc_entityresolution_id_mapping_workflow" "example" {
    description = "{{ description }}"
    id_mapping_incremental_run_config = "{{ id_mapping_incremental_run_config }}"
    id_mapping_techniques = "{{ id_mapping_techniques }}"
    input_source_config = "{{ input_source_config }}"
    output_source_config = "{{ output_source_config }}"
    role_arn = "{{ role_arn }}"
    tags = "{{ tags }}"
    workflow_name = "{{ workflow_name }}"
}
