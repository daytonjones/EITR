resource "awscc_msk_vpc_connection" "example" {
    authentication = "{{ authentication }}"
    client_subnets = ["{{ client_subnets }}"]
    security_groups = ["{{ security_groups }}"]
    tags = {
        key = "{{ tags_value }}"
      }
    target_cluster_arn = "{{ target_cluster_arn }}"
    vpc_id = "{{ vpc_id }}"
}
