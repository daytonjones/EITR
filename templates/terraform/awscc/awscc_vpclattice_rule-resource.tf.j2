resource "awscc_vpclattice_rule" "example" {
    action = "{{ action }}"
    listener_identifier = "{{ listener_identifier }}"
    match = "{{ match }}"
    name = "{{ name }}"
    priority = {{ priority }}
    service_identifier = "{{ service_identifier }}"
    tags = "{{ tags }}"
}
