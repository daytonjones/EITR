resource "awscc_connect_view" "example" {
    actions = ["{{ actions }}"]
    description = "{{ description }}"
    instance_arn = "{{ instance_arn }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    template = "{{ template }}"
}
