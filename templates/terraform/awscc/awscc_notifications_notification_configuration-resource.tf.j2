resource "awscc_notifications_notification_configuration" "example" {
    aggregation_duration = "{{ aggregation_duration }}"
    description = "{{ description }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
}
