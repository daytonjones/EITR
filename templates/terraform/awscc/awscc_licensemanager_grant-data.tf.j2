data "awscc_licensemanager_grant" "example" {
    allowed_operations = "{{ allowed_operations }}"
    grant_arn = "{{ grant_arn }}"
    grant_name = "{{ grant_name }}"
    home_region = "{{ home_region }}"
    id = "{{ id }}"
    license_arn = "{{ license_arn }}"
    principals = "{{ principals }}"
    status = "{{ status }}"
    version = "{{ version }}"
}