data "awscc_organizations_organizational_unit" "example" {
    arn = "{{ arn }}"
    id = "{{ id }}"
    name = "{{ name }}"
    organizational_unit_id = "{{ organizational_unit_id }}"
    parent_id = "{{ parent_id }}"
    tags = "{
    key = {{ key }}
    value = {{ value }}
}"
}