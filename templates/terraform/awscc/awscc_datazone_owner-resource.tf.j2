resource "awscc_datazone_owner" "example" {
    domain_identifier = "{{ domain_identifier }}"
    entity_identifier = "{{ entity_identifier }}"
    entity_type = "{{ entity_type }}"
    owner = "{{ owner }}"
}
