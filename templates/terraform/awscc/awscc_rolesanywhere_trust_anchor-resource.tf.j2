resource "awscc_rolesanywhere_trust_anchor" "example" {
    enabled = {{ enabled }}
    name = "{{ name }}"
    notification_settings = "{{ notification_settings }}"
    source = "{{ source }}"
    tags = "{{ tags }}"
}
