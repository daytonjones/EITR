resource "awscc_iam_managed_policy" "example" {
    description = "{{ description }}"
    groups = ["{{ groups }}"]
    managed_policy_name = "{{ managed_policy_name }}"
    path = "{{ path }}"
    policy_document = "{{ policy_document }}"
    roles = ["{{ roles }}"]
    users = ["{{ users }}"]
}
