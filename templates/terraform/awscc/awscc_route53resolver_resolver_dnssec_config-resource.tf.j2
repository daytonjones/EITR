resource "awscc_route53resolver_resolver_dnssec_config" "example" {
    id = "{{ id }}"
    owner_id = "{{ owner_id }}"
    resolver_dnssec_config_id = "{{ resolver_dnssec_config_id }}"
    resource_id = "{{ resource_id }}"
    validation_status = "{{ validation_status }}"
}