data "awscc_entityresolution_policy_statement" "example" {
    action = "{{ action }}"
    arn = "{{ arn }}"
    condition = "{{ condition }}"
    effect = "{{ effect }}"
    id = "{{ id }}"
    principal = "{{ principal }}"
    statement_id = "{{ statement_id }}"
}