resource "awscc_ec2_prefix_list" "example" {
    address_family = "{{ address_family }}"
    entries = "{{ entries }}"
    max_entries = {{ max_entries }}
    prefix_list_name = "{{ prefix_list_name }}"
    tags = "{{ tags }}"
}
