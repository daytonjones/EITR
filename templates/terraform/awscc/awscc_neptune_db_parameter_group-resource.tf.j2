resource "awscc_neptune_db_parameter_group" "example" {
    description = "{{ description }}"
    family = "{{ family }}"
    name = "{{ name }}"
    parameters = "{{ parameters }}"
    tags = "{{ tags }}"
}
