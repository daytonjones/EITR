resource "awscc_deadline_queue_environment" "example" {
    farm_id = "{{ farm_id }}"
    priority = {{ priority }}
    queue_id = "{{ queue_id }}"
    template = "{{ template }}"
    template_type = "{{ template_type }}"
}
