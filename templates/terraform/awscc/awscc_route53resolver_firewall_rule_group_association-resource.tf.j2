resource "awscc_route53resolver_firewall_rule_group_association" "example" {
    firewall_rule_group_id = "{{ firewall_rule_group_id }}"
    mutation_protection = "{{ mutation_protection }}"
    name = "{{ name }}"
    priority = {{ priority }}
    tags = "{{ tags }}"
    vpc_id = "{{ vpc_id }}"
}
