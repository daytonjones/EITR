resource "awscc_cleanrooms_privacy_budget_template" "example" {
    auto_refresh = "{{ auto_refresh }}"
    membership_identifier = "{{ membership_identifier }}"
    parameters = "{{ parameters }}"
    privacy_budget_type = "{{ privacy_budget_type }}"
    tags = "{{ tags }}"
}
