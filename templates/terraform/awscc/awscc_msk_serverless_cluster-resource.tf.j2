resource "awscc_msk_serverless_cluster" "example" {
    client_authentication = "{{ client_authentication }}"
    cluster_name = "{{ cluster_name }}"
    tags = {
        key = "{{ tags_value }}"
      }
    vpc_configs = "{{ vpc_configs }}"
}
