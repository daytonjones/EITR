data "awscc_lex_resource_policy" "example" {
    id = "{{ id }}"
    policy = "{{ policy }}"
    resource_arn = "{{ resource_arn }}"
    resource_policy_id = "{{ resource_policy_id }}"
    revision_id = "{{ revision_id }}"
}