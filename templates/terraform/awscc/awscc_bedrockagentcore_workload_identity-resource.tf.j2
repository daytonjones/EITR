resource "awscc_bedrockagentcore_workload_identity" "example" {
    allowed_resource_oauth_2_return_urls = ["{{ allowed_resource_oauth_2_return_urls }}"]
    name = "{{ name }}"
    tags = "{{ tags }}"
}
