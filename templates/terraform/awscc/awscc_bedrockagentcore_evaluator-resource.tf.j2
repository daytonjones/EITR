resource "awscc_bedrockagentcore_evaluator" "example" {
    description = "{{ description }}"
    evaluator_config = "{{ evaluator_config }}"
    evaluator_name = "{{ evaluator_name }}"
    level = "{{ level }}"
    tags = "{{ tags }}"
}
