resource "awscc_omics_reference_store" "example" {
    description = "{{ description }}"
    name = "{{ name }}"
    sse_config = "{{ sse_config }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
