resource "awscc_s3outposts_endpoint" "example" {
    access_type = "{{ access_type }}"
    customer_owned_ipv_4_pool = "{{ customer_owned_ipv_4_pool }}"
    failed_reason = "{{ failed_reason }}"
    outpost_id = "{{ outpost_id }}"
    security_group_id = "{{ security_group_id }}"
    subnet_id = "{{ subnet_id }}"
}
