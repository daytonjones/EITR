data "awscc_resourcegroups_tag_sync_task" "example" {
    group = "{{ group }}"
    group_arn = "{{ group_arn }}"
    group_name = "{{ group_name }}"
    id = "{{ id }}"
    role_arn = "{{ role_arn }}"
    status = "{{ status }}"
    tag_key = "{{ tag_key }}"
    tag_value = "{{ tag_value }}"
    task_arn = "{{ task_arn }}"
}