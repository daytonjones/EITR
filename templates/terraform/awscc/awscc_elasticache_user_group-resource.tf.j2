resource "awscc_elasticache_user_group" "example" {
    arn = "{{ arn }}"
    engine = "{{ engine }}"
    id = "{{ id }}"
    status = "{{ status }}"
    tags = "{
    key = {{ key }}
    value = {{ value }}
}"
    user_group_id = "{{ user_group_id }}"
    user_ids = "{{ user_ids }}"
}