resource "awscc_macie_custom_data_identifier" "example" {
    description = "{{ description }}"
    ignore_words = ["{{ ignore_words }}"]
    keywords = ["{{ keywords }}"]
    maximum_match_distance = {{ maximum_match_distance }}
    name = "{{ name }}"
    regex = "{{ regex }}"
    tags = "{{ tags }}"
}
