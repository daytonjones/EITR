resource "awscc_servicecatalog_launch_role_constraint" "example" {
    accept_language = "{{ accept_language }}"
    description = "{{ description }}"
    local_role_name = "{{ local_role_name }}"
    portfolio_id = "{{ portfolio_id }}"
    product_id = "{{ product_id }}"
    role_arn = "{{ role_arn }}"
}
