resource "awscc_bedrock_data_automation_library" "example" {
    encryption_configuration = "{{ encryption_configuration }}"
    library_description = "{{ library_description }}"
    library_name = "{{ library_name }}"
    tags = "{{ tags }}"
}
