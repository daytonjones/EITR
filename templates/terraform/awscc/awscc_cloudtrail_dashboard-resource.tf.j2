resource "awscc_cloudtrail_dashboard" "example" {
    name = "{{ name }}"
    refresh_schedule = "{{ refresh_schedule }}"
    tags = "{{ tags }}"
    termination_protection_enabled = {{ termination_protection_enabled }}
    widgets = "{{ widgets }}"
}
