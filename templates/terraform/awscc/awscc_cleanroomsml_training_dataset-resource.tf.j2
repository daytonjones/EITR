resource "awscc_cleanroomsml_training_dataset" "example" {
    description = "{{ description }}"
    name = "{{ name }}"
    role_arn = "{{ role_arn }}"
    tags = "{{ tags }}"
    training_data = "{{ training_data }}"
}
