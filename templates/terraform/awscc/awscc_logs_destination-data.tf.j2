data "awscc_logs_destination" "example" {
    arn = "{{ arn }}"
    destination_name = "{{ destination_name }}"
    destination_policy = "{{ destination_policy }}"
    id = "{{ id }}"
    role_arn = "{{ role_arn }}"
    target_arn = "{{ target_arn }}"
}