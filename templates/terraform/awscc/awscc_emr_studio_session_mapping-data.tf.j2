data "awscc_emr_studio_session_mapping" "example" {
    id = "{{ id }}"
    identity_name = "{{ identity_name }}"
    identity_type = "{{ identity_type }}"
    session_policy_arn = "{{ session_policy_arn }}"
    studio_id = "{{ studio_id }}"
}