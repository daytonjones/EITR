data "awscc_ec2_vpc_gateway_attachment" "example" {
    attachment_type = "{{ attachment_type }}"
    id = "{{ id }}"
    internet_gateway_id = "{{ internet_gateway_id }}"
    vpc_id = "{{ vpc_id }}"
    vpn_gateway_id = "{{ vpn_gateway_id }}"
}