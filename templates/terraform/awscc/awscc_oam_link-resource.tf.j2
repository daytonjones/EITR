resource "awscc_oam_link" "example" {
    label_template = "{{ label_template }}"
    link_configuration = "{{ link_configuration }}"
    resource_types = ["{{ resource_types }}"]
    sink_identifier = "{{ sink_identifier }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
