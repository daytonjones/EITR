resource "awscc_omics_variant_store" "example" {
    description = "{{ description }}"
    name = "{{ name }}"
    reference = "{{ reference }}"
    sse_config = "{{ sse_config }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
