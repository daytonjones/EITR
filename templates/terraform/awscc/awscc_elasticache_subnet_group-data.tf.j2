data "awscc_elasticache_subnet_group" "example" {
    cache_subnet_group_name = "{{ cache_subnet_group_name }}"
    description = "{{ description }}"
    id = "{{ id }}"
    subnet_ids = "{{ subnet_ids }}"
    tags = "{
    key = {{ key }}
    value = {{ value }}
}"
}