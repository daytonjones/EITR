resource "awscc_robomaker_robot" "example" {
    architecture = "{{ architecture }}"
    fleet = "{{ fleet }}"
    greengrass_group_id = "{{ greengrass_group_id }}"
    name = "{{ name }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
