resource "awscc_appconfig_extension" "example" {
    actions = "{{ actions }}"
    description = "{{ description }}"
    latest_version_number = {{ latest_version_number }}
    name = "{{ name }}"
    parameters = "{{ parameters }}"
    tags = "{{ tags }}"
}
