resource "tfmigrate_github_pr" "example" {
    destin_branch = "{{ destin_branch }}"
    pr_body = "{{ pr_body }}"
    pr_title = "{{ pr_title }}"
    pull_request_url = "{{ pull_request_url }}"
    repo_identifier = "{{ repo_identifier }}"
    source_branch = "{{ source_branch }}"
    summary = "{{ summary }}"
}