resource "tfmigrate_update_backend" "example" {
    backend_file_name = "{{ backend_file_name }}"
    directory_path = "{{ directory_path }}"
    org = "{{ org }}"
    project = "{{ project }}"
    tags = "{{ tags }}"
    workspace_map = "{{ workspace_map }}"
}