resource "tfmigrate_git_pr" "example" {
    destin_branch = "{{ destin_branch }}"
    pr_body = "{{ pr_body }}"
    pr_title = "{{ pr_title }}"
    repo_identifier = "{{ repo_identifier }}"
    source_branch = "{{ source_branch }}"
}
