data "consul_config_entry" "example" {
    config_json = "{{ config_json }}"
    id = "{{ id }}"
    kind = "{{ kind }}"
    name = "{{ name }}"
    namespace = "{{ namespace }}"
    partition = "{{ partition }}"
}