data "consul_acl_auth_method" "example" {
    config = "{{ config }}"
    config_json = "{{ config_json }}"
    description = "{{ description }}"
    display_name = "{{ display_name }}"
    id = "{{ id }}"
    max_token_ttl = "{{ max_token_ttl }}"
    name = "{{ name }}"
    namespace = "{{ namespace }}"
    namespace_rule = "{{ namespace_rule }}"
    partition = "{{ partition }}"
    token_locality = "{{ token_locality }}"
    type = "{{ type }}"
}