resource "consul_certificate_authority" "example" {
    config = {
        key = "{{ config_value }}"
      }
    config_json = "{{ config_json }}"
    connect_provider = "{{ connect_provider }}"
    id = "{{ id }}"
}
