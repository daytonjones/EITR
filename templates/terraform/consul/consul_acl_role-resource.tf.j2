resource "consul_acl_role" "example" {
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    namespace = "{{ namespace }}"
    partition = "{{ partition }}"
    policies = "{{ policies }}"
}