resource "consul_peering_token" "example" {
    id = "{{ id }}"
    meta = {
        key = "{{ meta_value }}"
      }
    partition = "{{ partition }}"
    peer_name = "{{ peer_name }}"
    server_external_addresses = ["{{ server_external_addresses }}"]
}
