resource "consul_acl_token" "example" {
    accessor_id = "{{ accessor_id }}"
    description = "{{ description }}"
    expiration_time = "{{ expiration_time }}"
    id = "{{ id }}"
    local = "{{ local }}"
    namespace = "{{ namespace }}"
    partition = "{{ partition }}"
    policies = "{{ policies }}"
    roles = "{{ roles }}"
}