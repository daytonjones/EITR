data "consul_service_health" "example" {
    datacenter = "{{ datacenter }}"
    filter = "{{ filter }}"
    id = "{{ id }}"
    name = "{{ name }}"
    near = "{{ near }}"
    node_meta = {
        key = "{{ node_meta_value }}"
      }
    passing = {{ passing }}
    tag = "{{ tag }}"
    wait_for = "{{ wait_for }}"
}
