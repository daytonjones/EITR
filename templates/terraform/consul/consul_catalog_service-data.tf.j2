data "consul_catalog_service" "example" {
    datacenter = "{{ datacenter }}"
    filter = "{{ filter }}"
    id = "{{ id }}"
    name = "{{ name }}"
    service = "{{ service }}"
    tag = "{{ tag }}"
}