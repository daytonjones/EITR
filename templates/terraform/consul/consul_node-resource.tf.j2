resource "consul_node" "example" {
    address = "{{ address }}"
    datacenter = "{{ datacenter }}"
    id = "{{ id }}"
    meta = "{{ meta }}"
    name = "{{ name }}"
    partition = "{{ partition }}"
    token = "{{ token }}"
}