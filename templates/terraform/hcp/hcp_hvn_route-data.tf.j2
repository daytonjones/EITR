data "hcp_hvn_route" "example" {
    hvn_link = "{{ hvn_link }}"
    hvn_route_id = "{{ hvn_route_id }}"
    id = "{{ id }}"
    project_id = "{{ project_id }}"
    timeouts {
      default = "{{ default }}"
    }
}
