resource "hcp_vault_cluster_admin_token" "example" {
    cluster_id = "{{ cluster_id }}"
    id = "{{ id }}"
    project_id = "{{ project_id }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      read = "{{ read }}"
    }
}
