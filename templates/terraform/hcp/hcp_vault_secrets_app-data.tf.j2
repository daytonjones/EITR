data "hcp_vault_secrets_app" "example" {
    app_name = "{{ app_name }}"
    id = "{{ id }}"
    organization_id = "{{ organization_id }}"
    project_id = "{{ project_id }}"
    secrets = "{{ secrets }}"
}