resource "hcp_hvn_peering_connection" "example" {
    hvn_1 = "{{ hvn_1 }}"
    hvn_2 = "{{ hvn_2 }}"
    id = "{{ id }}"
    project_id = "{{ project_id }}"
    timeouts {
      create = "{{ create }}"
      default = "{{ default }}"
      delete = "{{ delete }}"
    }
}
