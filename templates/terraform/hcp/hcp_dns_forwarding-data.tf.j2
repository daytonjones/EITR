data "hcp_dns_forwarding" "example" {
    dns_forwarding_id = "{{ dns_forwarding_id }}"
    hvn_id = "{{ hvn_id }}"
    id = "{{ id }}"
    project_id = "{{ project_id }}"
    timeouts {
      read = "{{ read }}"
    }
}
