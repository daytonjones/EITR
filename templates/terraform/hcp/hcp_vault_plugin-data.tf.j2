data "hcp_vault_plugin" "example" {
    cluster_id = "{{ cluster_id }}"
    id = "{{ id }}"
    plugin_name = "{{ plugin_name }}"
    plugin_type = "{{ plugin_type }}"
    project_id = "{{ project_id }}"
}