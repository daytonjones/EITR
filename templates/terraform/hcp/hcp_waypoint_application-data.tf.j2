data "hcp_waypoint_application" "example" {
    actions = ["{{ actions }}"]
    id = "{{ id }}"
    input_variables = "{{ input_variables }}"
    name = "{{ name }}"
    project_id = "{{ project_id }}"
}
