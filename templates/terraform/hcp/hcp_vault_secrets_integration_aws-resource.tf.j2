resource "hcp_vault_secrets_integration_aws" "example" {
    access_keys = "{{ access_keys }}"
    capabilities = ["{{ capabilities }}"]
    federated_workload_identity = "{{ federated_workload_identity }}"
    name = "{{ name }}"
    project_id = "{{ project_id }}"
}
