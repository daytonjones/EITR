resource "hcp_vault_radar_integration_jira_connection" "example" {
    base_url = "{{ base_url }}"
    email = "{{ email }}"
    id = "{{ id }}"
    name = "{{ name }}"
    project_id = "{{ project_id }}"
    token = "{{ token }}"
}