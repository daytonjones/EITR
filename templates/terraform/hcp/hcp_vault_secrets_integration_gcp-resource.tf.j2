resource "hcp_vault_secrets_integration_gcp" "example" {
    capabilities = ["{{ capabilities }}"]
    federated_workload_identity = "{{ federated_workload_identity }}"
    name = "{{ name }}"
    project_id = "{{ project_id }}"
    service_account_key = "{{ service_account_key }}"
}
