resource "hcp_vault_radar_integration_jira_subscription" "example" {
    assignee = "{{ assignee }}"
    connection_id = "{{ connection_id }}"
    issue_type = "{{ issue_type }}"
    jira_project_key = "{{ jira_project_key }}"
    message = "{{ message }}"
    name = "{{ name }}"
    project_id = "{{ project_id }}"
}
