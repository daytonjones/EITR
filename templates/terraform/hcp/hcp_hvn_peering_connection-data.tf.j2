data "hcp_hvn_peering_connection" "example" {
    hvn_1 = "{{ hvn_1 }}"
    hvn_2 = "{{ hvn_2 }}"
    id = "{{ id }}"
    peering_id = "{{ peering_id }}"
    project_id = "{{ project_id }}"
    timeouts {
      default = "{{ default }}"
    }
}
