data "hcp_project" "example" {
    description = "{{ description }}"
    name = "{{ name }}"
    project = "{{ project }}"
    resource_id = "{{ resource_id }}"
    resource_name = "{{ resource_name }}"
}