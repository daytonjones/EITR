resource "hcp_packer_run_task" "example" {
    id = "{{ id }}"
    project_id = "{{ project_id }}"
    regenerate_hmac = {{ regenerate_hmac }}
    timeouts {
      create = "{{ create }}"
      default = "{{ default }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
