resource "vsphere_content_library_item" "example" {
    description = "{{ description }}"
    file_url = "{{ file_url }}"
    id = "{{ id }}"
    library_id = "{{ library_id }}"
    name = "{{ name }}"
    source_uuid = "{{ source_uuid }}"
    type = "{{ type }}"
}