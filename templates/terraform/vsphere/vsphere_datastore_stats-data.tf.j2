data "vsphere_datastore_stats" "example" {
    capacity = {
        key = "{{ capacity_value }}"
      }
    datacenter_id = "{{ datacenter_id }}"
    free_space = {
        key = "{{ free_space_value }}"
      }
    id = "{{ id }}"
}
