resource "vsphere_configuration_profile" "example" {
    cluster_id = "{{ cluster_id }}"
    configuration = "{{ configuration }}"
    id = "{{ id }}"
    reference_host_id = "{{ reference_host_id }}"
}
