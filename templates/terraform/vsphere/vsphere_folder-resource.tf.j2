resource "vsphere_folder" "example" {
    custom_attributes = "{{ custom_attributes }}"
    datacenter_id = "{{ datacenter_id }}"
    id = "{{ id }}"
    path = "{{ path }}"
    tags = "{{ tags }}"
    type = "{{ type }}"
}