resource "vsphere_compute_cluster_vm_dependency_rule" "example" {
    compute_cluster_id = "{{ compute_cluster_id }}"
    dependency_vm_group_name = "{{ dependency_vm_group_name }}"
    enabled = "{{ enabled }}"
    id = "{{ id }}"
    mandatory = "{{ mandatory }}"
    name = "{{ name }}"
    vm_group_name = "{{ vm_group_name }}"
}