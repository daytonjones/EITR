resource "vsphere_tag_category" "example" {
    associable_types = "{{ associable_types }}"
    cardinality = "{{ cardinality }}"
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
}