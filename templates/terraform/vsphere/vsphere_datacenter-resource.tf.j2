resource "vsphere_datacenter" "example" {
    custom_attributes = {
        key = "{{ custom_attributes_value }}"
      }
    folder = "{{ folder }}"
    id = "{{ id }}"
    name = "{{ name }}"
    tags = ["{{ tags }}"]
}
