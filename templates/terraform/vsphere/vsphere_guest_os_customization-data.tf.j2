data "vsphere_guest_os_customization" "example" {
    change_version = "{{ change_version }}"
    description = "{{ description }}"
    id = "{{ id }}"
    last_update_time = "{{ last_update_time }}"
    name = "{{ name }}"
    spec = "{{ spec }}"
    type = "{{ type }}"
}