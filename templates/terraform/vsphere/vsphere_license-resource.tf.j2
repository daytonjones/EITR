resource "vsphere_license" "example" {
    edition_key = "{{ edition_key }}"
    id = "{{ id }}"
    labels = "{{ labels }}"
    license_key = "{{ license_key }}"
    name = "{{ name }}"
    total = "{{ total }}"
    used = "{{ used }}"
}