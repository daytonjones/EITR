resource "azurerm_logic_app_trigger_custom" "example" {
    body = "{{ body }}"
    callback_url = "{{ callback_url }}"
    id = "{{ id }}"
    logic_app_id = "{{ logic_app_id }}"
    name = "{{ name }}"
}