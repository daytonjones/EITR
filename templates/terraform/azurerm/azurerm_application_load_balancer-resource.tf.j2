resource "azurerm_application_load_balancer" "example" {
    id = "{{ id }}"
    location = "{{ location }}"
    name = "{{ name }}"
    primary_configuration_endpoint = "{{ primary_configuration_endpoint }}"
    resource_group_name = "{{ resource_group_name }}"
    tags = "{{ tags }}"
}