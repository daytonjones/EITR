data "azurerm_ip_groups" "example" {
    id = "{{ id }}"
    ids = "{{ ids }}"
    location = "{{ location }}"
    name = "{{ name }}"
    names = "{{ names }}"
    resource_group_name = "{{ resource_group_name }}"
    tags = "{{ tags }}"
}