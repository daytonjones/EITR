resource "azurerm_web_app_active_slot" "example" {
    id = "{{ id }}"
    overwrite_network_config = {{ overwrite_network_config }}
    slot_id = "{{ slot_id }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      read = "{{ read }}"
      update = "{{ update }}"
    }
}
