data "azurerm_management_group" "example" {
    all_management_group_ids = "{{ all_management_group_ids }}"
    all_subscription_ids = "{{ all_subscription_ids }}"
    display_name = "{{ display_name }}"
    id = "{{ id }}"
    management_group_ids = "{{ management_group_ids }}"
    name = "{{ name }}"
    parent_management_group_id = "{{ parent_management_group_id }}"
    subscription_ids = "{{ subscription_ids }}"
    tenant_scoped_id = "{{ tenant_scoped_id }}"
}