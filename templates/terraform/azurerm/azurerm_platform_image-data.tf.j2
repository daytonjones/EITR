data "azurerm_platform_image" "example" {
    id = "{{ id }}"
    location = "{{ location }}"
    offer = "{{ offer }}"
    publisher = "{{ publisher }}"
    sku = "{{ sku }}"
    version = "{{ version }}"
}