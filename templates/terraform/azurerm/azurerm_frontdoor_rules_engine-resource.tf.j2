resource "azurerm_frontdoor_rules_engine" "example" {
    enabled = "{{ enabled }}"
    frontdoor_name = "{{ frontdoor_name }}"
    id = "{{ id }}"
    location = "{{ location }}"
    name = "{{ name }}"
    resource_group_name = "{{ resource_group_name }}"
}