data "azurerm_storage_table_entities" "example" {
    filter = "{{ filter }}"
    id = "{{ id }}"
    select = ["{{ select }}"]
    storage_table_id = "{{ storage_table_id }}"
    timeouts {
      read = "{{ read }}"
    }
}
