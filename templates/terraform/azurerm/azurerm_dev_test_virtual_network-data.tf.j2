data "azurerm_dev_test_virtual_network" "example" {
    id = "{{ id }}"
    lab_name = "{{ lab_name }}"
    name = "{{ name }}"
    resource_group_name = "{{ resource_group_name }}"
    timeouts {
      read = "{{ read }}"
    }
}
