resource "azurerm_signalr_service_custom_domain" "example" {
    domain_name = "{{ domain_name }}"
    id = "{{ id }}"
    name = "{{ name }}"
    signalr_custom_certificate_id = "{{ signalr_custom_certificate_id }}"
    signalr_service_id = "{{ signalr_service_id }}"
}