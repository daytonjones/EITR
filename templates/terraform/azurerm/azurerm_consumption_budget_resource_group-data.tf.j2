data "azurerm_consumption_budget_resource_group" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    resource_group_id = "{{ resource_group_id }}"
    timeouts {
      read = "{{ read }}"
    }
}
