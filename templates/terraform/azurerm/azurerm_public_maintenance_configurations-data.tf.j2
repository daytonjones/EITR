data "azurerm_public_maintenance_configurations" "example" {
    configs = "{{ configs }}"
    id = "{{ id }}"
    location = "{{ location }}"
    recur_every = "{{ recur_every }}"
    scope = "{{ scope }}"
}