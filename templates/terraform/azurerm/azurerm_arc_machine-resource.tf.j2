resource "azurerm_arc_machine" "example" {
    id = "{{ id }}"
    kind = "{{ kind }}"
    location = "{{ location }}"
    name = "{{ name }}"
    resource_group_name = "{{ resource_group_name }}"
    tags = "{{ tags }}"
}