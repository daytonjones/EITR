resource "azurerm_network_function_collector_policy" "example" {
    id = "{{ id }}"
    location = "{{ location }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    traffic_collector_id = "{{ traffic_collector_id }}"
}