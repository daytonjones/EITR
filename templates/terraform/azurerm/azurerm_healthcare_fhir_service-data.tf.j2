data "azurerm_healthcare_fhir_service" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    tags = {
        key = "{{ tags_value }}"
      }
    workspace_id = "{{ workspace_id }}"
    timeouts {
      read = "{{ read }}"
    }
}
