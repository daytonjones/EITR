resource "azurerm_web_pubsub_custom_domain" "example" {
    domain_name = "{{ domain_name }}"
    id = "{{ id }}"
    name = "{{ name }}"
    web_pubsub_custom_certificate_id = "{{ web_pubsub_custom_certificate_id }}"
    web_pubsub_id = "{{ web_pubsub_id }}"
}