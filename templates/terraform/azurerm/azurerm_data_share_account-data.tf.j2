data "azurerm_data_share_account" "example" {
    id = "{{ id }}"
    identity = "{{ identity }}"
    name = "{{ name }}"
    resource_group_name = "{{ resource_group_name }}"
    tags = "{{ tags }}"
}