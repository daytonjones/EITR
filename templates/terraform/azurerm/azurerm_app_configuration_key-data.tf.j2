data "azurerm_app_configuration_key" "example" {
    configuration_store_id = "{{ configuration_store_id }}"
    id = "{{ id }}"
    key = "{{ key }}"
    label = "{{ label }}"
    timeouts {
      read = "{{ read }}"
    }
}
