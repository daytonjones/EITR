data "azurerm_mobile_network_service" "example" {
    id = "{{ id }}"
    location = "{{ location }}"
    mobile_network_id = "{{ mobile_network_id }}"
    name = "{{ name }}"
    pcc_rule = "{{ pcc_rule }}"
    service_precedence = "{{ service_precedence }}"
    service_qos_policy = "{{ service_qos_policy }}"
    tags = "{{ tags }}"
}