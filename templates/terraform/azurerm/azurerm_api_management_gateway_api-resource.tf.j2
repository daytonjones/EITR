resource "azurerm_api_management_gateway_api" "example" {
    api_id = "{{ api_id }}"
    gateway_id = "{{ gateway_id }}"
    id = "{{ id }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      read = "{{ read }}"
    }
}
