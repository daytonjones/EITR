data "azurerm_images" "example" {
    id = "{{ id }}"
    resource_group_name = "{{ resource_group_name }}"
    tags_filter = {
        key = "{{ tags_filter_value }}"
      }
    timeouts {
      read = "{{ read }}"
    }
}
