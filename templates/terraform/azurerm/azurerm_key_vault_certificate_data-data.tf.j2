data "azurerm_key_vault_certificate_data" "example" {
    id = "{{ id }}"
    key_vault_id = "{{ key_vault_id }}"
    name = "{{ name }}"
    version = "{{ version }}"
    timeouts {
      read = "{{ read }}"
    }
}
