resource "azurerm_subnet_route_table_association" "example" {
    id = "{{ id }}"
    route_table_id = "{{ route_table_id }}"
    subnet_id = "{{ subnet_id }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      read = "{{ read }}"
    }
}
