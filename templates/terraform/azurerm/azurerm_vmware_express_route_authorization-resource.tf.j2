resource "azurerm_vmware_express_route_authorization" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    private_cloud_id = "{{ private_cloud_id }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      read = "{{ read }}"
    }
}
