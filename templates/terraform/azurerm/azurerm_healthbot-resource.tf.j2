resource "azurerm_healthbot" "example" {
    bot_management_portal_url = "{{ bot_management_portal_url }}"
    id = "{{ id }}"
    location = "{{ location }}"
    name = "{{ name }}"
    resource_group_name = "{{ resource_group_name }}"
    sku_name = "{{ sku_name }}"
    tags = "{{ tags }}"
}