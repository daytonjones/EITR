data "azurerm_confidential_ledger" "example" {
    azuread_based_service_principal = "{{ azuread_based_service_principal }}"
    certificate_based_security_principal = "{{ certificate_based_security_principal }}"
    id = "{{ id }}"
    identity_service_endpoint = "{{ identity_service_endpoint }}"
    ledger_endpoint = "{{ ledger_endpoint }}"
    ledger_type = "{{ ledger_type }}"
    location = "{{ location }}"
    name = "{{ name }}"
    resource_group_name = "{{ resource_group_name }}"
    tags = "{{ tags }}"
}