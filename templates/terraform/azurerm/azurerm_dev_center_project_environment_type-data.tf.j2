data "azurerm_dev_center_project_environment_type" "example" {
    dev_center_project_id = "{{ dev_center_project_id }}"
    id = "{{ id }}"
    name = "{{ name }}"
    timeouts {
      read = "{{ read }}"
    }
}
