data "azurerm_site_recovery_protection_container" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    recovery_fabric_name = "{{ recovery_fabric_name }}"
    recovery_vault_name = "{{ recovery_vault_name }}"
    resource_group_name = "{{ resource_group_name }}"
}