resource "azurerm_bot_channel_alexa" "example" {
    bot_name = "{{ bot_name }}"
    id = "{{ id }}"
    location = "{{ location }}"
    resource_group_name = "{{ resource_group_name }}"
    skill_id = "{{ skill_id }}"
}