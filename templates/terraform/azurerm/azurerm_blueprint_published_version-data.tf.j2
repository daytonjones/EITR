data "azurerm_blueprint_published_version" "example" {
    blueprint_name = "{{ blueprint_name }}"
    id = "{{ id }}"
    scope_id = "{{ scope_id }}"
    version = "{{ version }}"
    timeouts {
      read = "{{ read }}"
    }
}
