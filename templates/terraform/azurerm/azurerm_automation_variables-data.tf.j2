data "azurerm_automation_variables" "example" {
    automation_account_id = "{{ automation_account_id }}"
    bool = "{{ bool }}"
    datetime = "{{ datetime }}"
    encrypted = "{{ encrypted }}"
    id = "{{ id }}"
    int = "{{ int }}"
    null = "{{ null }}"
    object = "{{ object }}"
    string = "{{ string }}"
}