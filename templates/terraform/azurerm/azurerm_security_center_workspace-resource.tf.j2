resource "azurerm_security_center_workspace" "example" {
    id = "{{ id }}"
    scope = "{{ scope }}"
    workspace_id = "{{ workspace_id }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      read = "{{ read }}"
      update = "{{ update }}"
    }
}
