resource "azurerm_vmware_netapp_volume_attachment" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    netapp_volume_id = "{{ netapp_volume_id }}"
    vmware_cluster_id = "{{ vmware_cluster_id }}"
}