data "azurerm_stack_hci_cluster" "example" {
    automanage_configuration_id = "{{ automanage_configuration_id }}"
    client_id = "{{ client_id }}"
    cloud_id = "{{ cloud_id }}"
    id = "{{ id }}"
    identity = "{{ identity }}"
    location = "{{ location }}"
    name = "{{ name }}"
    resource_group_name = "{{ resource_group_name }}"
    resource_provider_object_id = "{{ resource_provider_object_id }}"
    service_endpoint = "{{ service_endpoint }}"
    tags = "{{ tags }}"
    tenant_id = "{{ tenant_id }}"
}