data "azurerm_marketplace_agreement" "example" {
    id = "{{ id }}"
    offer = "{{ offer }}"
    plan = "{{ plan }}"
    publisher = "{{ publisher }}"
    timeouts {
      read = "{{ read }}"
    }
}
