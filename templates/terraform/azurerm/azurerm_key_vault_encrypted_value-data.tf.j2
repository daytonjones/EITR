data "azurerm_key_vault_encrypted_value" "example" {
    algorithm = "{{ algorithm }}"
    encrypted_data = "{{ encrypted_data }}"
    id = "{{ id }}"
    key_vault_key_id = "{{ key_vault_key_id }}"
    plain_text_value = "{{ plain_text_value }}"
    timeouts {
      read = "{{ read }}"
    }
}
