data "azurerm_aadb2c_directory" "example" {
    domain_name = "{{ domain_name }}"
    id = "{{ id }}"
    resource_group_name = "{{ resource_group_name }}"
    timeouts {
      read = "{{ read }}"
    }
}
