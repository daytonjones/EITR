resource "azurerm_container_registry_cache_rule" "example" {
    container_registry_id = "{{ container_registry_id }}"
    credential_set_id = "{{ credential_set_id }}"
    id = "{{ id }}"
    name = "{{ name }}"
    source_repo = "{{ source_repo }}"
    target_repo = "{{ target_repo }}"
}