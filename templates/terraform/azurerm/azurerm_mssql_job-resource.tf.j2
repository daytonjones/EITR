resource "azurerm_mssql_job" "example" {
    description = "{{ description }}"
    id = "{{ id }}"
    job_agent_id = "{{ job_agent_id }}"
    name = "{{ name }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      read = "{{ read }}"
      update = "{{ update }}"
    }
}
