resource "azurerm_mssql_outbound_firewall_rule" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    server_id = "{{ server_id }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      read = "{{ read }}"
    }
}
