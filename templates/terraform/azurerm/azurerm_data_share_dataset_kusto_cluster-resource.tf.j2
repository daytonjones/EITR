resource "azurerm_data_share_dataset_kusto_cluster" "example" {
    id = "{{ id }}"
    kusto_cluster_id = "{{ kusto_cluster_id }}"
    name = "{{ name }}"
    share_id = "{{ share_id }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      read = "{{ read }}"
    }
}
