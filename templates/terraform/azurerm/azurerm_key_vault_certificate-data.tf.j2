data "azurerm_key_vault_certificate" "example" {
    id = "{{ id }}"
    key_vault_id = "{{ key_vault_id }}"
    name = "{{ name }}"
    version = "{{ version }}"
    timeouts {
      read = "{{ read }}"
    }
}
