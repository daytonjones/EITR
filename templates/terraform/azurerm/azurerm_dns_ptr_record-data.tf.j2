data "azurerm_dns_ptr_record" "example" {
    fqdn = "{{ fqdn }}"
    id = "{{ id }}"
    name = "{{ name }}"
    records = "{{ records }}"
    resource_group_name = "{{ resource_group_name }}"
    tags = "{{ tags }}"
    ttl = "{{ ttl }}"
    zone_name = "{{ zone_name }}"
}