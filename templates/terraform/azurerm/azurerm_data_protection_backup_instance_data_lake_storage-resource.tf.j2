resource "azurerm_data_protection_backup_instance_data_lake_storage" "example" {
    backup_policy_data_lake_storage_id = "{{ backup_policy_data_lake_storage_id }}"
    data_protection_backup_vault_id = "{{ data_protection_backup_vault_id }}"
    id = "{{ id }}"
    location = "{{ location }}"
    name = "{{ name }}"
    storage_account_id = "{{ storage_account_id }}"
    storage_container_names = ["{{ storage_container_names }}"]
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      read = "{{ read }}"
      update = "{{ update }}"
    }
}
