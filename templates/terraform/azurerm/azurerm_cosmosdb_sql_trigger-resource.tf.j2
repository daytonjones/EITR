resource "azurerm_cosmosdb_sql_trigger" "example" {
    body = "{{ body }}"
    container_id = "{{ container_id }}"
    id = "{{ id }}"
    name = "{{ name }}"
    operation = "{{ operation }}"
    type = "{{ type }}"
}