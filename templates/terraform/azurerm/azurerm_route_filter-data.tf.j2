data "azurerm_route_filter" "example" {
    id = "{{ id }}"
    location = "{{ location }}"
    name = "{{ name }}"
    resource_group_name = "{{ resource_group_name }}"
    rule = "{{ rule }}"
    tags = "{{ tags }}"
}