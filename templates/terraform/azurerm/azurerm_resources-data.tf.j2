data "azurerm_resources" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    required_tags = {
        key = "{{ required_tags_value }}"
      }
    resource_group_name = "{{ resource_group_name }}"
    type = "{{ type }}"
    timeouts {
      read = "{{ read }}"
    }
}
