resource "azurerm_data_share_dataset_kusto_database" "example" {
    id = "{{ id }}"
    kusto_database_id = "{{ kusto_database_id }}"
    name = "{{ name }}"
    share_id = "{{ share_id }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      read = "{{ read }}"
    }
}
