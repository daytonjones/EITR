data "azurerm_attestation_provider" "example" {
    attestation_uri = "{{ attestation_uri }}"
    id = "{{ id }}"
    location = "{{ location }}"
    name = "{{ name }}"
    resource_group_name = "{{ resource_group_name }}"
    tags = "{{ tags }}"
    trust_model = "{{ trust_model }}"
}