ephemeral_resource "azurerm_key_vault_secret" "example" {
    expiration_date = "{{ expiration_date }}"
    key_vault_id = "{{ key_vault_id }}"
    name = "{{ name }}"
    not_before_date = "{{ not_before_date }}"
    value = "{{ value }}"
    version = "{{ version }}"
}