resource "azurerm_storage_sync_group" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    storage_sync_id = "{{ storage_sync_id }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      read = "{{ read }}"
    }
}
