resource "time_static" "example" {
    day = "{{ day }}"
    hour = "{{ hour }}"
    id = "{{ id }}"
    minute = "{{ minute }}"
    month = "{{ month }}"
    rfc3339 = "{{ rfc3339 }}"
    second = "{{ second }}"
    triggers = "{{ triggers }}"
    unix = "{{ unix }}"
    year = "{{ year }}"
}