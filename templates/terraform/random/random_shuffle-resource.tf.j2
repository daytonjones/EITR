resource "random_shuffle" "example" {
    id = "{{ id }}"
    input = "{{ input }}"
    keepers = "{{ keepers }}"
    result = "{{ result }}"
    result_count = "{{ result_count }}"
    seed = "{{ seed }}"
}