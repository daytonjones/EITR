provider "salesforce" {
    api_version = "{{ api_version }}"
    client_id = "{{ client_id }}"
    login_url = "{{ login_url }}"
    private_key = "{{ private_key }}"
    username = "{{ username }}"
}