resource "google_dataform_repository_iam_policy" "example" {
    id = "{{ id }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    region = "{{ region }}"
    repository = "{{ repository }}"
}
