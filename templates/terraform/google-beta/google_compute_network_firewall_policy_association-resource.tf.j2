resource "google_compute_network_firewall_policy_association" "example" {
    attachment_target = "{{ attachment_target }}"
    firewall_policy = "{{ firewall_policy }}"
    id = "{{ id }}"
    name = "{{ name }}"
    project = "{{ project }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
    }
}
