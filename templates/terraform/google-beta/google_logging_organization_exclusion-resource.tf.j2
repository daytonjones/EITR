resource "google_logging_organization_exclusion" "example" {
    description = "{{ description }}"
    disabled = "{{ disabled }}"
    filter = "{{ filter }}"
    id = "{{ id }}"
    name = "{{ name }}"
    org_id = "{{ org_id }}"
}