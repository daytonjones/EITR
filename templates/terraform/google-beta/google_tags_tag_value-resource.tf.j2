resource "google_tags_tag_value" "example" {
    description = "{{ description }}"
    id = "{{ id }}"
    parent = "{{ parent }}"
    short_name = "{{ short_name }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
