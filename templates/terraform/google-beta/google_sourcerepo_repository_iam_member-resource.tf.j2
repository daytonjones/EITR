resource "google_sourcerepo_repository_iam_member" "example" {
    id = "{{ id }}"
    member = "{{ member }}"
    project = "{{ project }}"
    repository = "{{ repository }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
