data "google_monitoring_notification_channel" "example" {
    display_name = "{{ display_name }}"
    id = "{{ id }}"
    labels = {
        key = "{{ labels_value }}"
      }
    project = "{{ project }}"
    type = "{{ type }}"
    user_labels = {
        key = "{{ user_labels_value }}"
      }
}
