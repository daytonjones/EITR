resource "google_dataplex_glossary" "example" {
    description = "{{ description }}"
    display_name = "{{ display_name }}"
    glossary_id = "{{ glossary_id }}"
    id = "{{ id }}"
    labels = {
        key = "{{ labels_value }}"
      }
    location = "{{ location }}"
    project = "{{ project }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
