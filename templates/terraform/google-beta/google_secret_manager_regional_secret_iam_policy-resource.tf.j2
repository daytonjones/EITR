resource "google_secret_manager_regional_secret_iam_policy" "example" {
    id = "{{ id }}"
    location = "{{ location }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    secret_id = "{{ secret_id }}"
}
