resource "google_compute_project_metadata_item" "example" {
    id = "{{ id }}"
    key = "{{ key }}"
    project = "{{ project }}"
    value = "{{ value }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
