resource "google_cloudbuild_bitbucket_server_config" "example" {
    api_key = "{{ api_key }}"
    config_id = "{{ config_id }}"
    host_uri = "{{ host_uri }}"
    id = "{{ id }}"
    location = "{{ location }}"
    name = "{{ name }}"
    peered_network = "{{ peered_network }}"
    project = "{{ project }}"
    ssl_ca = "{{ ssl_ca }}"
    username = "{{ username }}"
    webhook_key = "{{ webhook_key }}"
}