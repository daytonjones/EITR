resource "google_compute_machine_image_iam_policy" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    machine_image = "{{ machine_image }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
}