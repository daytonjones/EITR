resource "google_kms_key_handle" "example" {
    id = "{{ id }}"
    kms_key = "{{ kms_key }}"
    location = "{{ location }}"
    name = "{{ name }}"
    project = "{{ project }}"
    resource_type_selector = "{{ resource_type_selector }}"
}