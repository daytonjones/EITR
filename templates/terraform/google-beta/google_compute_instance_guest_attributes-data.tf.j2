data "google_compute_instance_guest_attributes" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    project = "{{ project }}"
    query_path = "{{ query_path }}"
    region = "{{ region }}"
    variable_key = "{{ variable_key }}"
    zone = "{{ zone }}"
}
