resource "google_firebase_hosting_release" "example" {
    channel_id = "{{ channel_id }}"
    id = "{{ id }}"
    message = "{{ message }}"
    name = "{{ name }}"
    release_id = "{{ release_id }}"
    site_id = "{{ site_id }}"
    type = "{{ type }}"
    version_name = "{{ version_name }}"
}