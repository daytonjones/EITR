resource "google_vertex_ai_endpoint_iam_member" "example" {
    endpoint = "{{ endpoint }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    member = "{{ member }}"
    project = "{{ project }}"
    role = "{{ role }}"
}