resource "google_healthcare_dicom_store_iam_member" "example" {
    dicom_store_id = "{{ dicom_store_id }}"
    id = "{{ id }}"
    member = "{{ member }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
