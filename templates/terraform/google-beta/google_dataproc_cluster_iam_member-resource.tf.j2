resource "google_dataproc_cluster_iam_member" "example" {
    cluster = "{{ cluster }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    member = "{{ member }}"
    project = "{{ project }}"
    region = "{{ region }}"
    role = "{{ role }}"
}