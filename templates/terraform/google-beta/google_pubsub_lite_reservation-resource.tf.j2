resource "google_pubsub_lite_reservation" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    project = "{{ project }}"
    region = "{{ region }}"
    throughput_capacity = "{{ throughput_capacity }}"
}