data "google_container_attached_install_manifest" "example" {
    cluster_id = "{{ cluster_id }}"
    id = "{{ id }}"
    location = "{{ location }}"
    platform_version = "{{ platform_version }}"
    project = "{{ project }}"
}
