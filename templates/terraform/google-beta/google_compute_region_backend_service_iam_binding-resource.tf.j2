resource "google_compute_region_backend_service_iam_binding" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    members = "{{ members }}"
    name = "{{ name }}"
    project = "{{ project }}"
    region = "{{ region }}"
    role = "{{ role }}"
}