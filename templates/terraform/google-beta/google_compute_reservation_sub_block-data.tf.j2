data "google_compute_reservation_sub_block" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    project = "{{ project }}"
    reservation = "{{ reservation }}"
    reservation_block = "{{ reservation_block }}"
    zone = "{{ zone }}"
}
