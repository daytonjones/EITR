resource "google_biglake_iceberg_table_iam_binding" "example" {
    catalog = "{{ catalog }}"
    id = "{{ id }}"
    members = ["{{ members }}"]
    name = "{{ name }}"
    namespace = "{{ namespace }}"
    project = "{{ project }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
