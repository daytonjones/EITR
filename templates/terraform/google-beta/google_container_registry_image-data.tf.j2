data "google_container_registry_image" "example" {
    digest = "{{ digest }}"
    id = "{{ id }}"
    image_url = "{{ image_url }}"
    name = "{{ name }}"
    project = "{{ project }}"
    region = "{{ region }}"
    tag = "{{ tag }}"
}