resource "google_compute_image_iam_member" "example" {
    id = "{{ id }}"
    image = "{{ image }}"
    member = "{{ member }}"
    project = "{{ project }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
