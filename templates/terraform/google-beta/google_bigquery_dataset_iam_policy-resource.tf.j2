resource "google_bigquery_dataset_iam_policy" "example" {
    dataset_id = "{{ dataset_id }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
}