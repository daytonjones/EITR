resource "google_gke_hub_scope_iam_policy" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    scope_id = "{{ scope_id }}"
}