resource "google_vertex_ai_feature_online_store_iam_policy" "example" {
    feature_online_store = "{{ feature_online_store }}"
    id = "{{ id }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    region = "{{ region }}"
}
