resource "google_compute_project_metadata" "example" {
    id = "{{ id }}"
    metadata = {
        key = "{{ metadata_value }}"
      }
    project = "{{ project }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
    }
}
