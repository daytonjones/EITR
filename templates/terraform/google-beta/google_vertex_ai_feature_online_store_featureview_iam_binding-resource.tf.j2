resource "google_vertex_ai_feature_online_store_featureview_iam_binding" "example" {
    feature_online_store = "{{ feature_online_store }}"
    feature_view = "{{ feature_view }}"
    id = "{{ id }}"
    members = ["{{ members }}"]
    project = "{{ project }}"
    region = "{{ region }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
