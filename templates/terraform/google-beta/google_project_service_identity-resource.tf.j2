resource "google_project_service_identity" "example" {
    id = "{{ id }}"
    project = "{{ project }}"
    service = "{{ service }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      read = "{{ read }}"
    }
}
