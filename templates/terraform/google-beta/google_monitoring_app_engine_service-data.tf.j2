data "google_monitoring_app_engine_service" "example" {
    display_name = "{{ display_name }}"
    id = "{{ id }}"
    module_id = "{{ module_id }}"
    name = "{{ name }}"
    project = "{{ project }}"
    service_id = "{{ service_id }}"
    telemetry = "{{ telemetry }}"
    user_labels = "{{ user_labels }}"
}