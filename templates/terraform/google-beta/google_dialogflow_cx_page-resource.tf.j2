resource "google_dialogflow_cx_page" "example" {
    display_name = "{{ display_name }}"
    id = "{{ id }}"
    language_code = "{{ language_code }}"
    name = "{{ name }}"
    parent = "{{ parent }}"
    transition_route_groups = "{{ transition_route_groups }}"
}