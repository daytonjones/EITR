resource "google_scc_v2_folder_notification_config" "example" {
    config_id = "{{ config_id }}"
    description = "{{ description }}"
    folder = "{{ folder }}"
    id = "{{ id }}"
    location = "{{ location }}"
    name = "{{ name }}"
    pubsub_topic = "{{ pubsub_topic }}"
    service_account = "{{ service_account }}"
}