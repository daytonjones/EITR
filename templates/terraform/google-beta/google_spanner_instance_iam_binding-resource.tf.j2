resource "google_spanner_instance_iam_binding" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    instance = "{{ instance }}"
    members = "{{ members }}"
    project = "{{ project }}"
    role = "{{ role }}"
}