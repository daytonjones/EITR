resource "google_access_context_manager_access_policy_iam_member" "example" {
    id = "{{ id }}"
    member = "{{ member }}"
    name = "{{ name }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
