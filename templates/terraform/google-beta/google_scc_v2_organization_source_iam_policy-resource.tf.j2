resource "google_scc_v2_organization_source_iam_policy" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    organization = "{{ organization }}"
    policy_data = "{{ policy_data }}"
    source = "{{ source }}"
}