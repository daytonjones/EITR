resource "google_logging_billing_account_bucket_config" "example" {
    billing_account = "{{ billing_account }}"
    bucket_id = "{{ bucket_id }}"
    description = "{{ description }}"
    id = "{{ id }}"
    lifecycle_state = "{{ lifecycle_state }}"
    location = "{{ location }}"
    name = "{{ name }}"
    retention_days = "{{ retention_days }}"
}