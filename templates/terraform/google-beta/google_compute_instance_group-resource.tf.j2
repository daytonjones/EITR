resource "google_compute_instance_group" "example" {
    description = "{{ description }}"
    id = "{{ id }}"
    instances = "{{ instances }}"
    name = "{{ name }}"
    network = "{{ network }}"
    project = "{{ project }}"
    self_link = "{{ self_link }}"
    size = "{{ size }}"
    zone = "{{ zone }}"
}