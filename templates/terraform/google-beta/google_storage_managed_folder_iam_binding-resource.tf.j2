resource "google_storage_managed_folder_iam_binding" "example" {
    bucket = "{{ bucket }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    managed_folder = "{{ managed_folder }}"
    members = "{{ members }}"
    role = "{{ role }}"
}