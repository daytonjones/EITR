resource "google_firebase_storage_bucket" "example" {
    bucket_id = "{{ bucket_id }}"
    id = "{{ id }}"
    project = "{{ project }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
    }
}
