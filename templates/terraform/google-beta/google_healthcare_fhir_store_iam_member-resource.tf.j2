resource "google_healthcare_fhir_store_iam_member" "example" {
    fhir_store_id = "{{ fhir_store_id }}"
    id = "{{ id }}"
    member = "{{ member }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
