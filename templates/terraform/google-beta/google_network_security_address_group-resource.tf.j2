resource "google_network_security_address_group" "example" {
    capacity = "{{ capacity }}"
    create_time = "{{ create_time }}"
    description = "{{ description }}"
    effective_labels = "{{ effective_labels }}"
    id = "{{ id }}"
    items = "{{ items }}"
    labels = "{{ labels }}"
    location = "{{ location }}"
    name = "{{ name }}"
    parent = "{{ parent }}"
    purpose = "{{ purpose }}"
    terraform_labels = "{{ terraform_labels }}"
    type = "{{ type }}"
    update_time = "{{ update_time }}"
}