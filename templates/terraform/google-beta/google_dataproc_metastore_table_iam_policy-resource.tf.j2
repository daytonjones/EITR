resource "google_dataproc_metastore_table_iam_policy" "example" {
    database_id = "{{ database_id }}"
    id = "{{ id }}"
    location = "{{ location }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    service_id = "{{ service_id }}"
    table = "{{ table }}"
}
