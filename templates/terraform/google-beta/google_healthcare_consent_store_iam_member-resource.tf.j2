resource "google_healthcare_consent_store_iam_member" "example" {
    consent_store_id = "{{ consent_store_id }}"
    dataset = "{{ dataset }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    member = "{{ member }}"
    role = "{{ role }}"
}