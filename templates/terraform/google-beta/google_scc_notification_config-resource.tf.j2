resource "google_scc_notification_config" "example" {
    config_id = "{{ config_id }}"
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    organization = "{{ organization }}"
    pubsub_topic = "{{ pubsub_topic }}"
    service_account = "{{ service_account }}"
}