resource "google_container_analysis_note_iam_policy" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    note = "{{ note }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
}