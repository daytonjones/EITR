resource "google_bigtable_table_iam_policy" "example" {
    id = "{{ id }}"
    instance_name = "{{ instance_name }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    table = "{{ table }}"
}
