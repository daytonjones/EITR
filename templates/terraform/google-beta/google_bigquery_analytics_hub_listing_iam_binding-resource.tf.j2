resource "google_bigquery_analytics_hub_listing_iam_binding" "example" {
    data_exchange_id = "{{ data_exchange_id }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    listing_id = "{{ listing_id }}"
    location = "{{ location }}"
    members = "{{ members }}"
    project = "{{ project }}"
    role = "{{ role }}"
}