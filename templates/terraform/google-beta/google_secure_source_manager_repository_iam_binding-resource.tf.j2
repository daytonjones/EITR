resource "google_secure_source_manager_repository_iam_binding" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    members = "{{ members }}"
    project = "{{ project }}"
    repository_id = "{{ repository_id }}"
    role = "{{ role }}"
}