resource "google_apigee_env_references" "example" {
    description = "{{ description }}"
    env_id = "{{ env_id }}"
    id = "{{ id }}"
    name = "{{ name }}"
    refers = "{{ refers }}"
    resource_type = "{{ resource_type }}"
}