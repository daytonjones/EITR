resource "google_vmwareengine_cluster" "example" {
    id = "{{ id }}"
    management = "{{ management }}"
    name = "{{ name }}"
    parent = "{{ parent }}"
    state = "{{ state }}"
    uid = "{{ uid }}"
}