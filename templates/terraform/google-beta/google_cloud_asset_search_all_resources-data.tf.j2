data "google_cloud_asset_search_all_resources" "example" {
    asset_types = "{{ asset_types }}"
    id = "{{ id }}"
    query = "{{ query }}"
    results = "{{ results }}"
    scope = "{{ scope }}"
}