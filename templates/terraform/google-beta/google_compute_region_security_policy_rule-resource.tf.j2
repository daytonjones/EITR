resource "google_compute_region_security_policy_rule" "example" {
    action = "{{ action }}"
    description = "{{ description }}"
    id = "{{ id }}"
    preview = "{{ preview }}"
    priority = "{{ priority }}"
    project = "{{ project }}"
    region = "{{ region }}"
    security_policy = "{{ security_policy }}"
}