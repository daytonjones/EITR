resource "google_secure_source_manager_instance_iam_member" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    instance_id = "{{ instance_id }}"
    location = "{{ location }}"
    member = "{{ member }}"
    project = "{{ project }}"
    role = "{{ role }}"
}