data "google_bigquery_analytics_hub_listing_iam_policy" "example" {
    data_exchange_id = "{{ data_exchange_id }}"
    id = "{{ id }}"
    listing_id = "{{ listing_id }}"
    location = "{{ location }}"
    project = "{{ project }}"
}
