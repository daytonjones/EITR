data "google_compute_instance_serial_port" "example" {
    contents = "{{ contents }}"
    id = "{{ id }}"
    instance = "{{ instance }}"
    port = "{{ port }}"
    project = "{{ project }}"
    zone = "{{ zone }}"
}