resource "google_data_loss_prevention_deidentify_template" "example" {
    create_time = "{{ create_time }}"
    description = "{{ description }}"
    display_name = "{{ display_name }}"
    id = "{{ id }}"
    name = "{{ name }}"
    parent = "{{ parent }}"
    template_id = "{{ template_id }}"
    update_time = "{{ update_time }}"
}