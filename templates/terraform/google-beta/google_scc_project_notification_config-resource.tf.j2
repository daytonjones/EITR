resource "google_scc_project_notification_config" "example" {
    config_id = "{{ config_id }}"
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    project = "{{ project }}"
    pubsub_topic = "{{ pubsub_topic }}"
    service_account = "{{ service_account }}"
}