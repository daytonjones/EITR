resource "google_firebase_app_check_service_config" "example" {
    enforcement_mode = "{{ enforcement_mode }}"
    id = "{{ id }}"
    name = "{{ name }}"
    project = "{{ project }}"
    service_id = "{{ service_id }}"
}