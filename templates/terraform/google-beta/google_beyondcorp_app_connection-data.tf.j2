data "google_beyondcorp_app_connection" "example" {
    application_endpoint = "{{ application_endpoint }}"
    connectors = "{{ connectors }}"
    display_name = "{{ display_name }}"
    effective_labels = "{{ effective_labels }}"
    gateway = "{{ gateway }}"
    id = "{{ id }}"
    labels = "{{ labels }}"
    name = "{{ name }}"
    project = "{{ project }}"
    region = "{{ region }}"
    terraform_labels = "{{ terraform_labels }}"
    type = "{{ type }}"
}