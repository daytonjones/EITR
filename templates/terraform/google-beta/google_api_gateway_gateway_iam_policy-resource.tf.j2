resource "google_api_gateway_gateway_iam_policy" "example" {
    gateway = "{{ gateway }}"
    id = "{{ id }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    region = "{{ region }}"
}
