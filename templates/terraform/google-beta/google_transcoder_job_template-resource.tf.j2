resource "google_transcoder_job_template" "example" {
    effective_labels = "{{ effective_labels }}"
    id = "{{ id }}"
    job_template_id = "{{ job_template_id }}"
    labels = "{{ labels }}"
    location = "{{ location }}"
    name = "{{ name }}"
    project = "{{ project }}"
    terraform_labels = "{{ terraform_labels }}"
}