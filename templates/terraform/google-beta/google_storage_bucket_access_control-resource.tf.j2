resource "google_storage_bucket_access_control" "example" {
    bucket = "{{ bucket }}"
    entity = "{{ entity }}"
    id = "{{ id }}"
    role = "{{ role }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
