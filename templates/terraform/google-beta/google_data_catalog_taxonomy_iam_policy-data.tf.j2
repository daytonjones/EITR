data "google_data_catalog_taxonomy_iam_policy" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    region = "{{ region }}"
    taxonomy = "{{ taxonomy }}"
}