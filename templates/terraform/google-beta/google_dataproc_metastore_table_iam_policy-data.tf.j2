data "google_dataproc_metastore_table_iam_policy" "example" {
    database_id = "{{ database_id }}"
    id = "{{ id }}"
    location = "{{ location }}"
    project = "{{ project }}"
    service_id = "{{ service_id }}"
    table = "{{ table }}"
}
