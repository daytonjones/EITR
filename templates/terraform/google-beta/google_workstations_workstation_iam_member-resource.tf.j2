resource "google_workstations_workstation_iam_member" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    member = "{{ member }}"
    project = "{{ project }}"
    role = "{{ role }}"
    workstation_cluster_id = "{{ workstation_cluster_id }}"
    workstation_config_id = "{{ workstation_config_id }}"
    workstation_id = "{{ workstation_id }}"
}