resource "google_data_catalog_taxonomy_iam_policy" "example" {
    id = "{{ id }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    region = "{{ region }}"
    taxonomy = "{{ taxonomy }}"
}
