resource "google_data_catalog_entry_group" "example" {
    description = "{{ description }}"
    display_name = "{{ display_name }}"
    entry_group_id = "{{ entry_group_id }}"
    id = "{{ id }}"
    project = "{{ project }}"
    region = "{{ region }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
