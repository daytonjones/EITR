resource "google_apigee_sharedflow" "example" {
    config_bundle = "{{ config_bundle }}"
    detect_md5hash = "{{ detect_md5hash }}"
    id = "{{ id }}"
    latest_revision_id = "{{ latest_revision_id }}"
    md5hash = "{{ md5hash }}"
    meta_data = "{{ meta_data }}"
    name = "{{ name }}"
    org_id = "{{ org_id }}"
    revision = "{{ revision }}"
}