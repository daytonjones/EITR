data "google_dataplex_asset_iam_policy" "example" {
    asset = "{{ asset }}"
    dataplex_zone = "{{ dataplex_zone }}"
    id = "{{ id }}"
    lake = "{{ lake }}"
    location = "{{ location }}"
    project = "{{ project }}"
}
