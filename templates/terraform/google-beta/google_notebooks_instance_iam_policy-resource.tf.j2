resource "google_notebooks_instance_iam_policy" "example" {
    id = "{{ id }}"
    instance_name = "{{ instance_name }}"
    location = "{{ location }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
}
