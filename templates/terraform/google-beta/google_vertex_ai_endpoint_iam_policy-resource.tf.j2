resource "google_vertex_ai_endpoint_iam_policy" "example" {
    endpoint = "{{ endpoint }}"
    id = "{{ id }}"
    location = "{{ location }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
}
