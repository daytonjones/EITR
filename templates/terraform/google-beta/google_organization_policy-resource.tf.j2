resource "google_organization_policy" "example" {
    constraint = "{{ constraint }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    org_id = "{{ org_id }}"
    update_time = "{{ update_time }}"
    version = "{{ version }}"
}