resource "google_gke_hub_membership_iam_policy" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    membership_id = "{{ membership_id }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
}