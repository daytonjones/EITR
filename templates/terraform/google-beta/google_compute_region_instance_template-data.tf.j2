data "google_compute_region_instance_template" "example" {
    filter = "{{ filter }}"
    id = "{{ id }}"
    most_recent = {{ most_recent }}
    name = "{{ name }}"
    project = "{{ project }}"
    region = "{{ region }}"
}
