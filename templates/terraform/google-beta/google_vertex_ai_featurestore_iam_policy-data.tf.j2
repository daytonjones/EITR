data "google_vertex_ai_featurestore_iam_policy" "example" {
    etag = "{{ etag }}"
    featurestore = "{{ featurestore }}"
    id = "{{ id }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    region = "{{ region }}"
}