resource "google_iap_web_forwarding_rule_service_iam_policy" "example" {
    forwarding_rule_service_name = "{{ forwarding_rule_service_name }}"
    id = "{{ id }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
}
