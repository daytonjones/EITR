resource "google_iap_app_engine_version_iam_binding" "example" {
    app_id = "{{ app_id }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    members = "{{ members }}"
    project = "{{ project }}"
    role = "{{ role }}"
    service = "{{ service }}"
    version_id = "{{ version_id }}"
}