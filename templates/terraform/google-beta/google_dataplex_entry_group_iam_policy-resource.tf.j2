resource "google_dataplex_entry_group_iam_policy" "example" {
    entry_group_id = "{{ entry_group_id }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
}