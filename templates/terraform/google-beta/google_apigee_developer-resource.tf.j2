resource "google_apigee_developer" "example" {
    email = "{{ email }}"
    first_name = "{{ first_name }}"
    id = "{{ id }}"
    last_name = "{{ last_name }}"
    org_id = "{{ org_id }}"
    user_name = "{{ user_name }}"
    attributes {
      name = "{{ name }}"
      value = "{{ value }}"
    }
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
