resource "google_apigee_developer" "example" {
    created_at = "{{ created_at }}"
    email = "{{ email }}"
    first_name = "{{ first_name }}"
    id = "{{ id }}"
    last_modified_at = "{{ last_modified_at }}"
    last_name = "{{ last_name }}"
    org_id = "{{ org_id }}"
    organizatio_name = "{{ organizatio_name }}"
    status = "{{ status }}"
    user_name = "{{ user_name }}"
}