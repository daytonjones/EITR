resource "google_compute_target_tcp_proxy" "example" {
    backend_service = "{{ backend_service }}"
    description = "{{ description }}"
    id = "{{ id }}"
    load_balancing_scheme = "{{ load_balancing_scheme }}"
    name = "{{ name }}"
    project = "{{ project }}"
    proxy_bind = {{ proxy_bind }}
    proxy_header = "{{ proxy_header }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
