resource "google_container_aws_node_pool" "example" {
    annotations = "{{ annotations }}"
    cluster = "{{ cluster }}"
    create_time = "{{ create_time }}"
    effective_annotations = "{{ effective_annotations }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    name = "{{ name }}"
    project = "{{ project }}"
    reconciling = "{{ reconciling }}"
    state = "{{ state }}"
    subnet_id = "{{ subnet_id }}"
    uid = "{{ uid }}"
    update_time = "{{ update_time }}"
    version = "{{ version }}"
}