resource "google_dataform_repository_iam_binding" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    members = "{{ members }}"
    project = "{{ project }}"
    region = "{{ region }}"
    repository = "{{ repository }}"
    role = "{{ role }}"
}