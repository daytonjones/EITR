data "google_bigtable_table_iam_policy" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    instance = "{{ instance }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    table = "{{ table }}"
}