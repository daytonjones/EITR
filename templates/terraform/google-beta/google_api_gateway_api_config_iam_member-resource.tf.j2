resource "google_api_gateway_api_config_iam_member" "example" {
    api = "{{ api }}"
    api_config = "{{ api_config }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    member = "{{ member }}"
    project = "{{ project }}"
    role = "{{ role }}"
}