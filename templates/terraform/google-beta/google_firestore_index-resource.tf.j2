resource "google_firestore_index" "example" {
    api_scope = "{{ api_scope }}"
    collection = "{{ collection }}"
    database = "{{ database }}"
    id = "{{ id }}"
    name = "{{ name }}"
    project = "{{ project }}"
    query_scope = "{{ query_scope }}"
}