data "google_parameter_manager_parameter_version" "example" {
    id = "{{ id }}"
    parameter = "{{ parameter }}"
    parameter_version_id = "{{ parameter_version_id }}"
    project = "{{ project }}"
}
