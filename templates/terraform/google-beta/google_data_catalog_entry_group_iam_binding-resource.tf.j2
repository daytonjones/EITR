resource "google_data_catalog_entry_group_iam_binding" "example" {
    entry_group = "{{ entry_group }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    members = "{{ members }}"
    project = "{{ project }}"
    region = "{{ region }}"
    role = "{{ role }}"
}