data "google_compute_router" "example" {
    bgp = "{{ bgp }}"
    creation_timestamp = "{{ creation_timestamp }}"
    description = "{{ description }}"
    encrypted_interconnect_router = "{{ encrypted_interconnect_router }}"
    id = "{{ id }}"
    name = "{{ name }}"
    network = "{{ network }}"
    project = "{{ project }}"
    region = "{{ region }}"
    self_link = "{{ self_link }}"
}