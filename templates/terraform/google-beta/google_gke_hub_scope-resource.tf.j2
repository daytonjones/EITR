resource "google_gke_hub_scope" "example" {
    id = "{{ id }}"
    labels = {
        key = "{{ labels_value }}"
      }
    namespace_labels = {
        key = "{{ namespace_labels_value }}"
      }
    project = "{{ project }}"
    scope_id = "{{ scope_id }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
