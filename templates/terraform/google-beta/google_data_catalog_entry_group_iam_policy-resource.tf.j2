resource "google_data_catalog_entry_group_iam_policy" "example" {
    entry_group = "{{ entry_group }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    region = "{{ region }}"
}