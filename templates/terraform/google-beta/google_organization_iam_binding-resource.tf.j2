resource "google_organization_iam_binding" "example" {
    id = "{{ id }}"
    members = ["{{ members }}"]
    org_id = "{{ org_id }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
