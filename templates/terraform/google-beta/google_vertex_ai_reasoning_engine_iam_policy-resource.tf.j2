resource "google_vertex_ai_reasoning_engine_iam_policy" "example" {
    id = "{{ id }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    reasoning_engine = "{{ reasoning_engine }}"
    region = "{{ region }}"
}
