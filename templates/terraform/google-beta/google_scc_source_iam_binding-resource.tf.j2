resource "google_scc_source_iam_binding" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    members = "{{ members }}"
    organization = "{{ organization }}"
    role = "{{ role }}"
    source = "{{ source }}"
}