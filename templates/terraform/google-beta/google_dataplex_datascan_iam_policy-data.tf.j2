data "google_dataplex_datascan_iam_policy" "example" {
    data_scan_id = "{{ data_scan_id }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
}