resource "google_runtimeconfig_config_iam_policy" "example" {
    config = "{{ config }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
}