resource "google_vertex_ai_featurestore_entitytype_iam_policy" "example" {
    entitytype = "{{ entitytype }}"
    featurestore = "{{ featurestore }}"
    id = "{{ id }}"
    policy_data = "{{ policy_data }}"
}
