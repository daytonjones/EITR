resource "google_dataplex_entry_group" "example" {
    description = "{{ description }}"
    display_name = "{{ display_name }}"
    entry_group_id = "{{ entry_group_id }}"
    id = "{{ id }}"
    labels = {
        key = "{{ labels_value }}"
      }
    location = "{{ location }}"
    project = "{{ project }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
