resource "google_vertex_ai_feature_group_iam_policy" "example" {
    feature_group = "{{ feature_group }}"
    id = "{{ id }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    region = "{{ region }}"
}
