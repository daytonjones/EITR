resource "google_data_catalog_taxonomy_iam_member" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    member = "{{ member }}"
    project = "{{ project }}"
    region = "{{ region }}"
    role = "{{ role }}"
    taxonomy = "{{ taxonomy }}"
}