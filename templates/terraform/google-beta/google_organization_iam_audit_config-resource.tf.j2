resource "google_organization_iam_audit_config" "example" {
    id = "{{ id }}"
    org_id = "{{ org_id }}"
    service = "{{ service }}"
    audit_log_config {
      exempted_members = ["{{ exempted_members }}"]
      log_type = "{{ log_type }}"
    }
}
