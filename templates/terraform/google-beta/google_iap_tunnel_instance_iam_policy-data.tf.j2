data "google_iap_tunnel_instance_iam_policy" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    instance = "{{ instance }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    zone = "{{ zone }}"
}