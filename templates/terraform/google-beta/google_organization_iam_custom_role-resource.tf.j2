resource "google_organization_iam_custom_role" "example" {
    description = "{{ description }}"
    id = "{{ id }}"
    org_id = "{{ org_id }}"
    permissions = ["{{ permissions }}"]
    role_id = "{{ role_id }}"
    stage = "{{ stage }}"
    title = "{{ title }}"
}
