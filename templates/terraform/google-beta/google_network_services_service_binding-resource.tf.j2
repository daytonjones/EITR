resource "google_network_services_service_binding" "example" {
    create_time = "{{ create_time }}"
    description = "{{ description }}"
    effective_labels = "{{ effective_labels }}"
    id = "{{ id }}"
    labels = "{{ labels }}"
    name = "{{ name }}"
    project = "{{ project }}"
    service = "{{ service }}"
    terraform_labels = "{{ terraform_labels }}"
    update_time = "{{ update_time }}"
}