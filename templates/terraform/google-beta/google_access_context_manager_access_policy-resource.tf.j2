resource "google_access_context_manager_access_policy" "example" {
    id = "{{ id }}"
    parent = "{{ parent }}"
    scopes = ["{{ scopes }}"]
    title = "{{ title }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
