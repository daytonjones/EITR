resource "google_dns_managed_zone_iam_policy" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    managed_zone = "{{ managed_zone }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
}