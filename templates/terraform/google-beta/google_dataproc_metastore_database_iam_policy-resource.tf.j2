resource "google_dataproc_metastore_database_iam_policy" "example" {
    database = "{{ database }}"
    id = "{{ id }}"
    location = "{{ location }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    service_id = "{{ service_id }}"
}
