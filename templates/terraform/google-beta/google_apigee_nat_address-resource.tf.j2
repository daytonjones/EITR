resource "google_apigee_nat_address" "example" {
    activate = "{{ activate }}"
    id = "{{ id }}"
    instance_id = "{{ instance_id }}"
    ip_address = "{{ ip_address }}"
    name = "{{ name }}"
    state = "{{ state }}"
}