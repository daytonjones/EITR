resource "google_compute_region_backend_service_iam_policy" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    name = "{{ name }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    region = "{{ region }}"
}