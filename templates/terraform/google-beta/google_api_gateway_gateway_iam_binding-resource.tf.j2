resource "google_api_gateway_gateway_iam_binding" "example" {
    etag = "{{ etag }}"
    gateway = "{{ gateway }}"
    id = "{{ id }}"
    members = "{{ members }}"
    project = "{{ project }}"
    region = "{{ region }}"
    role = "{{ role }}"
}