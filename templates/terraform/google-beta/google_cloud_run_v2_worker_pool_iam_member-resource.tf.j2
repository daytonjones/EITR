resource "google_cloud_run_v2_worker_pool_iam_member" "example" {
    id = "{{ id }}"
    location = "{{ location }}"
    member = "{{ member }}"
    name = "{{ name }}"
    project = "{{ project }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
