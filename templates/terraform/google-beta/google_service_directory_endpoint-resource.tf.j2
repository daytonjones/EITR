resource "google_service_directory_endpoint" "example" {
    address = "{{ address }}"
    endpoint_id = "{{ endpoint_id }}"
    id = "{{ id }}"
    metadata = "{{ metadata }}"
    name = "{{ name }}"
    network = "{{ network }}"
    port = "{{ port }}"
    service = "{{ service }}"
}