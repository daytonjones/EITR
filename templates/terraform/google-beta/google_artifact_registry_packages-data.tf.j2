data "google_artifact_registry_packages" "example" {
    filter = "{{ filter }}"
    id = "{{ id }}"
    location = "{{ location }}"
    project = "{{ project }}"
    repository_id = "{{ repository_id }}"
}
