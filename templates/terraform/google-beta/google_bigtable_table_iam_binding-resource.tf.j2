resource "google_bigtable_table_iam_binding" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    instance = "{{ instance }}"
    members = "{{ members }}"
    project = "{{ project }}"
    role = "{{ role }}"
    table = "{{ table }}"
}