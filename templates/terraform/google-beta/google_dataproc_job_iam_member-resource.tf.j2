resource "google_dataproc_job_iam_member" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    job_id = "{{ job_id }}"
    member = "{{ member }}"
    project = "{{ project }}"
    region = "{{ region }}"
    role = "{{ role }}"
}