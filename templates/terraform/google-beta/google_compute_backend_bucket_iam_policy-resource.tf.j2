resource "google_compute_backend_bucket_iam_policy" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    name = "{{ name }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
}