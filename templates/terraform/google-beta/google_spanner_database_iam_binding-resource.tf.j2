resource "google_spanner_database_iam_binding" "example" {
    database = "{{ database }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    instance = "{{ instance }}"
    members = "{{ members }}"
    project = "{{ project }}"
    role = "{{ role }}"
}