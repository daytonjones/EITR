resource "google_dataplex_zone_iam_member" "example" {
    dataplex_zone = "{{ dataplex_zone }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    lake = "{{ lake }}"
    location = "{{ location }}"
    member = "{{ member }}"
    project = "{{ project }}"
    role = "{{ role }}"
}