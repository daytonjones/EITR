resource "google_resource_manager_lien" "example" {
    id = "{{ id }}"
    origin = "{{ origin }}"
    parent = "{{ parent }}"
    reason = "{{ reason }}"
    restrictions = ["{{ restrictions }}"]
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
    }
}
