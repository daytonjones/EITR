resource "google_compute_backend_service_iam_binding" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    members = "{{ members }}"
    name = "{{ name }}"
    project = "{{ project }}"
    role = "{{ role }}"
}