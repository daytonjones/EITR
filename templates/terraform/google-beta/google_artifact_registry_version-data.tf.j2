data "google_artifact_registry_version" "example" {
    id = "{{ id }}"
    location = "{{ location }}"
    package_name = "{{ package_name }}"
    project = "{{ project }}"
    repository_id = "{{ repository_id }}"
    version_name = "{{ version_name }}"
    view = "{{ view }}"
}
