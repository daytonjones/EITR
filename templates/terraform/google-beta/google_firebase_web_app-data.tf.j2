data "google_firebase_web_app" "example" {
    api_key_id = "{{ api_key_id }}"
    app_id = "{{ app_id }}"
    app_urls = "{{ app_urls }}"
    deletion_policy = "{{ deletion_policy }}"
    display_name = "{{ display_name }}"
    id = "{{ id }}"
    name = "{{ name }}"
    project = "{{ project }}"
}