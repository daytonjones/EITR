data "google_artifact_registry_docker_image" "example" {
    id = "{{ id }}"
    image_name = "{{ image_name }}"
    location = "{{ location }}"
    project = "{{ project }}"
    repository_id = "{{ repository_id }}"
}
