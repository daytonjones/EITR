resource "google_artifact_registry_vpcsc_config" "example" {
    id = "{{ id }}"
    location = "{{ location }}"
    project = "{{ project }}"
    vpcsc_policy = "{{ vpcsc_policy }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
