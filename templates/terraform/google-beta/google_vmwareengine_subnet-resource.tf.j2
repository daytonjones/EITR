resource "google_vmwareengine_subnet" "example" {
    id = "{{ id }}"
    ip_cidr_range = "{{ ip_cidr_range }}"
    name = "{{ name }}"
    parent = "{{ parent }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
