resource "google_dataproc_autoscaling_policy_iam_member" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    member = "{{ member }}"
    policy_id = "{{ policy_id }}"
    project = "{{ project }}"
    role = "{{ role }}"
}