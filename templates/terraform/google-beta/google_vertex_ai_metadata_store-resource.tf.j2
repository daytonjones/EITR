resource "google_vertex_ai_metadata_store" "example" {
    create_time = "{{ create_time }}"
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    project = "{{ project }}"
    region = "{{ region }}"
    state = "{{ state }}"
    update_time = "{{ update_time }}"
}