resource "google_compute_instance_iam_member" "example" {
    id = "{{ id }}"
    instance_name = "{{ instance_name }}"
    member = "{{ member }}"
    project = "{{ project }}"
    role = "{{ role }}"
    zone = "{{ zone }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
