resource "google_compute_region_disk_iam_policy" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    region = "{{ region }}"
}
