resource "google_compute_machine_image_iam_member" "example" {
    id = "{{ id }}"
    machine_image = "{{ machine_image }}"
    member = "{{ member }}"
    project = "{{ project }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
