resource "google_container_analysis_note_iam_binding" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    members = "{{ members }}"
    note = "{{ note }}"
    project = "{{ project }}"
    role = "{{ role }}"
}