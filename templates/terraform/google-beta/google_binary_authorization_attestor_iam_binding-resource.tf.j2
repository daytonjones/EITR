resource "google_binary_authorization_attestor_iam_binding" "example" {
    attestor = "{{ attestor }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    members = "{{ members }}"
    project = "{{ project }}"
    role = "{{ role }}"
}