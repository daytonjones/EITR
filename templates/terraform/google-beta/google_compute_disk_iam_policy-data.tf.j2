data "google_compute_disk_iam_policy" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    name = "{{ name }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    zone = "{{ zone }}"
}