resource "google_compute_region_disk_resource_policy_attachment" "example" {
    disk = "{{ disk }}"
    id = "{{ id }}"
    name = "{{ name }}"
    project = "{{ project }}"
    region = "{{ region }}"
}