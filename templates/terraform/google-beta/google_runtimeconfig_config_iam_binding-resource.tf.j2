resource "google_runtimeconfig_config_iam_binding" "example" {
    config = "{{ config }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    members = "{{ members }}"
    project = "{{ project }}"
    role = "{{ role }}"
}