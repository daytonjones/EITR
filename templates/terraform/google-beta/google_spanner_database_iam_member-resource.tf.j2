resource "google_spanner_database_iam_member" "example" {
    database = "{{ database }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    instance = "{{ instance }}"
    member = "{{ member }}"
    project = "{{ project }}"
    role = "{{ role }}"
}