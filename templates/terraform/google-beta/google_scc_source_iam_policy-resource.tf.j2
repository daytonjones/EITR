resource "google_scc_source_iam_policy" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    organization = "{{ organization }}"
    policy_data = "{{ policy_data }}"
    source = "{{ source }}"
}