resource "google_apikeys_key" "example" {
    display_name = "{{ display_name }}"
    id = "{{ id }}"
    key_string = "{{ key_string }}"
    name = "{{ name }}"
    project = "{{ project }}"
    uid = "{{ uid }}"
}