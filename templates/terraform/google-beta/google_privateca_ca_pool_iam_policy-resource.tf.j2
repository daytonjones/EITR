resource "google_privateca_ca_pool_iam_policy" "example" {
    ca_pool = "{{ ca_pool }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
}