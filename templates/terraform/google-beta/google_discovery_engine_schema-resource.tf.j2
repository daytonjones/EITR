resource "google_discovery_engine_schema" "example" {
    data_store_id = "{{ data_store_id }}"
    id = "{{ id }}"
    json_schema = "{{ json_schema }}"
    location = "{{ location }}"
    project = "{{ project }}"
    schema_id = "{{ schema_id }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
    }
}
