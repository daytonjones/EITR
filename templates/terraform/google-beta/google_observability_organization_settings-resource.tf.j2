resource "google_observability_organization_settings" "example" {
    default_storage_location = "{{ default_storage_location }}"
    id = "{{ id }}"
    kms_key_name = "{{ kms_key_name }}"
    location = "{{ location }}"
    organization = "{{ organization }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
