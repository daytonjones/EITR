resource "google_secure_source_manager_repository_iam_member" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    member = "{{ member }}"
    project = "{{ project }}"
    repository_id = "{{ repository_id }}"
    role = "{{ role }}"
}