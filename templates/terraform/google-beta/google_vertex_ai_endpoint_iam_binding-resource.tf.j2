resource "google_vertex_ai_endpoint_iam_binding" "example" {
    endpoint = "{{ endpoint }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    members = "{{ members }}"
    project = "{{ project }}"
    role = "{{ role }}"
}