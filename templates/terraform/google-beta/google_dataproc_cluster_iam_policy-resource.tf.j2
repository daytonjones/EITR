resource "google_dataproc_cluster_iam_policy" "example" {
    cluster = "{{ cluster }}"
    id = "{{ id }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    region = "{{ region }}"
}
