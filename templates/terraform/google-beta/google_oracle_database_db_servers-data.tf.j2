data "google_oracle_database_db_servers" "example" {
    cloud_exadata_infrastructure = "{{ cloud_exadata_infrastructure }}"
    db_servers = "{{ db_servers }}"
    id = "{{ id }}"
    location = "{{ location }}"
    project = "{{ project }}"
}