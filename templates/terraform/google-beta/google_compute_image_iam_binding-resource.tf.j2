resource "google_compute_image_iam_binding" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    image = "{{ image }}"
    members = "{{ members }}"
    project = "{{ project }}"
    role = "{{ role }}"
}