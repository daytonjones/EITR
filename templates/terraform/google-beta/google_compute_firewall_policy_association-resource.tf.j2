resource "google_compute_firewall_policy_association" "example" {
    attachment_target = "{{ attachment_target }}"
    firewall_policy = "{{ firewall_policy }}"
    id = "{{ id }}"
    name = "{{ name }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
