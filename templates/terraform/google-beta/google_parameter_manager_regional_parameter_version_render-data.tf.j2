data "google_parameter_manager_regional_parameter_version_render" "example" {
    id = "{{ id }}"
    location = "{{ location }}"
    parameter = "{{ parameter }}"
    parameter_version_id = "{{ parameter_version_id }}"
    project = "{{ project }}"
}
