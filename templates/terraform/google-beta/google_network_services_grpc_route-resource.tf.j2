resource "google_network_services_grpc_route" "example" {
    create_time = "{{ create_time }}"
    description = "{{ description }}"
    effective_labels = "{{ effective_labels }}"
    gateways = "{{ gateways }}"
    hostnames = "{{ hostnames }}"
    id = "{{ id }}"
    labels = "{{ labels }}"
    meshes = "{{ meshes }}"
    name = "{{ name }}"
    project = "{{ project }}"
    self_link = "{{ self_link }}"
    terraform_labels = "{{ terraform_labels }}"
    update_time = "{{ update_time }}"
}