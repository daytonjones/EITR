resource "google_vertex_ai_featurestore_entitytype_iam_member" "example" {
    entitytype = "{{ entitytype }}"
    featurestore = "{{ featurestore }}"
    id = "{{ id }}"
    member = "{{ member }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
