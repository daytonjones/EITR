resource "google_scc_v2_folder_mute_config" "example" {
    create_time = "{{ create_time }}"
    description = "{{ description }}"
    filter = "{{ filter }}"
    folder = "{{ folder }}"
    id = "{{ id }}"
    location = "{{ location }}"
    most_recent_editor = "{{ most_recent_editor }}"
    mute_config_id = "{{ mute_config_id }}"
    name = "{{ name }}"
    type = "{{ type }}"
    update_time = "{{ update_time }}"
}