resource "google_storage_managed_folder" "example" {
    bucket = "{{ bucket }}"
    force_destroy = {{ force_destroy }}
    id = "{{ id }}"
    name = "{{ name }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
