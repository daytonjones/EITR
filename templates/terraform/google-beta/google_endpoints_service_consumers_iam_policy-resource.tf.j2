resource "google_endpoints_service_consumers_iam_policy" "example" {
    consumer_project = "{{ consumer_project }}"
    id = "{{ id }}"
    policy_data = "{{ policy_data }}"
    service_name = "{{ service_name }}"
}
