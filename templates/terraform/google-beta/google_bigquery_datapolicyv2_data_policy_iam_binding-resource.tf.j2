resource "google_bigquery_datapolicyv2_data_policy_iam_binding" "example" {
    data_policy_id = "{{ data_policy_id }}"
    id = "{{ id }}"
    location = "{{ location }}"
    members = ["{{ members }}"]
    project = "{{ project }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
