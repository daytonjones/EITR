data "google_compute_ha_vpn_gateway" "example" {
    description = "{{ description }}"
    gateway_ip_version = "{{ gateway_ip_version }}"
    id = "{{ id }}"
    name = "{{ name }}"
    network = "{{ network }}"
    project = "{{ project }}"
    region = "{{ region }}"
    self_link = "{{ self_link }}"
    stack_type = "{{ stack_type }}"
    vpn_interfaces = "{{ vpn_interfaces }}"
}