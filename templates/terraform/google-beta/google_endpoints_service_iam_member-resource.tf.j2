resource "google_endpoints_service_iam_member" "example" {
    id = "{{ id }}"
    member = "{{ member }}"
    role = "{{ role }}"
    service_name = "{{ service_name }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
