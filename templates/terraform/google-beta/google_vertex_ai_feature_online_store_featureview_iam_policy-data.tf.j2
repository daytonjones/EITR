data "google_vertex_ai_feature_online_store_featureview_iam_policy" "example" {
    feature_online_store = "{{ feature_online_store }}"
    feature_view = "{{ feature_view }}"
    id = "{{ id }}"
    project = "{{ project }}"
    region = "{{ region }}"
}
