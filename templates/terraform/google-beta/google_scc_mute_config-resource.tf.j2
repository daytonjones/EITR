resource "google_scc_mute_config" "example" {
    create_time = "{{ create_time }}"
    description = "{{ description }}"
    filter = "{{ filter }}"
    id = "{{ id }}"
    most_recent_editor = "{{ most_recent_editor }}"
    mute_config_id = "{{ mute_config_id }}"
    name = "{{ name }}"
    parent = "{{ parent }}"
    update_time = "{{ update_time }}"
}