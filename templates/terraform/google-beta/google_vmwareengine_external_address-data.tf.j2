data "google_vmwareengine_external_address" "example" {
    create_time = "{{ create_time }}"
    description = "{{ description }}"
    external_ip = "{{ external_ip }}"
    id = "{{ id }}"
    internal_ip = "{{ internal_ip }}"
    name = "{{ name }}"
    parent = "{{ parent }}"
    state = "{{ state }}"
    uid = "{{ uid }}"
    update_time = "{{ update_time }}"
}