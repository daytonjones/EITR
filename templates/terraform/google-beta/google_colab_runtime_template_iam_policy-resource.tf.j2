resource "google_colab_runtime_template_iam_policy" "example" {
    id = "{{ id }}"
    location = "{{ location }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    runtime_template = "{{ runtime_template }}"
}
