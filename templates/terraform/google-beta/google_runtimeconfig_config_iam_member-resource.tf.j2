resource "google_runtimeconfig_config_iam_member" "example" {
    config = "{{ config }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    member = "{{ member }}"
    project = "{{ project }}"
    role = "{{ role }}"
}