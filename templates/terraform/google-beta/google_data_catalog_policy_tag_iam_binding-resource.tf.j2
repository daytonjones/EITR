resource "google_data_catalog_policy_tag_iam_binding" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    members = "{{ members }}"
    policy_tag = "{{ policy_tag }}"
    role = "{{ role }}"
}