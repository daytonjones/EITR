resource "google_apigee_api" "example" {
    config_bundle = "{{ config_bundle }}"
    detect_md5hash = "{{ detect_md5hash }}"
    id = "{{ id }}"
    name = "{{ name }}"
    org_id = "{{ org_id }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
