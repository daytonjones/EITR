resource "google_firebase_app_check_recaptcha_enterprise_config" "example" {
    app_id = "{{ app_id }}"
    id = "{{ id }}"
    name = "{{ name }}"
    project = "{{ project }}"
    site_key = "{{ site_key }}"
    token_ttl = "{{ token_ttl }}"
}