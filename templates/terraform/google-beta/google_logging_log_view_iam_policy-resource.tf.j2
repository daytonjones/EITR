resource "google_logging_log_view_iam_policy" "example" {
    bucket = "{{ bucket }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    name = "{{ name }}"
    parent = "{{ parent }}"
    policy_data = "{{ policy_data }}"
}