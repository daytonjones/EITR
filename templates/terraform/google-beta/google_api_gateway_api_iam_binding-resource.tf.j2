resource "google_api_gateway_api_iam_binding" "example" {
    api = "{{ api }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    members = "{{ members }}"
    project = "{{ project }}"
    role = "{{ role }}"
}