resource "google_endpoints_service_iam_binding" "example" {
    id = "{{ id }}"
    members = ["{{ members }}"]
    role = "{{ role }}"
    service_name = "{{ service_name }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
