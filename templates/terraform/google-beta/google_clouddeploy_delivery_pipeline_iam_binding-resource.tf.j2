resource "google_clouddeploy_delivery_pipeline_iam_binding" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    members = "{{ members }}"
    name = "{{ name }}"
    project = "{{ project }}"
    role = "{{ role }}"
}