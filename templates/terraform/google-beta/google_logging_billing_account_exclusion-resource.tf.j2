resource "google_logging_billing_account_exclusion" "example" {
    billing_account = "{{ billing_account }}"
    description = "{{ description }}"
    disabled = "{{ disabled }}"
    filter = "{{ filter }}"
    id = "{{ id }}"
    name = "{{ name }}"
}