resource "google_dialogflow_cx_test_case" "example" {
    creation_time = "{{ creation_time }}"
    display_name = "{{ display_name }}"
    id = "{{ id }}"
    last_test_result = "{{ last_test_result }}"
    name = "{{ name }}"
    notes = "{{ notes }}"
    parent = "{{ parent }}"
    tags = "{{ tags }}"
}