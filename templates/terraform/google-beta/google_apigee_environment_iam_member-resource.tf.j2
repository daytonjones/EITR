resource "google_apigee_environment_iam_member" "example" {
    env_id = "{{ env_id }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    member = "{{ member }}"
    org_id = "{{ org_id }}"
    role = "{{ role }}"
}