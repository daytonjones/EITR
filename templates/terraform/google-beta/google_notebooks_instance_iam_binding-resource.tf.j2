resource "google_notebooks_instance_iam_binding" "example" {
    id = "{{ id }}"
    instance_name = "{{ instance_name }}"
    location = "{{ location }}"
    members = ["{{ members }}"]
    project = "{{ project }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
