resource "google_compute_region_backend_service_iam_member" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    member = "{{ member }}"
    name = "{{ name }}"
    project = "{{ project }}"
    region = "{{ region }}"
    role = "{{ role }}"
}