resource "google_monitoring_monitored_project" "example" {
    id = "{{ id }}"
    metrics_scope = "{{ metrics_scope }}"
    name = "{{ name }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
    }
}
