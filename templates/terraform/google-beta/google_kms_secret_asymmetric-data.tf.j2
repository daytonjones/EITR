data "google_kms_secret_asymmetric" "example" {
    ciphertext = "{{ ciphertext }}"
    crc32 = "{{ crc32 }}"
    crypto_key_version = "{{ crypto_key_version }}"
    id = "{{ id }}"
    plaintext = "{{ plaintext }}"
}