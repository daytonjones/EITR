data "hcs_cluster" "example" {
    cluster_name = "{{ cluster_name }}"
    id = "{{ id }}"
    managed_application_name = "{{ managed_application_name }}"
    resource_group_name = "{{ resource_group_name }}"
    timeouts {
      default = "{{ default }}"
    }
}
