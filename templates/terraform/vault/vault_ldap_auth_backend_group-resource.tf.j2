resource "vault_ldap_auth_backend_group" "example" {
    backend = "{{ backend }}"
    groupname = "{{ groupname }}"
    id = "{{ id }}"
    namespace = "{{ namespace }}"
    policies = "{{ policies }}"
}