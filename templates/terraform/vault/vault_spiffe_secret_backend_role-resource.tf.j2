resource "vault_spiffe_secret_backend_role" "example" {
    mount = "{{ mount }}"
    name = "{{ name }}"
    namespace = "{{ namespace }}"
    template = "{{ template }}"
    ttl = "{{ ttl }}"
    use_jti_claim = {{ use_jti_claim }}
}
