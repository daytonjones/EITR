data "vault_ldap_dynamic_credentials" "example" {
    distinguished_names = "{{ distinguished_names }}"
    id = "{{ id }}"
    lease_duration = "{{ lease_duration }}"
    lease_id = "{{ lease_id }}"
    lease_renewable = "{{ lease_renewable }}"
    mount = "{{ mount }}"
    namespace = "{{ namespace }}"
    password = "{{ password }}"
    role_name = "{{ role_name }}"
    username = "{{ username }}"
}