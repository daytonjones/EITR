ephemeral "vault_generic_endpoint" "example" {
    data_json = "{{ data_json }}"
    mount_id = "{{ mount_id }}"
    namespace = "{{ namespace }}"
    path = "{{ path }}"
    path_wrap_ttl = "{{ path_wrap_ttl }}"
    write_fields = ["{{ write_fields }}"]
}
