data "vault_aws_access_credentials" "example" {
    backend = "{{ backend }}"
    id = "{{ id }}"
    namespace = "{{ namespace }}"
    region = "{{ region }}"
    role = "{{ role }}"
    role_arn = "{{ role_arn }}"
    ttl = "{{ ttl }}"
    type = "{{ type }}"
}
