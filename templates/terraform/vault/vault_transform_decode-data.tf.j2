data "vault_transform_decode" "example" {
    batch_input = "{{ batch_input }}"
    batch_results = "{{ batch_results }}"
    decoded_value = "{{ decoded_value }}"
    id = "{{ id }}"
    namespace = "{{ namespace }}"
    path = "{{ path }}"
    role_name = "{{ role_name }}"
    transformation = "{{ transformation }}"
    tweak = "{{ tweak }}"
    value = "{{ value }}"
}