ephemeral "vault_kubernetes_service_account_token" "example" {
    backend = "{{ backend }}"
    cluster_role_binding = {{ cluster_role_binding }}
    kubernetes_namespace = "{{ kubernetes_namespace }}"
    mount_id = "{{ mount_id }}"
    namespace = "{{ namespace }}"
    role = "{{ role }}"
    ttl = "{{ ttl }}"
}
