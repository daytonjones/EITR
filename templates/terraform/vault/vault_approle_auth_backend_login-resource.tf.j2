resource "vault_approle_auth_backend_login" "example" {
    backend = "{{ backend }}"
    id = "{{ id }}"
    namespace = "{{ namespace }}"
    role_id = "{{ role_id }}"
    secret_id = "{{ secret_id }}"
    secret_id_wo = "{{ secret_id_wo }}"
    secret_id_wo_version = {{ secret_id_wo_version }}
}
