resource "vault_aws_secret_backend_static_role" "example" {
    backend = "{{ backend }}"
    id = "{{ id }}"
    name = "{{ name }}"
    namespace = "{{ namespace }}"
    rotation_period = "{{ rotation_period }}"
    username = "{{ username }}"
}