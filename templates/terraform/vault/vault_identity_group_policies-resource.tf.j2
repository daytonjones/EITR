resource "vault_identity_group_policies" "example" {
    exclusive = {{ exclusive }}
    group_id = "{{ group_id }}"
    id = "{{ id }}"
    namespace = "{{ namespace }}"
    policies = ["{{ policies }}"]
}
