ephemeral "vault_cf_auth_login" "example" {
    cf_instance_cert = "{{ cf_instance_cert }}"
    mount = "{{ mount }}"
    mount_id = "{{ mount_id }}"
    namespace = "{{ namespace }}"
    role = "{{ role }}"
    signature = "{{ signature }}"
    signing_time = "{{ signing_time }}"
}
