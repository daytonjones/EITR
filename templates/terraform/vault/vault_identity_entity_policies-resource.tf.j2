resource "vault_identity_entity_policies" "example" {
    entity_id = "{{ entity_id }}"
    entity_name = "{{ entity_name }}"
    exclusive = "{{ exclusive }}"
    id = "{{ id }}"
    namespace = "{{ namespace }}"
    policies = "{{ policies }}"
}