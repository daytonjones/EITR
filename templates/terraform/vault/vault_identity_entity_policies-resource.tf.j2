resource "vault_identity_entity_policies" "example" {
    entity_id = "{{ entity_id }}"
    exclusive = {{ exclusive }}
    id = "{{ id }}"
    namespace = "{{ namespace }}"
    policies = ["{{ policies }}"]
}
