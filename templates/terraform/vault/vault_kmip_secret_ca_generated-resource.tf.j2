resource "vault_kmip_secret_ca_generated" "example" {
    key_bits = {{ key_bits }}
    key_type = "{{ key_type }}"
    name = "{{ name }}"
    namespace = "{{ namespace }}"
    path = "{{ path }}"
    ttl = {{ ttl }}
}
