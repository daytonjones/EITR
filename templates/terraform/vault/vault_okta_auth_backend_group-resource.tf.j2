resource "vault_okta_auth_backend_group" "example" {
    group_name = "{{ group_name }}"
    id = "{{ id }}"
    namespace = "{{ namespace }}"
    path = "{{ path }}"
    policies = "{{ policies }}"
}