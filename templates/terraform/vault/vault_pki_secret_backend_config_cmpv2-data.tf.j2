data "vault_pki_secret_backend_config_cmpv2" "example" {
    backend = "{{ backend }}"
    disabled_validations = ["{{ disabled_validations }}"]
    id = "{{ id }}"
    namespace = "{{ namespace }}"
}
