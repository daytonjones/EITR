resource "vault_identity_oidc_assignment" "example" {
    entity_ids = "{{ entity_ids }}"
    group_ids = "{{ group_ids }}"
    id = "{{ id }}"
    name = "{{ name }}"
    namespace = "{{ namespace }}"
}