resource "vault_identity_entity" "example" {
    disabled = {{ disabled }}
    external_policies = {{ external_policies }}
    id = "{{ id }}"
    metadata = {
        key = "{{ metadata_value }}"
      }
    name = "{{ name }}"
    namespace = "{{ namespace }}"
    policies = ["{{ policies }}"]
}
