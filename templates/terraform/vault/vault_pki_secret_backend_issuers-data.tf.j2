data "vault_pki_secret_backend_issuers" "example" {
    backend = "{{ backend }}"
    id = "{{ id }}"
    key_info = "{{ key_info }}"
    key_info_json = "{{ key_info_json }}"
    keys = "{{ keys }}"
    namespace = "{{ namespace }}"
}