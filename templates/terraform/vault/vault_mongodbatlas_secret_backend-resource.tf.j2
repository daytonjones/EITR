resource "vault_mongodbatlas_secret_backend" "example" {
    id = "{{ id }}"
    mount = "{{ mount }}"
    namespace = "{{ namespace }}"
    path = "{{ path }}"
    private_key = "{{ private_key }}"
    public_key = "{{ public_key }}"
}