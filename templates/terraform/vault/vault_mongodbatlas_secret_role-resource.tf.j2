resource "vault_mongodbatlas_secret_role" "example" {
    cidr_blocks = "{{ cidr_blocks }}"
    id = "{{ id }}"
    ip_addresses = "{{ ip_addresses }}"
    max_ttl = "{{ max_ttl }}"
    mount = "{{ mount }}"
    name = "{{ name }}"
    namespace = "{{ namespace }}"
    organization_id = "{{ organization_id }}"
    project_id = "{{ project_id }}"
    project_roles = "{{ project_roles }}"
    roles = "{{ roles }}"
    ttl = "{{ ttl }}"
}