resource "vault_pki_secret_backend_config_issuers" "example" {
    backend = "{{ backend }}"
    default = "{{ default }}"
    default_follows_latest_issuer = "{{ default_follows_latest_issuer }}"
    id = "{{ id }}"
    namespace = "{{ namespace }}"
}