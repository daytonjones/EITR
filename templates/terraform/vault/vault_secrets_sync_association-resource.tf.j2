resource "vault_secrets_sync_association" "example" {
    id = "{{ id }}"
    mount = "{{ mount }}"
    name = "{{ name }}"
    namespace = "{{ namespace }}"
    secret_name = "{{ secret_name }}"
    type = "{{ type }}"
}
