data "vault_generic_secret" "example" {
    id = "{{ id }}"
    namespace = "{{ namespace }}"
    path = "{{ path }}"
    version = {{ version }}
    with_lease_start_time = {{ with_lease_start_time }}
}
