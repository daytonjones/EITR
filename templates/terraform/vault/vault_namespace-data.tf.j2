data "vault_namespace" "example" {
    custom_metadata = "{{ custom_metadata }}"
    id = "{{ id }}"
    namespace = "{{ namespace }}"
    namespace_id = "{{ namespace_id }}"
    path = "{{ path }}"
    path_fq = "{{ path_fq }}"
}