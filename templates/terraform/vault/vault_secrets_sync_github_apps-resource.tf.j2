resource "vault_secrets_sync_github_apps" "example" {
    app_id = "{{ app_id }}"
    fingerprint = "{{ fingerprint }}"
    id = "{{ id }}"
    name = "{{ name }}"
    namespace = "{{ namespace }}"
    private_key = "{{ private_key }}"
}