resource "vault_azure_secret_backend" "example" {
    client_id = "{{ client_id }}"
    client_secret = "{{ client_secret }}"
    description = "{{ description }}"
    disable_remount = "{{ disable_remount }}"
    environment = "{{ environment }}"
    id = "{{ id }}"
    identity_token_audience = "{{ identity_token_audience }}"
    identity_token_key = "{{ identity_token_key }}"
    identity_token_ttl = "{{ identity_token_ttl }}"
    namespace = "{{ namespace }}"
    path = "{{ path }}"
    subscription_id = "{{ subscription_id }}"
    tenant_id = "{{ tenant_id }}"
    use_microsoft_graph_api = "{{ use_microsoft_graph_api }}"
}