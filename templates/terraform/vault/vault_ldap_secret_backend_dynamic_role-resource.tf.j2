resource "vault_ldap_secret_backend_dynamic_role" "example" {
    creation_ldif = "{{ creation_ldif }}"
    default_ttl = "{{ default_ttl }}"
    deletion_ldif = "{{ deletion_ldif }}"
    id = "{{ id }}"
    max_ttl = "{{ max_ttl }}"
    mount = "{{ mount }}"
    namespace = "{{ namespace }}"
    role_name = "{{ role_name }}"
    rollback_ldif = "{{ rollback_ldif }}"
    username_template = "{{ username_template }}"
}