resource "vault_mfa_pingid" "example" {
    id = "{{ id }}"
    mount_accessor = "{{ mount_accessor }}"
    name = "{{ name }}"
    namespace = "{{ namespace }}"
    settings_file_base64 = "{{ settings_file_base64 }}"
    username_format = "{{ username_format }}"
}
