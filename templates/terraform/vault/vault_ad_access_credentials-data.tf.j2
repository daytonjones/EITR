data "vault_ad_access_credentials" "example" {
    backend = "{{ backend }}"
    current_password = "{{ current_password }}"
    id = "{{ id }}"
    last_password = "{{ last_password }}"
    namespace = "{{ namespace }}"
    role = "{{ role }}"
    username = "{{ username }}"
}