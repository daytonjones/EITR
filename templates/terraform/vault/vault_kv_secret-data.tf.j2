data "vault_kv_secret" "example" {
    data = "{{ data }}"
    data_json = "{{ data_json }}"
    id = "{{ id }}"
    lease_duration = "{{ lease_duration }}"
    lease_id = "{{ lease_id }}"
    lease_renewable = "{{ lease_renewable }}"
    namespace = "{{ namespace }}"
    path = "{{ path }}"
}