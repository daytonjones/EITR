resource "vault_identity_group_alias" "example" {
    canonical_id = "{{ canonical_id }}"
    id = "{{ id }}"
    mount_accessor = "{{ mount_accessor }}"
    name = "{{ name }}"
    namespace = "{{ namespace }}"
}