resource "vault_nomad_secret_role" "example" {
    backend = "{{ backend }}"
    global = "{{ global }}"
    id = "{{ id }}"
    namespace = "{{ namespace }}"
    policies = "{{ policies }}"
    role = "{{ role }}"
    type = "{{ type }}"
}