resource "vault_gcp_secret_roleset" "example" {
    backend = "{{ backend }}"
    id = "{{ id }}"
    namespace = "{{ namespace }}"
    project = "{{ project }}"
    roleset = "{{ roleset }}"
    secret_type = "{{ secret_type }}"
    service_account_email = "{{ service_account_email }}"
    token_scopes = "{{ token_scopes }}"
}