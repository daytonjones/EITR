data "vault_nomad_access_token" "example" {
    accessor_id = "{{ accessor_id }}"
    backend = "{{ backend }}"
    id = "{{ id }}"
    namespace = "{{ namespace }}"
    role = "{{ role }}"
    secret_id = "{{ secret_id }}"
}