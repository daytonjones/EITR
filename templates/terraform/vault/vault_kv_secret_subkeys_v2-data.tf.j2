data "vault_kv_secret_subkeys_v2" "example" {
    data = "{{ data }}"
    data_json = "{{ data_json }}"
    depth = "{{ depth }}"
    id = "{{ id }}"
    mount = "{{ mount }}"
    name = "{{ name }}"
    namespace = "{{ namespace }}"
    path = "{{ path }}"
    version = "{{ version }}"
}