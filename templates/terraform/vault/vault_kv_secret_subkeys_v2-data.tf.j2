data "vault_kv_secret_subkeys_v2" "example" {
    depth = {{ depth }}
    id = "{{ id }}"
    mount = "{{ mount }}"
    name = "{{ name }}"
    namespace = "{{ namespace }}"
    version = {{ version }}
}
