data "vault_identity_oidc_client_creds" "example" {
    client_id = "{{ client_id }}"
    client_secret = "{{ client_secret }}"
    id = "{{ id }}"
    name = "{{ name }}"
    namespace = "{{ namespace }}"
}