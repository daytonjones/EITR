ephemeral "vault_gcp_service_account_key" "example" {
    key_algorithm = "{{ key_algorithm }}"
    key_type = "{{ key_type }}"
    mount = "{{ mount }}"
    mount_id = "{{ mount_id }}"
    namespace = "{{ namespace }}"
    roleset = "{{ roleset }}"
    static_account = "{{ static_account }}"
}
