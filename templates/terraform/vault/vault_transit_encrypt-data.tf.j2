data "vault_transit_encrypt" "example" {
    backend = "{{ backend }}"
    context = "{{ context }}"
    id = "{{ id }}"
    key = "{{ key }}"
    key_version = {{ key_version }}
    namespace = "{{ namespace }}"
    plaintext = "{{ plaintext }}"
}
