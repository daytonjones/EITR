ephemeral "vault_spiffe_secret_backend_mintjwt" "example" {
    audience = "{{ audience }}"
    mount = "{{ mount }}"
    mount_id = "{{ mount_id }}"
    name = "{{ name }}"
    namespace = "{{ namespace }}"
}
