resource "vault_keymgmt_distribute_key" "example" {
    key_name = "{{ key_name }}"
    kms_name = "{{ kms_name }}"
    mount = "{{ mount }}"
    namespace = "{{ namespace }}"
    protection = "{{ protection }}"
    purpose = ["{{ purpose }}"]
}
