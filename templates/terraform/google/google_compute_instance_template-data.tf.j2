data "google_compute_instance_template" "example" {
    filter = "{{ filter }}"
    id = "{{ id }}"
    most_recent = {{ most_recent }}
    name = "{{ name }}"
    project = "{{ project }}"
    self_link_unique = "{{ self_link_unique }}"
}
