resource "google_data_catalog_tag" "example" {
    column = "{{ column }}"
    id = "{{ id }}"
    name = "{{ name }}"
    parent = "{{ parent }}"
    template = "{{ template }}"
    template_displayname = "{{ template_displayname }}"
}