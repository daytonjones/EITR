resource "google_storage_object_acl" "example" {
    bucket = "{{ bucket }}"
    id = "{{ id }}"
    object = "{{ object }}"
    predefined_acl = "{{ predefined_acl }}"
    role_entity = "{{ role_entity }}"
}