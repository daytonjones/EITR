resource "google_gemini_repository_group_iam_binding" "example" {
    code_repository_index = "{{ code_repository_index }}"
    id = "{{ id }}"
    location = "{{ location }}"
    members = ["{{ members }}"]
    project = "{{ project }}"
    repository_group_id = "{{ repository_group_id }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
