ephemeral "google_service_account_jwt" "example" {
    delegates = ["{{ delegates }}"]
    expires_in = {{ expires_in }}
    payload = "{{ payload }}"
    target_service_account = "{{ target_service_account }}"
}
