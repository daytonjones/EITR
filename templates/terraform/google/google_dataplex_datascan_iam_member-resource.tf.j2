resource "google_dataplex_datascan_iam_member" "example" {
    data_scan_id = "{{ data_scan_id }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    member = "{{ member }}"
    project = "{{ project }}"
    role = "{{ role }}"
}