resource "google_cloud_run_v2_job_iam_binding" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    members = "{{ members }}"
    name = "{{ name }}"
    project = "{{ project }}"
    role = "{{ role }}"
}