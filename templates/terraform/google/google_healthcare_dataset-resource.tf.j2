resource "google_healthcare_dataset" "example" {
    id = "{{ id }}"
    location = "{{ location }}"
    name = "{{ name }}"
    project = "{{ project }}"
    self_link = "{{ self_link }}"
    time_zone = "{{ time_zone }}"
}