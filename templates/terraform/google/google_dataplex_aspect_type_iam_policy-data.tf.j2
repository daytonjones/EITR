data "google_dataplex_aspect_type_iam_policy" "example" {
    aspect_type_id = "{{ aspect_type_id }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
}