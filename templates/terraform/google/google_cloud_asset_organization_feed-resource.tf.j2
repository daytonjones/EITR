resource "google_cloud_asset_organization_feed" "example" {
    asset_names = "{{ asset_names }}"
    asset_types = "{{ asset_types }}"
    billing_project = "{{ billing_project }}"
    content_type = "{{ content_type }}"
    feed_id = "{{ feed_id }}"
    id = "{{ id }}"
    name = "{{ name }}"
    org_id = "{{ org_id }}"
}