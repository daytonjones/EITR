resource "google_network_security_address_group_iam_policy" "example" {
    id = "{{ id }}"
    location = "{{ location }}"
    name = "{{ name }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
}
