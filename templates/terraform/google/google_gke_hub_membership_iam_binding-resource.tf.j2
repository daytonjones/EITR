resource "google_gke_hub_membership_iam_binding" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    members = "{{ members }}"
    membership_id = "{{ membership_id }}"
    project = "{{ project }}"
    role = "{{ role }}"
}