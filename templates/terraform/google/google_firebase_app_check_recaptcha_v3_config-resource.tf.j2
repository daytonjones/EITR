resource "google_firebase_app_check_recaptcha_v3_config" "example" {
    app_id = "{{ app_id }}"
    id = "{{ id }}"
    name = "{{ name }}"
    project = "{{ project }}"
    site_secret = "{{ site_secret }}"
    site_secret_set = "{{ site_secret_set }}"
    token_ttl = "{{ token_ttl }}"
}