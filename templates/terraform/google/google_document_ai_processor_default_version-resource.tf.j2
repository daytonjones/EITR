resource "google_document_ai_processor_default_version" "example" {
    id = "{{ id }}"
    processor = "{{ processor }}"
    version = "{{ version }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
    }
}
