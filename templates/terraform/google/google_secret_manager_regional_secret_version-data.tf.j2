data "google_secret_manager_regional_secret_version" "example" {
    id = "{{ id }}"
    is_secret_data_base64 = {{ is_secret_data_base64 }}
    location = "{{ location }}"
    project = "{{ project }}"
    secret = "{{ secret }}"
    version = "{{ version }}"
}
