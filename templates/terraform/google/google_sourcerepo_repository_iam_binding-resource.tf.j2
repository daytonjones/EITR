resource "google_sourcerepo_repository_iam_binding" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    members = "{{ members }}"
    project = "{{ project }}"
    repository = "{{ repository }}"
    role = "{{ role }}"
}