data "google_artifact_registry_tags" "example" {
    filter = "{{ filter }}"
    id = "{{ id }}"
    location = "{{ location }}"
    package_name = "{{ package_name }}"
    project = "{{ project }}"
    repository_id = "{{ repository_id }}"
}
