resource "google_compute_instance_iam_policy" "example" {
    id = "{{ id }}"
    instance_name = "{{ instance_name }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    zone = "{{ zone }}"
}
