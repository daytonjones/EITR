data "google_compute_default_service_account" "example" {
    display_name = "{{ display_name }}"
    email = "{{ email }}"
    id = "{{ id }}"
    member = "{{ member }}"
    name = "{{ name }}"
    project = "{{ project }}"
    unique_id = "{{ unique_id }}"
}