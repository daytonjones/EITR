resource "google_cloud_tasks_queue_iam_member" "example" {
    id = "{{ id }}"
    location = "{{ location }}"
    member = "{{ member }}"
    name = "{{ name }}"
    project = "{{ project }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
