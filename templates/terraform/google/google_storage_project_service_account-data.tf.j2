data "google_storage_project_service_account" "example" {
    email_address = "{{ email_address }}"
    id = "{{ id }}"
    member = "{{ member }}"
    project = "{{ project }}"
    user_project = "{{ user_project }}"
}