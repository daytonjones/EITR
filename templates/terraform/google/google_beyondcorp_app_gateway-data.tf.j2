data "google_beyondcorp_app_gateway" "example" {
    allocated_connections = "{{ allocated_connections }}"
    display_name = "{{ display_name }}"
    effective_labels = "{{ effective_labels }}"
    host_type = "{{ host_type }}"
    id = "{{ id }}"
    labels = "{{ labels }}"
    name = "{{ name }}"
    project = "{{ project }}"
    region = "{{ region }}"
    state = "{{ state }}"
    terraform_labels = "{{ terraform_labels }}"
    type = "{{ type }}"
    uri = "{{ uri }}"
}