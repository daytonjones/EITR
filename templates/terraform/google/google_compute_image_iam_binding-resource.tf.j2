resource "google_compute_image_iam_binding" "example" {
    id = "{{ id }}"
    image = "{{ image }}"
    members = ["{{ members }}"]
    project = "{{ project }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
