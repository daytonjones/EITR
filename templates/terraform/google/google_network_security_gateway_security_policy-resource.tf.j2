resource "google_network_security_gateway_security_policy" "example" {
    create_time = "{{ create_time }}"
    description = "{{ description }}"
    id = "{{ id }}"
    location = "{{ location }}"
    name = "{{ name }}"
    project = "{{ project }}"
    self_link = "{{ self_link }}"
    tls_inspection_policy = "{{ tls_inspection_policy }}"
    update_time = "{{ update_time }}"
}