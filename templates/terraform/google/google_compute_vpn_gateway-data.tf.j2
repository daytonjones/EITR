data "google_compute_vpn_gateway" "example" {
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    network = "{{ network }}"
    project = "{{ project }}"
    region = "{{ region }}"
    self_link = "{{ self_link }}"
}