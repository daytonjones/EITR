data "google_discovery_engine_data_store" "example" {
    data_store_id = "{{ data_store_id }}"
    display_name = "{{ display_name }}"
    id = "{{ id }}"
    location = "{{ location }}"
    project = "{{ project }}"
}
