resource "google_dataproc_metastore_service_iam_policy" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    service_id = "{{ service_id }}"
}