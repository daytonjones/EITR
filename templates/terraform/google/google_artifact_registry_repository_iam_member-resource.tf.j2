resource "google_artifact_registry_repository_iam_member" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    member = "{{ member }}"
    project = "{{ project }}"
    repository = "{{ repository }}"
    role = "{{ role }}"
}