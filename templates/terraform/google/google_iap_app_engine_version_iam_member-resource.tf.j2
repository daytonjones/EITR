resource "google_iap_app_engine_version_iam_member" "example" {
    app_id = "{{ app_id }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    member = "{{ member }}"
    project = "{{ project }}"
    role = "{{ role }}"
    service = "{{ service }}"
    version_id = "{{ version_id }}"
}