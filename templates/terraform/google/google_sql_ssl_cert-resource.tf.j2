resource "google_sql_ssl_cert" "example" {
    common_name = "{{ common_name }}"
    id = "{{ id }}"
    instance = "{{ instance }}"
    project = "{{ project }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
    }
}
