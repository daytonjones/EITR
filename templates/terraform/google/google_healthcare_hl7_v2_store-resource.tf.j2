resource "google_healthcare_hl7_v2_store" "example" {
    dataset = "{{ dataset }}"
    effective_labels = "{{ effective_labels }}"
    id = "{{ id }}"
    labels = "{{ labels }}"
    name = "{{ name }}"
    reject_duplicate_message = "{{ reject_duplicate_message }}"
    self_link = "{{ self_link }}"
    terraform_labels = "{{ terraform_labels }}"
}