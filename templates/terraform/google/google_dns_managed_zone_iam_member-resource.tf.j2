resource "google_dns_managed_zone_iam_member" "example" {
    id = "{{ id }}"
    managed_zone = "{{ managed_zone }}"
    member = "{{ member }}"
    project = "{{ project }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
