resource "google_dataplex_aspect_type_iam_binding" "example" {
    aspect_type_id = "{{ aspect_type_id }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    members = "{{ members }}"
    project = "{{ project }}"
    role = "{{ role }}"
}