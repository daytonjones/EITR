resource "google_compute_target_instance" "example" {
    description = "{{ description }}"
    id = "{{ id }}"
    instance = "{{ instance }}"
    name = "{{ name }}"
    nat_policy = "{{ nat_policy }}"
    project = "{{ project }}"
    zone = "{{ zone }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
    }
}
