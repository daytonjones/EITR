data "google_iap_web_region_backend_service_iam_policy" "example" {
    id = "{{ id }}"
    project = "{{ project }}"
    region = "{{ region }}"
    web_region_backend_service = "{{ web_region_backend_service }}"
}
