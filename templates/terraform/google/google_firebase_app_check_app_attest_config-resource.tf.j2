resource "google_firebase_app_check_app_attest_config" "example" {
    app_id = "{{ app_id }}"
    id = "{{ id }}"
    name = "{{ name }}"
    project = "{{ project }}"
    token_ttl = "{{ token_ttl }}"
}