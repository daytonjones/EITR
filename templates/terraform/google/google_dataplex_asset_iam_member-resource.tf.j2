resource "google_dataplex_asset_iam_member" "example" {
    asset = "{{ asset }}"
    dataplex_zone = "{{ dataplex_zone }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    lake = "{{ lake }}"
    location = "{{ location }}"
    member = "{{ member }}"
    project = "{{ project }}"
    role = "{{ role }}"
}