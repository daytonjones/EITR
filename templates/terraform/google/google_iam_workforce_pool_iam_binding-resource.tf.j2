resource "google_iam_workforce_pool_iam_binding" "example" {
    id = "{{ id }}"
    location = "{{ location }}"
    members = ["{{ members }}"]
    role = "{{ role }}"
    workforce_pool_id = "{{ workforce_pool_id }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
