data "google_certificate_manager_dns_authorization" "example" {
    domain = "{{ domain }}"
    id = "{{ id }}"
    location = "{{ location }}"
    name = "{{ name }}"
    project = "{{ project }}"
}
