resource "google_dataplex_asset_iam_policy" "example" {
    asset = "{{ asset }}"
    dataplex_zone = "{{ dataplex_zone }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    lake = "{{ lake }}"
    location = "{{ location }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
}