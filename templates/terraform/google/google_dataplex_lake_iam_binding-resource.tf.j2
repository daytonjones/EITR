resource "google_dataplex_lake_iam_binding" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    lake = "{{ lake }}"
    location = "{{ location }}"
    members = "{{ members }}"
    project = "{{ project }}"
    role = "{{ role }}"
}