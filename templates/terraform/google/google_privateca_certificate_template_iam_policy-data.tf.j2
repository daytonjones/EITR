data "google_privateca_certificate_template_iam_policy" "example" {
    certificate_template = "{{ certificate_template }}"
    id = "{{ id }}"
    location = "{{ location }}"
    project = "{{ project }}"
}
