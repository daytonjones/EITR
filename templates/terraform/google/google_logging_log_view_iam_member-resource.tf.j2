resource "google_logging_log_view_iam_member" "example" {
    bucket = "{{ bucket }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    member = "{{ member }}"
    name = "{{ name }}"
    parent = "{{ parent }}"
    role = "{{ role }}"
}