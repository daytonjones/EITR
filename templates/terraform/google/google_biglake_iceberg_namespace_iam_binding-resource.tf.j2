resource "google_biglake_iceberg_namespace_iam_binding" "example" {
    catalog = "{{ catalog }}"
    id = "{{ id }}"
    members = ["{{ members }}"]
    namespace_id = "{{ namespace_id }}"
    project = "{{ project }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
