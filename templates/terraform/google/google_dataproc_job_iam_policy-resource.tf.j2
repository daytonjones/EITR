resource "google_dataproc_job_iam_policy" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    job_id = "{{ job_id }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    region = "{{ region }}"
}