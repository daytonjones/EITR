resource "google_bigquery_datapolicy_data_policy_iam_binding" "example" {
    data_policy_id = "{{ data_policy_id }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    members = "{{ members }}"
    project = "{{ project }}"
    role = "{{ role }}"
}