resource "google_dataplex_asset_iam_binding" "example" {
    asset = "{{ asset }}"
    dataplex_zone = "{{ dataplex_zone }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    lake = "{{ lake }}"
    location = "{{ location }}"
    members = "{{ members }}"
    project = "{{ project }}"
    role = "{{ role }}"
}