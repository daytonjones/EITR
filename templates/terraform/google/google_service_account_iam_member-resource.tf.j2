resource "google_service_account_iam_member" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    member = "{{ member }}"
    role = "{{ role }}"
    service_account_id = "{{ service_account_id }}"
}