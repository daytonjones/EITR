data "google_dns_managed_zone" "example" {
    description = "{{ description }}"
    dns_name = "{{ dns_name }}"
    id = "{{ id }}"
    managed_zone_id = "{{ managed_zone_id }}"
    name = "{{ name }}"
    name_servers = "{{ name_servers }}"
    project = "{{ project }}"
    visibility = "{{ visibility }}"
}