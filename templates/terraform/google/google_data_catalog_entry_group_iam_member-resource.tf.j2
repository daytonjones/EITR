resource "google_data_catalog_entry_group_iam_member" "example" {
    entry_group = "{{ entry_group }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    member = "{{ member }}"
    project = "{{ project }}"
    region = "{{ region }}"
    role = "{{ role }}"
}