resource "google_artifact_registry_repository_iam_binding" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    members = "{{ members }}"
    project = "{{ project }}"
    repository = "{{ repository }}"
    role = "{{ role }}"
}