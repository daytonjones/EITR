resource "google_logging_folder_exclusion" "example" {
    description = "{{ description }}"
    disabled = "{{ disabled }}"
    filter = "{{ filter }}"
    folder = "{{ folder }}"
    id = "{{ id }}"
    name = "{{ name }}"
}