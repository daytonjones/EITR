resource "google_bigquery_analytics_hub_listing_iam_member" "example" {
    data_exchange_id = "{{ data_exchange_id }}"
    id = "{{ id }}"
    listing_id = "{{ listing_id }}"
    location = "{{ location }}"
    member = "{{ member }}"
    project = "{{ project }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
