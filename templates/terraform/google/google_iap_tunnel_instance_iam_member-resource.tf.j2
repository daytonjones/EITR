resource "google_iap_tunnel_instance_iam_member" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    instance = "{{ instance }}"
    member = "{{ member }}"
    project = "{{ project }}"
    role = "{{ role }}"
    zone = "{{ zone }}"
}