data "google_apphub_application" "example" {
    application_id = "{{ application_id }}"
    attributes = "{{ attributes }}"
    create_time = "{{ create_time }}"
    description = "{{ description }}"
    display_name = "{{ display_name }}"
    id = "{{ id }}"
    location = "{{ location }}"
    name = "{{ name }}"
    project = "{{ project }}"
    scope = "{{ scope }}"
    state = "{{ state }}"
    uid = "{{ uid }}"
    update_time = "{{ update_time }}"
}