resource "google_access_context_manager_access_policy_iam_binding" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    members = "{{ members }}"
    name = "{{ name }}"
    role = "{{ role }}"
}