resource "google_healthcare_pipeline_job" "example" {
    dataset = "{{ dataset }}"
    disable_lineage = "{{ disable_lineage }}"
    effective_labels = "{{ effective_labels }}"
    id = "{{ id }}"
    labels = "{{ labels }}"
    location = "{{ location }}"
    name = "{{ name }}"
    self_link = "{{ self_link }}"
    terraform_labels = "{{ terraform_labels }}"
}