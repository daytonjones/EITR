resource "google_project_iam_custom_role" "example" {
    deleted = "{{ deleted }}"
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    permissions = "{{ permissions }}"
    project = "{{ project }}"
    role_id = "{{ role_id }}"
    stage = "{{ stage }}"
    title = "{{ title }}"
}