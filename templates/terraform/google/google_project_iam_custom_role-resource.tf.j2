resource "google_project_iam_custom_role" "example" {
    description = "{{ description }}"
    id = "{{ id }}"
    permissions = ["{{ permissions }}"]
    project = "{{ project }}"
    role_id = "{{ role_id }}"
    stage = "{{ stage }}"
    title = "{{ title }}"
}
