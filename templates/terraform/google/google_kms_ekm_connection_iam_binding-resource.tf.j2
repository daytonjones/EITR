resource "google_kms_ekm_connection_iam_binding" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    members = "{{ members }}"
    name = "{{ name }}"
    project = "{{ project }}"
    role = "{{ role }}"
}