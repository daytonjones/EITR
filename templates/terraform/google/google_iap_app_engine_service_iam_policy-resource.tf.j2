resource "google_iap_app_engine_service_iam_policy" "example" {
    app_id = "{{ app_id }}"
    id = "{{ id }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    service = "{{ service }}"
}
