resource "google_dataplex_glossary_iam_policy" "example" {
    glossary_id = "{{ glossary_id }}"
    id = "{{ id }}"
    location = "{{ location }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
}
