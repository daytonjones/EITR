resource "google_bigquery_dataset_iam_member" "example" {
    dataset_id = "{{ dataset_id }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    member = "{{ member }}"
    project = "{{ project }}"
    role = "{{ role }}"
}