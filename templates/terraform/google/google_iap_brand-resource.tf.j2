resource "google_iap_brand" "example" {
    application_title = "{{ application_title }}"
    id = "{{ id }}"
    project = "{{ project }}"
    support_email = "{{ support_email }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
    }
}
