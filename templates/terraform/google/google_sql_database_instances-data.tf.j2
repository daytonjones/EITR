data "google_sql_database_instances" "example" {
    database_version = "{{ database_version }}"
    id = "{{ id }}"
    project = "{{ project }}"
    region = "{{ region }}"
    state = "{{ state }}"
    tier = "{{ tier }}"
    zone = "{{ zone }}"
}
