resource "google_folder_access_approval_settings" "example" {
    active_key_version = "{{ active_key_version }}"
    folder_id = "{{ folder_id }}"
    id = "{{ id }}"
    notification_emails = ["{{ notification_emails }}"]
    enrolled_services {
      cloud_product = "{{ cloud_product }}"
      enrollment_level = "{{ enrollment_level }}"
    }
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
