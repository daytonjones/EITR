resource "google_resource_manager_lien" "example" {
    create_time = "{{ create_time }}"
    id = "{{ id }}"
    name = "{{ name }}"
    origin = "{{ origin }}"
    parent = "{{ parent }}"
    reason = "{{ reason }}"
    restrictions = "{{ restrictions }}"
}