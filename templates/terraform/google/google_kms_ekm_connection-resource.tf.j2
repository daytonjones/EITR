resource "google_kms_ekm_connection" "example" {
    create_time = "{{ create_time }}"
    crypto_space_path = "{{ crypto_space_path }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    key_management_mode = "{{ key_management_mode }}"
    location = "{{ location }}"
    name = "{{ name }}"
    project = "{{ project }}"
}