data "google_iam_testable_permissions" "example" {
    custom_support_level = "{{ custom_support_level }}"
    full_resource_name = "{{ full_resource_name }}"
    id = "{{ id }}"
    permissions = "{{ permissions }}"
    stages = "{{ stages }}"
}