resource "google_bigquery_reservation_assignment" "example" {
    assignee = "{{ assignee }}"
    id = "{{ id }}"
    job_type = "{{ job_type }}"
    location = "{{ location }}"
    name = "{{ name }}"
    project = "{{ project }}"
    reservation = "{{ reservation }}"
    state = "{{ state }}"
}