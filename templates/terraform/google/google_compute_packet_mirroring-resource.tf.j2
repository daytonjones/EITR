resource "google_compute_packet_mirroring" "example" {
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    priority = "{{ priority }}"
    project = "{{ project }}"
    region = "{{ region }}"
}