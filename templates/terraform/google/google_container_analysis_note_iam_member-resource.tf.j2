resource "google_container_analysis_note_iam_member" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    member = "{{ member }}"
    note = "{{ note }}"
    project = "{{ project }}"
    role = "{{ role }}"
}