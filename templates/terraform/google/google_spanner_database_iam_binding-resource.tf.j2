resource "google_spanner_database_iam_binding" "example" {
    database = "{{ database }}"
    id = "{{ id }}"
    instance = "{{ instance }}"
    members = ["{{ members }}"]
    project = "{{ project }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
