resource "google_app_engine_firewall_rule" "example" {
    action = "{{ action }}"
    description = "{{ description }}"
    id = "{{ id }}"
    priority = "{{ priority }}"
    project = "{{ project }}"
    source_range = "{{ source_range }}"
}