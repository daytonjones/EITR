resource "google_storage_hmac_key" "example" {
    access_id = "{{ access_id }}"
    id = "{{ id }}"
    project = "{{ project }}"
    secret = "{{ secret }}"
    service_account_email = "{{ service_account_email }}"
    state = "{{ state }}"
    time_created = "{{ time_created }}"
    updated = "{{ updated }}"
}