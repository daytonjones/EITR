resource "google_healthcare_consent_store_iam_policy" "example" {
    consent_store_id = "{{ consent_store_id }}"
    dataset = "{{ dataset }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    policy_data = "{{ policy_data }}"
}