data "google_dataplex_zone_iam_policy" "example" {
    dataplex_zone = "{{ dataplex_zone }}"
    id = "{{ id }}"
    lake = "{{ lake }}"
    location = "{{ location }}"
    project = "{{ project }}"
}
