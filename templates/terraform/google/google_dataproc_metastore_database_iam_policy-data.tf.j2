data "google_dataproc_metastore_database_iam_policy" "example" {
    database = "{{ database }}"
    id = "{{ id }}"
    location = "{{ location }}"
    project = "{{ project }}"
    service_id = "{{ service_id }}"
}
