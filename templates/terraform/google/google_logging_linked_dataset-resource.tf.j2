resource "google_logging_linked_dataset" "example" {
    bucket = "{{ bucket }}"
    description = "{{ description }}"
    id = "{{ id }}"
    link_id = "{{ link_id }}"
    location = "{{ location }}"
    parent = "{{ parent }}"
    bigquery_dataset {
    }
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
    }
}
