resource "google_kms_secret_ciphertext" "example" {
    additional_authenticated_data = "{{ additional_authenticated_data }}"
    ciphertext = "{{ ciphertext }}"
    crypto_key = "{{ crypto_key }}"
    id = "{{ id }}"
    plaintext = "{{ plaintext }}"
}