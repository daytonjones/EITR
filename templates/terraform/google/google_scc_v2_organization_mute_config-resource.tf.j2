resource "google_scc_v2_organization_mute_config" "example" {
    description = "{{ description }}"
    filter = "{{ filter }}"
    id = "{{ id }}"
    location = "{{ location }}"
    mute_config_id = "{{ mute_config_id }}"
    organization = "{{ organization }}"
    type = "{{ type }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
