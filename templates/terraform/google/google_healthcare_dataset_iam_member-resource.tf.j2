resource "google_healthcare_dataset_iam_member" "example" {
    dataset_id = "{{ dataset_id }}"
    id = "{{ id }}"
    member = "{{ member }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
