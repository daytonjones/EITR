resource "google_compute_storage_pool_iam_policy" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    zone = "{{ zone }}"
}
