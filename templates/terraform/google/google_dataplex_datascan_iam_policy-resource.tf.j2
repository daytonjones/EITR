resource "google_dataplex_datascan_iam_policy" "example" {
    data_scan_id = "{{ data_scan_id }}"
    id = "{{ id }}"
    location = "{{ location }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
}
