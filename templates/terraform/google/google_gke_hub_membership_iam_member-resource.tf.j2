resource "google_gke_hub_membership_iam_member" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    member = "{{ member }}"
    membership_id = "{{ membership_id }}"
    project = "{{ project }}"
    role = "{{ role }}"
}