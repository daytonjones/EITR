resource "google_organization_iam_member" "example" {
    id = "{{ id }}"
    member = "{{ member }}"
    org_id = "{{ org_id }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
