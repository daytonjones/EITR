resource "google_network_security_url_lists" "example" {
    create_time = "{{ create_time }}"
    description = "{{ description }}"
    id = "{{ id }}"
    location = "{{ location }}"
    name = "{{ name }}"
    project = "{{ project }}"
    update_time = "{{ update_time }}"
    values = "{{ values }}"
}