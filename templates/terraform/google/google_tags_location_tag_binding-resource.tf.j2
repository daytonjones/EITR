resource "google_tags_location_tag_binding" "example" {
    id = "{{ id }}"
    location = "{{ location }}"
    parent = "{{ parent }}"
    tag_value = "{{ tag_value }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
    }
}
