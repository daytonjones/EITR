resource "google_backup_dr_management_server" "example" {
    id = "{{ id }}"
    location = "{{ location }}"
    name = "{{ name }}"
    project = "{{ project }}"
    type = "{{ type }}"
    networks {
      network = "{{ network }}"
      peering_mode = "{{ peering_mode }}"
    }
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
    }
}
