data "google_oracle_database_odb_subnet" "example" {
    id = "{{ id }}"
    location = "{{ location }}"
    odb_subnet_id = "{{ odb_subnet_id }}"
    odbnetwork = "{{ odbnetwork }}"
    project = "{{ project }}"
}
