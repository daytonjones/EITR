resource "google_service_networking_vpc_service_controls" "example" {
    enabled = "{{ enabled }}"
    id = "{{ id }}"
    network = "{{ network }}"
    project = "{{ project }}"
    service = "{{ service }}"
}