resource "google_logging_log_scope" "example" {
    create_time = "{{ create_time }}"
    description = "{{ description }}"
    id = "{{ id }}"
    location = "{{ location }}"
    name = "{{ name }}"
    parent = "{{ parent }}"
    resource_names = "{{ resource_names }}"
    update_time = "{{ update_time }}"
}