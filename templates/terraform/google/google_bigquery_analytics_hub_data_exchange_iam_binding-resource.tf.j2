resource "google_bigquery_analytics_hub_data_exchange_iam_binding" "example" {
    data_exchange_id = "{{ data_exchange_id }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    members = "{{ members }}"
    project = "{{ project }}"
    role = "{{ role }}"
}