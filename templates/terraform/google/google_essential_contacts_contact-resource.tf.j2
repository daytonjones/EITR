resource "google_essential_contacts_contact" "example" {
    email = "{{ email }}"
    id = "{{ id }}"
    language_tag = "{{ language_tag }}"
    notification_category_subscriptions = ["{{ notification_category_subscriptions }}"]
    parent = "{{ parent }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
