data "google_billing_account" "example" {
    billing_account = "{{ billing_account }}"
    display_name = "{{ display_name }}"
    id = "{{ id }}"
    lookup_projects = "{{ lookup_projects }}"
    name = "{{ name }}"
    open = "{{ open }}"
    project_ids = "{{ project_ids }}"
}