resource "google_scc_v2_organization_source_iam_member" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    member = "{{ member }}"
    organization = "{{ organization }}"
    role = "{{ role }}"
    source = "{{ source }}"
}