resource "google_iap_client" "example" {
    brand = "{{ brand }}"
    display_name = "{{ display_name }}"
    id = "{{ id }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
    }
}
