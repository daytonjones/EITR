resource "google_beyondcorp_security_gateway_application_iam_binding" "example" {
    application_id = "{{ application_id }}"
    id = "{{ id }}"
    members = ["{{ members }}"]
    project = "{{ project }}"
    role = "{{ role }}"
    security_gateway_id = "{{ security_gateway_id }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
}
