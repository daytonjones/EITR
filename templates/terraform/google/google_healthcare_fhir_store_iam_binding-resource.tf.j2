resource "google_healthcare_fhir_store_iam_binding" "example" {
    etag = "{{ etag }}"
    fhir_store_id = "{{ fhir_store_id }}"
    id = "{{ id }}"
    members = "{{ members }}"
    role = "{{ role }}"
}