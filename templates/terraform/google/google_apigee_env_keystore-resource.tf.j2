resource "google_apigee_env_keystore" "example" {
    env_id = "{{ env_id }}"
    id = "{{ id }}"
    name = "{{ name }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
    }
}
