resource "google_tags_tag_value" "example" {
    create_time = "{{ create_time }}"
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    namespaced_name = "{{ namespaced_name }}"
    parent = "{{ parent }}"
    short_name = "{{ short_name }}"
    update_time = "{{ update_time }}"
}