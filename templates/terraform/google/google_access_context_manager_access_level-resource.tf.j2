resource "google_access_context_manager_access_level" "example" {
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    parent = "{{ parent }}"
    title = "{{ title }}"
}