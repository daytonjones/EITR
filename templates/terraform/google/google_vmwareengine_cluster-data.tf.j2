data "google_vmwareengine_cluster" "example" {
    autoscaling_settings = "{{ autoscaling_settings }}"
    id = "{{ id }}"
    management = "{{ management }}"
    name = "{{ name }}"
    node_type_configs = "{{ node_type_configs }}"
    parent = "{{ parent }}"
    state = "{{ state }}"
    uid = "{{ uid }}"
}