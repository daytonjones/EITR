resource "google_iap_web_backend_service_iam_member" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    member = "{{ member }}"
    project = "{{ project }}"
    role = "{{ role }}"
    web_backend_service = "{{ web_backend_service }}"
}