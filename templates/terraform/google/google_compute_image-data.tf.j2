data "google_compute_image" "example" {
    family = "{{ family }}"
    filter = "{{ filter }}"
    id = "{{ id }}"
    most_recent = {{ most_recent }}
    name = "{{ name }}"
    project = "{{ project }}"
}
