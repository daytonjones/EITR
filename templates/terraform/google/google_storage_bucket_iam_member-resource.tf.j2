resource "google_storage_bucket_iam_member" "example" {
    bucket = "{{ bucket }}"
    id = "{{ id }}"
    member = "{{ member }}"
    role = "{{ role }}"
    condition {
      description = "{{ description }}"
      expression = "{{ expression }}"
      title = "{{ title }}"
    }
    timeouts {
      create = "{{ create }}"
    }
}
