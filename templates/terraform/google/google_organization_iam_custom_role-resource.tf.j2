resource "google_organization_iam_custom_role" "example" {
    deleted = "{{ deleted }}"
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    org_id = "{{ org_id }}"
    permissions = "{{ permissions }}"
    role_id = "{{ role_id }}"
    stage = "{{ stage }}"
    title = "{{ title }}"
}