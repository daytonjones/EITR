resource "google_compute_snapshot_iam_member" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    member = "{{ member }}"
    name = "{{ name }}"
    project = "{{ project }}"
    role = "{{ role }}"
}