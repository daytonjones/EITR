data "google_dns_record_set" "example" {
    id = "{{ id }}"
    managed_zone = "{{ managed_zone }}"
    name = "{{ name }}"
    project = "{{ project }}"
    rrdatas = "{{ rrdatas }}"
    ttl = "{{ ttl }}"
    type = "{{ type }}"
}