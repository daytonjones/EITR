resource "google_apigee_nat_address" "example" {
    activate = {{ activate }}
    id = "{{ id }}"
    instance_id = "{{ instance_id }}"
    name = "{{ name }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
