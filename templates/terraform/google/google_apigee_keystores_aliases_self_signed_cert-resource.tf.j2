resource "google_apigee_keystores_aliases_self_signed_cert" "example" {
    alias = "{{ alias }}"
    cert_validity_in_days = "{{ cert_validity_in_days }}"
    certs_info = "{{ certs_info }}"
    environment = "{{ environment }}"
    id = "{{ id }}"
    key_size = "{{ key_size }}"
    keystore = "{{ keystore }}"
    org_id = "{{ org_id }}"
    sig_alg = "{{ sig_alg }}"
    type = "{{ type }}"
}