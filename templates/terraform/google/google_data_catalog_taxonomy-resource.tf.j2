resource "google_data_catalog_taxonomy" "example" {
    activated_policy_types = "{{ activated_policy_types }}"
    description = "{{ description }}"
    display_name = "{{ display_name }}"
    id = "{{ id }}"
    name = "{{ name }}"
    project = "{{ project }}"
    region = "{{ region }}"
}