resource "google_spanner_database_iam_policy" "example" {
    database = "{{ database }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    instance = "{{ instance }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
}