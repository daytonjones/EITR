data "google_oracle_database_db_nodes" "example" {
    cloud_vm_cluster = "{{ cloud_vm_cluster }}"
    db_nodes = "{{ db_nodes }}"
    id = "{{ id }}"
    location = "{{ location }}"
    project = "{{ project }}"
}