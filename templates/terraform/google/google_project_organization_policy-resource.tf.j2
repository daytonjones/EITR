resource "google_project_organization_policy" "example" {
    constraint = "{{ constraint }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    project = "{{ project }}"
    update_time = "{{ update_time }}"
    version = "{{ version }}"
}