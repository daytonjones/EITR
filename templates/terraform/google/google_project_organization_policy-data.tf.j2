data "google_project_organization_policy" "example" {
    boolean_policy = "{{ boolean_policy }}"
    constraint = "{{ constraint }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    list_policy = "{{ list_policy }}"
    project = "{{ project }}"
    restore_policy = "{{ restore_policy }}"
    update_time = "{{ update_time }}"
    version = "{{ version }}"
}