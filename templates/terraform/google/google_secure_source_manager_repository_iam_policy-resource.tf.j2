resource "google_secure_source_manager_repository_iam_policy" "example" {
    id = "{{ id }}"
    location = "{{ location }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    repository_id = "{{ repository_id }}"
}
