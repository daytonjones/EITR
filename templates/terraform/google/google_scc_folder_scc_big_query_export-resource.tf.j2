resource "google_scc_folder_scc_big_query_export" "example" {
    big_query_export_id = "{{ big_query_export_id }}"
    dataset = "{{ dataset }}"
    description = "{{ description }}"
    filter = "{{ filter }}"
    folder = "{{ folder }}"
    id = "{{ id }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
