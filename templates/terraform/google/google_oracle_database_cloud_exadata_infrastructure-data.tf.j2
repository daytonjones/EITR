data "google_oracle_database_cloud_exadata_infrastructure" "example" {
    cloud_exadata_infrastructure_id = "{{ cloud_exadata_infrastructure_id }}"
    id = "{{ id }}"
    location = "{{ location }}"
    project = "{{ project }}"
}
