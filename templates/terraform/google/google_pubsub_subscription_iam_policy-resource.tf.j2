resource "google_pubsub_subscription_iam_policy" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    subscription = "{{ subscription }}"
}