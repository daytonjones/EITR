resource "google_compute_image_iam_member" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    image = "{{ image }}"
    member = "{{ member }}"
    project = "{{ project }}"
    role = "{{ role }}"
}