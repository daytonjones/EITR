resource "google_network_services_multicast_producer_association" "example" {
    description = "{{ description }}"
    id = "{{ id }}"
    labels = {
        key = "{{ labels_value }}"
      }
    location = "{{ location }}"
    multicast_domain_activation = "{{ multicast_domain_activation }}"
    multicast_producer_association_id = "{{ multicast_producer_association_id }}"
    network = "{{ network }}"
    project = "{{ project }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
