data "google_vertex_ai_reasoning_engine_query" "example" {
    class_method = "{{ class_method }}"
    id = "{{ id }}"
    input = "{{ input }}"
    project = "{{ project }}"
    reasoning_engine_id = "{{ reasoning_engine_id }}"
    region = "{{ region }}"
}
