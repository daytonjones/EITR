resource "google_compute_instance_iam_binding" "example" {
    etag = "{{ etag }}"
    id = "{{ id }}"
    instance_name = "{{ instance_name }}"
    members = "{{ members }}"
    project = "{{ project }}"
    role = "{{ role }}"
    zone = "{{ zone }}"
}