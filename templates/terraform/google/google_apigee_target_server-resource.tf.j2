resource "google_apigee_target_server" "example" {
    description = "{{ description }}"
    env_id = "{{ env_id }}"
    host = "{{ host }}"
    id = "{{ id }}"
    is_enabled = "{{ is_enabled }}"
    name = "{{ name }}"
    port = "{{ port }}"
    protocol = "{{ protocol }}"
}