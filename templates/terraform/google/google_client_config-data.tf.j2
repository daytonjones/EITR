data "google_client_config" "example" {
    access_token = "{{ access_token }}"
    default_labels = "{{ default_labels }}"
    id = "{{ id }}"
    project = "{{ project }}"
    region = "{{ region }}"
    zone = "{{ zone }}"
}