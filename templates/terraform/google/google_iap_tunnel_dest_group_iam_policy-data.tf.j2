data "google_iap_tunnel_dest_group_iam_policy" "example" {
    dest_group = "{{ dest_group }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    policy_data = "{{ policy_data }}"
    project = "{{ project }}"
    region = "{{ region }}"
}