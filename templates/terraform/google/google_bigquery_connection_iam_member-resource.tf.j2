resource "google_bigquery_connection_iam_member" "example" {
    connection_id = "{{ connection_id }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    location = "{{ location }}"
    member = "{{ member }}"
    project = "{{ project }}"
    role = "{{ role }}"
}