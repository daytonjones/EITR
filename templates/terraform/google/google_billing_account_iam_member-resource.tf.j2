resource "google_billing_account_iam_member" "example" {
    billing_account_id = "{{ billing_account_id }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    member = "{{ member }}"
    role = "{{ role }}"
}