resource "boundary_managed_group" "example" {
    auth_method_id = "{{ auth_method_id }}"
    description = "{{ description }}"
    filter = "{{ filter }}"
    id = "{{ id }}"
    name = "{{ name }}"
}