data "boundary_group" "example" {
    description = "{{ description }}"
    id = "{{ id }}"
    member_ids = "{{ member_ids }}"
    name = "{{ name }}"
    scope = "{{ scope }}"
    scope_id = "{{ scope_id }}"
}