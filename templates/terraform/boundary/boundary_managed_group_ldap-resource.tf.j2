resource "boundary_managed_group_ldap" "example" {
    auth_method_id = "{{ auth_method_id }}"
    description = "{{ description }}"
    group_names = "{{ group_names }}"
    id = "{{ id }}"
    name = "{{ name }}"
}