resource "boundary_account" "example" {
    auth_method_id = "{{ auth_method_id }}"
    description = "{{ description }}"
    id = "{{ id }}"
    login_name = "{{ login_name }}"
    name = "{{ name }}"
    password = "{{ password }}"
    type = "{{ type }}"
}