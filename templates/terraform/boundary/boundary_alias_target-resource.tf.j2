resource "boundary_alias_target" "example" {
    authorize_session_host_id = "{{ authorize_session_host_id }}"
    description = "{{ description }}"
    destination_id = "{{ destination_id }}"
    id = "{{ id }}"
    name = "{{ name }}"
    scope_id = "{{ scope_id }}"
    type = "{{ type }}"
    value = "{{ value }}"
}