data "boundary_auth_method" "example" {
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    scope = "{{ scope }}"
    scope_id = "{{ scope_id }}"
    type = "{{ type }}"
}