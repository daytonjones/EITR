resource "opc_compute_vnic_set" "example" {
    applied_acls = "{{ applied_acls }}"
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    virtual_nics = "{{ virtual_nics }}"
}