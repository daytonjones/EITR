resource "opc_compute_security_protocol" "example" {
    description = "{{ description }}"
    dst_ports = "{{ dst_ports }}"
    id = "{{ id }}"
    ip_protocol = "{{ ip_protocol }}"
    name = "{{ name }}"
    src_ports = "{{ src_ports }}"
    tags = "{{ tags }}"
    uri = "{{ uri }}"
}