data "opc_compute_machine_image" "example" {
    account = "{{ account }}"
    attributes = "{{ attributes }}"
    description = "{{ description }}"
    error_reason = "{{ error_reason }}"
    file = "{{ file }}"
    hypervisor = "{{ hypervisor }}"
    id = "{{ id }}"
    image_format = "{{ image_format }}"
    name = "{{ name }}"
    no_upload = "{{ no_upload }}"
    platform = "{{ platform }}"
    sizes = "{{ sizes }}"
    state = "{{ state }}"
    uri = "{{ uri }}"
}