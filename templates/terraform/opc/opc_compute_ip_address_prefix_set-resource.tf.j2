resource "opc_compute_ip_address_prefix_set" "example" {
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    prefixes = "{{ prefixes }}"
    tags = "{{ tags }}"
    uri = "{{ uri }}"
}