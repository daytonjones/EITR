resource "opc_compute_image_list_entry" "example" {
    attributes = "{{ attributes }}"
    id = "{{ id }}"
    machine_images = "{{ machine_images }}"
    name = "{{ name }}"
    uri = "{{ uri }}"
    version = "{{ version }}"
}