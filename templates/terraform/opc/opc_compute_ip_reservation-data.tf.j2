data "opc_compute_ip_reservation" "example" {
    id = "{{ id }}"
    ip = "{{ ip }}"
    name = "{{ name }}"
    parent_pool = "{{ parent_pool }}"
    permanent = "{{ permanent }}"
    tags = "{{ tags }}"
    used = "{{ used }}"
}