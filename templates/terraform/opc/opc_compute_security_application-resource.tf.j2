resource "opc_compute_security_application" "example" {
    description = "{{ description }}"
    dport = "{{ dport }}"
    icmpcode = "{{ icmpcode }}"
    icmptype = "{{ icmptype }}"
    id = "{{ id }}"
    name = "{{ name }}"
    protocol = "{{ protocol }}"
}