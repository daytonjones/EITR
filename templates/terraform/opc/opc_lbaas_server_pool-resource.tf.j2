resource "opc_lbaas_server_pool" "example" {
    consumers = "{{ consumers }}"
    enabled = "{{ enabled }}"
    id = "{{ id }}"
    load_balancer = "{{ load_balancer }}"
    name = "{{ name }}"
    operation_details = "{{ operation_details }}"
    servers = "{{ servers }}"
    state = "{{ state }}"
    status = "{{ status }}"
    tags = "{{ tags }}"
    uri = "{{ uri }}"
    vnic_set = "{{ vnic_set }}"
}