resource "opc_compute_snapshot" "example" {
    account = "{{ account }}"
    creation_time = "{{ creation_time }}"
    id = "{{ id }}"
    instance = "{{ instance }}"
    machine_image = "{{ machine_image }}"
    name = "{{ name }}"
    uri = "{{ uri }}"
}