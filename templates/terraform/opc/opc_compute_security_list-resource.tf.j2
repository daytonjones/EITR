resource "opc_compute_security_list" "example" {
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    outbound_cidr_policy = "{{ outbound_cidr_policy }}"
    policy = "{{ policy }}"
}