resource "opc_compute_sec_rule" "example" {
    action = "{{ action }}"
    application = "{{ application }}"
    description = "{{ description }}"
    destination_list = "{{ destination_list }}"
    disabled = "{{ disabled }}"
    id = "{{ id }}"
    name = "{{ name }}"
    source_list = "{{ source_list }}"
}