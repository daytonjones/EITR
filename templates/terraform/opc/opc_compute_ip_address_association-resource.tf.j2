resource "opc_compute_ip_address_association" "example" {
    description = "{{ description }}"
    id = "{{ id }}"
    ip_address_reservation = "{{ ip_address_reservation }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    uri = "{{ uri }}"
    vnic = "{{ vnic }}"
}