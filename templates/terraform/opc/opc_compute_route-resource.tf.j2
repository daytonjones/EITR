resource "opc_compute_route" "example" {
    admin_distance = "{{ admin_distance }}"
    description = "{{ description }}"
    id = "{{ id }}"
    ip_address_prefix = "{{ ip_address_prefix }}"
    name = "{{ name }}"
    next_hop_vnic_set = "{{ next_hop_vnic_set }}"
    tags = "{{ tags }}"
}