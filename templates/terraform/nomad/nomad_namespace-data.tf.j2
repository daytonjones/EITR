data "nomad_namespace" "example" {
    capabilities = "{{ capabilities }}"
    description = "{{ description }}"
    id = "{{ id }}"
    meta = "{{ meta }}"
    name = "{{ name }}"
    node_pool_config = "{{ node_pool_config }}"
    quota = "{{ quota }}"
}