data "nomad_scaling_policy" "example" {
    enabled = "{{ enabled }}"
    id = "{{ id }}"
    max = "{{ max }}"
    min = "{{ min }}"
    policy = "{{ policy }}"
    target = "{{ target }}"
    type = "{{ type }}"
}