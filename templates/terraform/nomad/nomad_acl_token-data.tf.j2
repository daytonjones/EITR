data "nomad_acl_token" "example" {
    accessor_id = "{{ accessor_id }}"
    create_time = "{{ create_time }}"
    expiration_time = "{{ expiration_time }}"
    expiration_ttl = "{{ expiration_ttl }}"
    global = "{{ global }}"
    id = "{{ id }}"
    name = "{{ name }}"
    policies = "{{ policies }}"
    roles = "{{ roles }}"
    secret_id = "{{ secret_id }}"
    type = "{{ type }}"
}