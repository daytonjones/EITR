resource "nomad_sentinel_policy" "example" {
    description = "{{ description }}"
    enforcement_level = "{{ enforcement_level }}"
    id = "{{ id }}"
    name = "{{ name }}"
    policy = "{{ policy }}"
    scope = "{{ scope }}"
}