resource "nomad_acl_policy" "example" {
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    rules_hcl = "{{ rules_hcl }}"
    job_acl {
      group = "{{ group }}"
      job_id = "{{ job_id }}"
      namespace = "{{ namespace }}"
      task = "{{ task }}"
    }
}
