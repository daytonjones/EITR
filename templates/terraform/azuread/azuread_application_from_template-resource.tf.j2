resource "azuread_application_from_template" "example" {
    display_name = "{{ display_name }}"
    id = "{{ id }}"
    template_id = "{{ template_id }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      read = "{{ read }}"
      update = "{{ update }}"
    }
}
