data "azuread_access_package" "example" {
    catalog_id = "{{ catalog_id }}"
    display_name = "{{ display_name }}"
    id = "{{ id }}"
    object_id = "{{ object_id }}"
    timeouts {
      read = "{{ read }}"
    }
}
