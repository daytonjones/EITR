data "azuread_application" "example" {
    client_id = "{{ client_id }}"
    display_name = "{{ display_name }}"
    id = "{{ id }}"
    identifier_uri = "{{ identifier_uri }}"
    object_id = "{{ object_id }}"
    timeouts {
      read = "{{ read }}"
    }
}
