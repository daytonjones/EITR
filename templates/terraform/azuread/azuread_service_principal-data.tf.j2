data "azuread_service_principal" "example" {
    client_id = "{{ client_id }}"
    display_name = "{{ display_name }}"
    id = "{{ id }}"
    object_id = "{{ object_id }}"
    timeouts {
      read = "{{ read }}"
    }
}
