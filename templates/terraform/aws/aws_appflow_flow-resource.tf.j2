resource "aws_appflow_flow" "example" {
    arn = "{{ arn }}"
    description = "{{ description }}"
    flow_status = "{{ flow_status }}"
    id = "{{ id }}"
    kms_arn = "{{ kms_arn }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}