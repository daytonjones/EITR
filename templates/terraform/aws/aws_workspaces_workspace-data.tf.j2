data "aws_workspaces_workspace" "example" {
    directory_id = "{{ directory_id }}"
    id = "{{ id }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
    user_name = "{{ user_name }}"
    workspace_id = "{{ workspace_id }}"
}
