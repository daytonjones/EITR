resource "aws_apigatewayv2_deployment" "example" {
    api_id = "{{ api_id }}"
    auto_deployed = "{{ auto_deployed }}"
    description = "{{ description }}"
    id = "{{ id }}"
    triggers = "{{ triggers }}"
}