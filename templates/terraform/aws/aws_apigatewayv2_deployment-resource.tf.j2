resource "aws_apigatewayv2_deployment" "example" {
    api_id = "{{ api_id }}"
    description = "{{ description }}"
    id = "{{ id }}"
    region = "{{ region }}"
    triggers = {
        key = "{{ triggers_value }}"
      }
}
