data "aws_kinesis_stream_consumer" "example" {
    arn = "{{ arn }}"
    creation_timestamp = "{{ creation_timestamp }}"
    id = "{{ id }}"
    name = "{{ name }}"
    status = "{{ status }}"
    stream_arn = "{{ stream_arn }}"
}