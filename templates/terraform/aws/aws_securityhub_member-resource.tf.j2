resource "aws_securityhub_member" "example" {
    account_id = "{{ account_id }}"
    email = "{{ email }}"
    id = "{{ id }}"
    invite = "{{ invite }}"
    master_id = "{{ master_id }}"
    member_status = "{{ member_status }}"
}