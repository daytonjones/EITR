resource "aws_inspector2_enabler" "example" {
    account_ids = ["{{ account_ids }}"]
    id = "{{ id }}"
    region = "{{ region }}"
    resource_types = ["{{ resource_types }}"]
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
