resource "aws_datasync_location_nfs" "example" {
    arn = "{{ arn }}"
    id = "{{ id }}"
    server_hostname = "{{ server_hostname }}"
    subdirectory = "{{ subdirectory }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
    uri = "{{ uri }}"
}