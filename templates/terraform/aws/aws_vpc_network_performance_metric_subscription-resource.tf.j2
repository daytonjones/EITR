resource "aws_vpc_network_performance_metric_subscription" "example" {
    destination = "{{ destination }}"
    id = "{{ id }}"
    metric = "{{ metric }}"
    period = "{{ period }}"
    source = "{{ source }}"
    statistic = "{{ statistic }}"
}