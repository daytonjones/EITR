resource "aws_ce_anomaly_subscription" "example" {
    account_id = "{{ account_id }}"
    arn = "{{ arn }}"
    frequency = "{{ frequency }}"
    id = "{{ id }}"
    monitor_arn_list = "{{ monitor_arn_list }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}