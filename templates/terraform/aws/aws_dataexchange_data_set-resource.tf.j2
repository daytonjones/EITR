resource "aws_dataexchange_data_set" "example" {
    arn = "{{ arn }}"
    asset_type = "{{ asset_type }}"
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}