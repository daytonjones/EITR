data "aws_api_gateway_api_key" "example" {
    arn = "{{ arn }}"
    created_date = "{{ created_date }}"
    customer_id = "{{ customer_id }}"
    description = "{{ description }}"
    enabled = "{{ enabled }}"
    id = "{{ id }}"
    last_updated_date = "{{ last_updated_date }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    value = "{{ value }}"
}