data "aws_ssmincidents_response_plan" "example" {
    action = "{{ action }}"
    arn = "{{ arn }}"
    chat_channel = "{{ chat_channel }}"
    display_name = "{{ display_name }}"
    engagements = "{{ engagements }}"
    id = "{{ id }}"
    incident_template = "{{ incident_template }}"
    integration = "{{ integration }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
}