data "aws_appconfig_environment" "example" {
    application_id = "{{ application_id }}"
    environment_id = "{{ environment_id }}"
    id = "{{ id }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
