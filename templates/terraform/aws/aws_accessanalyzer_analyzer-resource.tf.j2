resource "aws_accessanalyzer_analyzer" "example" {
    analyzer_name = "{{ analyzer_name }}"
    arn = "{{ arn }}"
    id = "{{ id }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
    type = "{{ type }}"
}