resource "aws_route53recoveryreadiness_recovery_group" "example" {
    arn = "{{ arn }}"
    cells = "{{ cells }}"
    id = "{{ id }}"
    recovery_group_name = "{{ recovery_group_name }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}