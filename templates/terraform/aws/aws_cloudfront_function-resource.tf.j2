resource "aws_cloudfront_function" "example" {
    code = "{{ code }}"
    comment = "{{ comment }}"
    id = "{{ id }}"
    key_value_store_associations = ["{{ key_value_store_associations }}"]
    name = "{{ name }}"
    publish = {{ publish }}
    runtime = "{{ runtime }}"
}
