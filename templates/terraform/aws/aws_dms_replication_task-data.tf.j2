data "aws_dms_replication_task" "example" {
    id = "{{ id }}"
    region = "{{ region }}"
    replication_task_id = "{{ replication_task_id }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
