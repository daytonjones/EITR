resource "aws_resourceexplorer2_index" "example" {
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
    type = "{{ type }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
