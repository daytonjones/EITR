resource "aws_resourceexplorer2_view" "example" {
    arn = "{{ arn }}"
    default_view = "{{ default_view }}"
    id = "{{ id }}"
    name = "{{ name }}"
    scope = "{{ scope }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}