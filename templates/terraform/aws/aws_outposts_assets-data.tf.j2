data "aws_outposts_assets" "example" {
    arn = "{{ arn }}"
    host_id_filter = ["{{ host_id_filter }}"]
    id = "{{ id }}"
    region = "{{ region }}"
    status_id_filter = ["{{ status_id_filter }}"]
}
