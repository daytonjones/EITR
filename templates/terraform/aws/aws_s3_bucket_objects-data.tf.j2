data "aws_s3_bucket_objects" "example" {
    bucket = "{{ bucket }}"
    delimiter = "{{ delimiter }}"
    encoding_type = "{{ encoding_type }}"
    fetch_owner = {{ fetch_owner }}
    id = "{{ id }}"
    max_keys = {{ max_keys }}
    prefix = "{{ prefix }}"
    region = "{{ region }}"
    start_after = "{{ start_after }}"
}
