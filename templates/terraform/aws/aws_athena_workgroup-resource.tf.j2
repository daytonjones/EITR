resource "aws_athena_workgroup" "example" {
    arn = "{{ arn }}"
    description = "{{ description }}"
    force_destroy = "{{ force_destroy }}"
    id = "{{ id }}"
    name = "{{ name }}"
    state = "{{ state }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}