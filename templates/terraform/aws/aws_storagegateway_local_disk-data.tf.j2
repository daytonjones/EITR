data "aws_storagegateway_local_disk" "example" {
    disk_id = "{{ disk_id }}"
    disk_node = "{{ disk_node }}"
    disk_path = "{{ disk_path }}"
    gateway_arn = "{{ gateway_arn }}"
    id = "{{ id }}"
}