resource "aws_wafregional_rule_group" "example" {
    arn = "{{ arn }}"
    id = "{{ id }}"
    metric_name = "{{ metric_name }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}