data "aws_ec2_local_gateway_virtual_interface_groups" "example" {
    id = "{{ id }}"
    ids = "{{ ids }}"
    local_gateway_virtual_interface_ids = "{{ local_gateway_virtual_interface_ids }}"
    tags = "{{ tags }}"
}