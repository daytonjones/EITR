data "aws_quicksight_theme" "example" {
    aws_account_id = "{{ aws_account_id }}"
    id = "{{ id }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
    theme_id = "{{ theme_id }}"
}
