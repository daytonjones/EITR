resource "aws_opensearch_inbound_connection_accepter" "example" {
    connection_id = "{{ connection_id }}"
    id = "{{ id }}"
    region = "{{ region }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
    }
}
