resource "aws_connect_bot_association" "example" {
    id = "{{ id }}"
    instance_id = "{{ instance_id }}"
    region = "{{ region }}"
    lex_bot {
      lex_region = "{{ lex_region }}"
      name = "{{ name }}"
    }
}
