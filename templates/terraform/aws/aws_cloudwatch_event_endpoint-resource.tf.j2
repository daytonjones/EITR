resource "aws_cloudwatch_event_endpoint" "example" {
    arn = "{{ arn }}"
    description = "{{ description }}"
    endpoint_url = "{{ endpoint_url }}"
    id = "{{ id }}"
    name = "{{ name }}"
    role_arn = "{{ role_arn }}"
}