resource "aws_xray_group" "example" {
    arn = "{{ arn }}"
    filter_expression = "{{ filter_expression }}"
    group_name = "{{ group_name }}"
    id = "{{ id }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}