data "aws_route53_resolver_endpoint" "example" {
    id = "{{ id }}"
    region = "{{ region }}"
    resolver_endpoint_id = "{{ resolver_endpoint_id }}"
    filter {
      name = "{{ name }}"
      values = ["{{ values }}"]
    }
}
