resource "aws_organizations_organizational_unit" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    parent_id = "{{ parent_id }}"
    tags = {
        key = "{{ tags_value }}"
      }
    tags_all = {
        key = "{{ tags_all_value }}"
      }
}
