resource "aws_organizations_organizational_unit" "example" {
    accounts = "{{ accounts }}"
    arn = "{{ arn }}"
    id = "{{ id }}"
    name = "{{ name }}"
    parent_id = "{{ parent_id }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}