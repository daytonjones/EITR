resource "aws_iot_thing_group" "example" {
    arn = "{{ arn }}"
    id = "{{ id }}"
    metadata = "{{ metadata }}"
    name = "{{ name }}"
    parent_group_name = "{{ parent_group_name }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
    version = "{{ version }}"
}