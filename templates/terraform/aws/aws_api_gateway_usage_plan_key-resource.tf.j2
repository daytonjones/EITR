resource "aws_api_gateway_usage_plan_key" "example" {
    id = "{{ id }}"
    key_id = "{{ key_id }}"
    key_type = "{{ key_type }}"
    name = "{{ name }}"
    usage_plan_id = "{{ usage_plan_id }}"
    value = "{{ value }}"
}