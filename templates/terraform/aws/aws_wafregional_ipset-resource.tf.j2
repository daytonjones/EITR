resource "aws_wafregional_ipset" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    region = "{{ region }}"
    ip_set_descriptor {
      type = "{{ type }}"
      value = "{{ value }}"
    }
}
