resource "aws_pinpoint_sms_channel" "example" {
    application_id = "{{ application_id }}"
    enabled = {{ enabled }}
    id = "{{ id }}"
    region = "{{ region }}"
    sender_id = "{{ sender_id }}"
    short_code = "{{ short_code }}"
}
