resource "aws_pinpoint_app" "example" {
    application_id = "{{ application_id }}"
    arn = "{{ arn }}"
    id = "{{ id }}"
    name = "{{ name }}"
    name_prefix = "{{ name_prefix }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}