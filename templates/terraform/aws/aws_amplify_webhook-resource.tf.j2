resource "aws_amplify_webhook" "example" {
    app_id = "{{ app_id }}"
    arn = "{{ arn }}"
    branch_name = "{{ branch_name }}"
    description = "{{ description }}"
    id = "{{ id }}"
    url = "{{ url }}"
}