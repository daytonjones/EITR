data "aws_api_gateway_vpc_link" "example" {
    arn = "{{ arn }}"
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    status = "{{ status }}"
    status_message = "{{ status_message }}"
    tags = "{{ tags }}"
    target_arns = "{{ target_arns }}"
}