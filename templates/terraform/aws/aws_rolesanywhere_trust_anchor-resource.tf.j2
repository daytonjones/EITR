resource "aws_rolesanywhere_trust_anchor" "example" {
    arn = "{{ arn }}"
    enabled = "{{ enabled }}"
    id = "{{ id }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}