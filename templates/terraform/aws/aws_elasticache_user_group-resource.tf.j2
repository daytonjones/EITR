resource "aws_elasticache_user_group" "example" {
    arn = "{{ arn }}"
    engine = "{{ engine }}"
    id = "{{ id }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
    user_group_id = "{{ user_group_id }}"
    user_ids = "{{ user_ids }}"
}