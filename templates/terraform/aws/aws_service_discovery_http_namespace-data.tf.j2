data "aws_service_discovery_http_namespace" "example" {
    arn = "{{ arn }}"
    description = "{{ description }}"
    http_name = "{{ http_name }}"
    id = "{{ id }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
}