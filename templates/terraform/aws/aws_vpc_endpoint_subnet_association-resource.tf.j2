resource "aws_vpc_endpoint_subnet_association" "example" {
    id = "{{ id }}"
    region = "{{ region }}"
    subnet_id = "{{ subnet_id }}"
    vpc_endpoint_id = "{{ vpc_endpoint_id }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
    }
}
