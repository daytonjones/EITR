resource "aws_iot_thing" "example" {
    attributes = {
        key = "{{ attributes_value }}"
      }
    id = "{{ id }}"
    name = "{{ name }}"
    region = "{{ region }}"
    thing_type_name = "{{ thing_type_name }}"
}
