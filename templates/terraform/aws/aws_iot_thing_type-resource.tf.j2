resource "aws_iot_thing_type" "example" {
    arn = "{{ arn }}"
    deprecated = "{{ deprecated }}"
    id = "{{ id }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}