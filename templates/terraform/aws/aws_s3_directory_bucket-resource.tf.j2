resource "aws_s3_directory_bucket" "example" {
    arn = "{{ arn }}"
    bucket = "{{ bucket }}"
    data_redundancy = "{{ data_redundancy }}"
    force_destroy = "{{ force_destroy }}"
    id = "{{ id }}"
    type = "{{ type }}"
}