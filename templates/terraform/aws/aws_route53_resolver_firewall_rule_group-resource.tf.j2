resource "aws_route53_resolver_firewall_rule_group" "example" {
    arn = "{{ arn }}"
    id = "{{ id }}"
    name = "{{ name }}"
    owner_id = "{{ owner_id }}"
    share_status = "{{ share_status }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}