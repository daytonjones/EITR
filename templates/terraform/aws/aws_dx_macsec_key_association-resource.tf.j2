resource "aws_dx_macsec_key_association" "example" {
    cak = "{{ cak }}"
    ckn = "{{ ckn }}"
    connection_id = "{{ connection_id }}"
    id = "{{ id }}"
    secret_arn = "{{ secret_arn }}"
    start_on = "{{ start_on }}"
    state = "{{ state }}"
}