resource "aws_inspector2_delegated_admin_account" "example" {
    account_id = "{{ account_id }}"
    id = "{{ id }}"
    region = "{{ region }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
    }
}
