data "aws_connect_queue" "example" {
    id = "{{ id }}"
    instance_id = "{{ instance_id }}"
    name = "{{ name }}"
    queue_id = "{{ queue_id }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
