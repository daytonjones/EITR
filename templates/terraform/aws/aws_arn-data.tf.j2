data "aws_arn" "example" {
    account = "{{ account }}"
    arn = "{{ arn }}"
    id = "{{ id }}"
    partition = "{{ partition }}"
    region = "{{ region }}"
    resource = "{{ resource }}"
    service = "{{ service }}"
}