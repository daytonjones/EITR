resource "aws_shield_drt_access_role_arn_association" "example" {
    role_arn = "{{ role_arn }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
