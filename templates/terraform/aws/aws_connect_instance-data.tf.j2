data "aws_connect_instance" "example" {
    id = "{{ id }}"
    instance_alias = "{{ instance_alias }}"
    instance_id = "{{ instance_id }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
