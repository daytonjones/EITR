resource "aws_api_gateway_resource" "example" {
    id = "{{ id }}"
    parent_id = "{{ parent_id }}"
    path_part = "{{ path_part }}"
    region = "{{ region }}"
    rest_api_id = "{{ rest_api_id }}"
}
