resource "aws_vpclattice_service_network" "example" {
    arn = "{{ arn }}"
    auth_type = "{{ auth_type }}"
    id = "{{ id }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}