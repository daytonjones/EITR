resource "aws_opensearch_package_association" "example" {
    domain_name = "{{ domain_name }}"
    id = "{{ id }}"
    package_id = "{{ package_id }}"
    region = "{{ region }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
    }
}
