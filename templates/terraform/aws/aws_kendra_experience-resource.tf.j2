resource "aws_kendra_experience" "example" {
    arn = "{{ arn }}"
    description = "{{ description }}"
    endpoints = "{{ endpoints }}"
    experience_id = "{{ experience_id }}"
    id = "{{ id }}"
    index_id = "{{ index_id }}"
    name = "{{ name }}"
    role_arn = "{{ role_arn }}"
    status = "{{ status }}"
}