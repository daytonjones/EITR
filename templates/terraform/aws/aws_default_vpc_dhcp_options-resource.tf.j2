resource "aws_default_vpc_dhcp_options" "example" {
    id = "{{ id }}"
    owner_id = "{{ owner_id }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
    tags_all = {
        key = "{{ tags_all_value }}"
      }
}
