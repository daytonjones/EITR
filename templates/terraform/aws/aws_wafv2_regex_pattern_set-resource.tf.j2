resource "aws_wafv2_regex_pattern_set" "example" {
    arn = "{{ arn }}"
    description = "{{ description }}"
    id = "{{ id }}"
    lock_token = "{{ lock_token }}"
    name = "{{ name }}"
    scope = "{{ scope }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}