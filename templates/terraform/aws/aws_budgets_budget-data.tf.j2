data "aws_budgets_budget" "example" {
    account_id = "{{ account_id }}"
    id = "{{ id }}"
    name = "{{ name }}"
    name_prefix = "{{ name_prefix }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
