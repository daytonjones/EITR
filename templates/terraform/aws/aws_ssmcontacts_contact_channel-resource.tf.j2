resource "aws_ssmcontacts_contact_channel" "example" {
    contact_id = "{{ contact_id }}"
    id = "{{ id }}"
    name = "{{ name }}"
    region = "{{ region }}"
    type = "{{ type }}"
    delivery_address {
      simple_address = "{{ simple_address }}"
    }
}
