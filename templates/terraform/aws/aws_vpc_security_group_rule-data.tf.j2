data "aws_vpc_security_group_rule" "example" {
    region = "{{ region }}"
    security_group_rule_id = "{{ security_group_rule_id }}"
    filter {
      name = "{{ name }}"
      values = ["{{ values }}"]
    }
}
