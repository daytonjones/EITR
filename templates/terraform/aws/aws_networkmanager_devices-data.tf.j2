data "aws_networkmanager_devices" "example" {
    global_network_id = "{{ global_network_id }}"
    id = "{{ id }}"
    site_id = "{{ site_id }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
