data "aws_kendra_faq" "example" {
    arn = "{{ arn }}"
    created_at = "{{ created_at }}"
    description = "{{ description }}"
    error_message = "{{ error_message }}"
    faq_id = "{{ faq_id }}"
    file_format = "{{ file_format }}"
    id = "{{ id }}"
    index_id = "{{ index_id }}"
    language_code = "{{ language_code }}"
    name = "{{ name }}"
    role_arn = "{{ role_arn }}"
    s3_path = "{{ s3_path }}"
    status = "{{ status }}"
    tags = "{{ tags }}"
    updated_at = "{{ updated_at }}"
}