data "aws_s3_bucket_object" "example" {
    bucket = "{{ bucket }}"
    id = "{{ id }}"
    key = "{{ key }}"
    range = "{{ range }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
    version_id = "{{ version_id }}"
}
