resource "aws_lb_ssl_negotiation_policy" "example" {
    id = "{{ id }}"
    lb_port = "{{ lb_port }}"
    load_balancer = "{{ load_balancer }}"
    name = "{{ name }}"
    triggers = "{{ triggers }}"
}