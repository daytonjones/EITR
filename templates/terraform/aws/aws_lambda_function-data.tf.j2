data "aws_lambda_function" "example" {
    function_name = "{{ function_name }}"
    id = "{{ id }}"
    qualifier = "{{ qualifier }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
