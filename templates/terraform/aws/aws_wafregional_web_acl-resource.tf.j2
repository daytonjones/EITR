resource "aws_wafregional_web_acl" "example" {
    arn = "{{ arn }}"
    id = "{{ id }}"
    metric_name = "{{ metric_name }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}