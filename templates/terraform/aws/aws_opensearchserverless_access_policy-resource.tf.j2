resource "aws_opensearchserverless_access_policy" "example" {
    description = "{{ description }}"
    name = "{{ name }}"
    policy = "{{ policy }}"
    region = "{{ region }}"
    type = "{{ type }}"
}
