resource "aws_codeartifact_domain" "example" {
    domain = "{{ domain }}"
    encryption_key = "{{ encryption_key }}"
    id = "{{ id }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
    tags_all = {
        key = "{{ tags_all_value }}"
      }
}
