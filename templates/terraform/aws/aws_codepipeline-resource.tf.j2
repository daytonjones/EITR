resource "aws_codepipeline" "example" {
    arn = "{{ arn }}"
    execution_mode = "{{ execution_mode }}"
    id = "{{ id }}"
    name = "{{ name }}"
    pipeline_type = "{{ pipeline_type }}"
    role_arn = "{{ role_arn }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}