resource "aws_sagemaker_flow_definition" "example" {
    arn = "{{ arn }}"
    flow_definition_name = "{{ flow_definition_name }}"
    id = "{{ id }}"
    role_arn = "{{ role_arn }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}