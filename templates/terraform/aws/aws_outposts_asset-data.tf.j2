data "aws_outposts_asset" "example" {
    arn = "{{ arn }}"
    asset_id = "{{ asset_id }}"
    asset_type = "{{ asset_type }}"
    host_id = "{{ host_id }}"
    id = "{{ id }}"
    rack_elevation = "{{ rack_elevation }}"
    rack_id = "{{ rack_id }}"
}