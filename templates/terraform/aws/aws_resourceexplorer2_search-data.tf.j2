data "aws_resourceexplorer2_search" "example" {
    id = "{{ id }}"
    query_string = "{{ query_string }}"
    resource_count = "{{ resource_count }}"
    resources = "{{ resources }}"
    view_arn = "{{ view_arn }}"
}