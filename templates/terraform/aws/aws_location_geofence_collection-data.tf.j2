data "aws_location_geofence_collection" "example" {
    collection_name = "{{ collection_name }}"
    id = "{{ id }}"
    kms_key_id = "{{ kms_key_id }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
