data "aws_iam_instance_profile" "example" {
    arn = "{{ arn }}"
    create_date = "{{ create_date }}"
    id = "{{ id }}"
    name = "{{ name }}"
    path = "{{ path }}"
    role_arn = "{{ role_arn }}"
    role_id = "{{ role_id }}"
    role_name = "{{ role_name }}"
}