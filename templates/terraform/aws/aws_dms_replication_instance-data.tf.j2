data "aws_dms_replication_instance" "example" {
    id = "{{ id }}"
    region = "{{ region }}"
    replication_instance_id = "{{ replication_instance_id }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
