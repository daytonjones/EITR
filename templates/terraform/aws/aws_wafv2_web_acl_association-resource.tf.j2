resource "aws_wafv2_web_acl_association" "example" {
    id = "{{ id }}"
    region = "{{ region }}"
    resource_arn = "{{ resource_arn }}"
    web_acl_arn = "{{ web_acl_arn }}"
    timeouts {
      create = "{{ create }}"
    }
}
