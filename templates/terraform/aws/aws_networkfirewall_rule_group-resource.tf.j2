resource "aws_networkfirewall_rule_group" "example" {
    arn = "{{ arn }}"
    capacity = "{{ capacity }}"
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    rules = "{{ rules }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
    type = "{{ type }}"
    update_token = "{{ update_token }}"
}