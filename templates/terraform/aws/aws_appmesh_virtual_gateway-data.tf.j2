data "aws_appmesh_virtual_gateway" "example" {
    id = "{{ id }}"
    mesh_name = "{{ mesh_name }}"
    name = "{{ name }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
