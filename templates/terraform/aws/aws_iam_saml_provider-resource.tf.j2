resource "aws_iam_saml_provider" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    saml_metadata_document = "{{ saml_metadata_document }}"
    tags = {
        key = "{{ tags_value }}"
      }
    tags_all = {
        key = "{{ tags_all_value }}"
      }
}
