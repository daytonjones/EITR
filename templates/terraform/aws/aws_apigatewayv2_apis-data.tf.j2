data "aws_apigatewayv2_apis" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    protocol_type = "{{ protocol_type }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
