resource "aws_network_acl" "example" {
    arn = "{{ arn }}"
    egress = "{{ egress }}"
    id = "{{ id }}"
    ingress = "{{ ingress }}"
    owner_id = "{{ owner_id }}"
    subnet_ids = "{{ subnet_ids }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
    vpc_id = "{{ vpc_id }}"
}