data "aws_imagebuilder_container_recipes" "example" {
    id = "{{ id }}"
    owner = "{{ owner }}"
    region = "{{ region }}"
    filter {
      name = "{{ name }}"
      values = ["{{ values }}"]
    }
}
