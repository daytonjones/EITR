resource "aws_cloudfront_public_key" "example" {
    comment = "{{ comment }}"
    encoded_key = "{{ encoded_key }}"
    id = "{{ id }}"
    name = "{{ name }}"
    name_prefix = "{{ name_prefix }}"
}
