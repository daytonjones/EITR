data "aws_cloudfront_origin_access_identities" "example" {
    comments = "{{ comments }}"
    iam_arns = "{{ iam_arns }}"
    id = "{{ id }}"
    ids = "{{ ids }}"
    s3_canonical_user_ids = "{{ s3_canonical_user_ids }}"
}