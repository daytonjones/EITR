data "aws_prometheus_workspaces" "example" {
    alias_prefix = "{{ alias_prefix }}"
    aliases = "{{ aliases }}"
    arns = "{{ arns }}"
    id = "{{ id }}"
    workspace_ids = "{{ workspace_ids }}"
}