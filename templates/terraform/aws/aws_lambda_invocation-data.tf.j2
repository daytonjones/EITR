data "aws_lambda_invocation" "example" {
    function_name = "{{ function_name }}"
    id = "{{ id }}"
    input = "{{ input }}"
    qualifier = "{{ qualifier }}"
    region = "{{ region }}"
    tenant_id = "{{ tenant_id }}"
}
