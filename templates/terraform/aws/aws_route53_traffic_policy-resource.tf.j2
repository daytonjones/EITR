resource "aws_route53_traffic_policy" "example" {
    comment = "{{ comment }}"
    document = "{{ document }}"
    id = "{{ id }}"
    name = "{{ name }}"
    type = "{{ type }}"
    version = "{{ version }}"
}