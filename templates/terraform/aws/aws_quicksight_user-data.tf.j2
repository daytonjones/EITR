data "aws_quicksight_user" "example" {
    aws_account_id = "{{ aws_account_id }}"
    id = "{{ id }}"
    namespace = "{{ namespace }}"
    region = "{{ region }}"
    user_name = "{{ user_name }}"
}
