resource "aws_organizations_resource_policy" "example" {
    content = "{{ content }}"
    id = "{{ id }}"
    tags = {
        key = "{{ tags_value }}"
      }
    tags_all = {
        key = "{{ tags_all_value }}"
      }
}
