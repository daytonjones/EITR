data "aws_mq_broker_instance_type_offerings" "example" {
    broker_instance_options = "{{ broker_instance_options }}"
    engine_type = "{{ engine_type }}"
    host_instance_type = "{{ host_instance_type }}"
    id = "{{ id }}"
    storage_type = "{{ storage_type }}"
}