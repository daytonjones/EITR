data "aws_cloudwatch_log_data_protection_policy_document" "example" {
    description = "{{ description }}"
    id = "{{ id }}"
    json = "{{ json }}"
    name = "{{ name }}"
    version = "{{ version }}"
}