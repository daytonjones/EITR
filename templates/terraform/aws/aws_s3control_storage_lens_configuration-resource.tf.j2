resource "aws_s3control_storage_lens_configuration" "example" {
    account_id = "{{ account_id }}"
    arn = "{{ arn }}"
    config_id = "{{ config_id }}"
    id = "{{ id }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}