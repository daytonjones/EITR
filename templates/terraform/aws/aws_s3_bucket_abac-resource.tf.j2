resource "aws_s3_bucket_abac" "example" {
    bucket = "{{ bucket }}"
    expected_bucket_owner = "{{ expected_bucket_owner }}"
    region = "{{ region }}"
    abac_status {
      status = "{{ status }}"
    }
}
