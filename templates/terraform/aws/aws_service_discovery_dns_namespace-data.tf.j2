data "aws_service_discovery_dns_namespace" "example" {
    arn = "{{ arn }}"
    description = "{{ description }}"
    hosted_zone = "{{ hosted_zone }}"
    id = "{{ id }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    type = "{{ type }}"
}