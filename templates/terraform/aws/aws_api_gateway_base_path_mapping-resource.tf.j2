resource "aws_api_gateway_base_path_mapping" "example" {
    api_id = "{{ api_id }}"
    base_path = "{{ base_path }}"
    domain_name = "{{ domain_name }}"
    id = "{{ id }}"
    stage_name = "{{ stage_name }}"
}