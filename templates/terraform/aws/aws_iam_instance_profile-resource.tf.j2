resource "aws_iam_instance_profile" "example" {
    arn = "{{ arn }}"
    create_date = "{{ create_date }}"
    id = "{{ id }}"
    name = "{{ name }}"
    name_prefix = "{{ name_prefix }}"
    path = "{{ path }}"
    role = "{{ role }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
    unique_id = "{{ unique_id }}"
}