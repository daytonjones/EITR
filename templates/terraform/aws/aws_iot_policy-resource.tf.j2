resource "aws_iot_policy" "example" {
    arn = "{{ arn }}"
    default_version_id = "{{ default_version_id }}"
    id = "{{ id }}"
    name = "{{ name }}"
    policy = "{{ policy }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}