resource "aws_fsx_backup" "example" {
    arn = "{{ arn }}"
    file_system_id = "{{ file_system_id }}"
    id = "{{ id }}"
    kms_key_id = "{{ kms_key_id }}"
    owner_id = "{{ owner_id }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
    type = "{{ type }}"
    volume_id = "{{ volume_id }}"
}