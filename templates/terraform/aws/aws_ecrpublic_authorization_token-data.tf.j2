data "aws_ecrpublic_authorization_token" "example" {
    authorization_token = "{{ authorization_token }}"
    expires_at = "{{ expires_at }}"
    id = "{{ id }}"
    password = "{{ password }}"
    user_name = "{{ user_name }}"
}