resource "aws_rekognition_collection" "example" {
    collection_id = "{{ collection_id }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
    timeouts {
      create = "{{ create }}"
    }
}
