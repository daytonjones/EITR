resource "aws_main_route_table_association" "example" {
    id = "{{ id }}"
    region = "{{ region }}"
    route_table_id = "{{ route_table_id }}"
    vpc_id = "{{ vpc_id }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
