resource "aws_apprunner_vpc_ingress_connection" "example" {
    arn = "{{ arn }}"
    domain_name = "{{ domain_name }}"
    id = "{{ id }}"
    name = "{{ name }}"
    service_arn = "{{ service_arn }}"
    status = "{{ status }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}