data "aws_vpc_peering_connection" "example" {
    accepter = "{{ accepter }}"
    cidr_block = "{{ cidr_block }}"
    cidr_block_set = "{{ cidr_block_set }}"
    id = "{{ id }}"
    ipv6_cidr_block_set = "{{ ipv6_cidr_block_set }}"
    owner_id = "{{ owner_id }}"
    peer_cidr_block = "{{ peer_cidr_block }}"
    peer_cidr_block_set = "{{ peer_cidr_block_set }}"
    peer_ipv6_cidr_block_set = "{{ peer_ipv6_cidr_block_set }}"
    peer_owner_id = "{{ peer_owner_id }}"
    peer_region = "{{ peer_region }}"
    peer_vpc_id = "{{ peer_vpc_id }}"
    region = "{{ region }}"
    requester = "{{ requester }}"
    status = "{{ status }}"
    tags = "{{ tags }}"
    vpc_id = "{{ vpc_id }}"
}