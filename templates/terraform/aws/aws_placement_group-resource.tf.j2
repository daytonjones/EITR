resource "aws_placement_group" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    partition_count = {{ partition_count }}
    region = "{{ region }}"
    spread_level = "{{ spread_level }}"
    strategy = "{{ strategy }}"
    tags = {
        key = "{{ tags_value }}"
      }
    tags_all = {
        key = "{{ tags_all_value }}"
      }
}
