data "aws_ec2_spot_price" "example" {
    availability_zone = "{{ availability_zone }}"
    id = "{{ id }}"
    instance_type = "{{ instance_type }}"
    region = "{{ region }}"
    filter {
      name = "{{ name }}"
      values = ["{{ values }}"]
    }
    timeouts {
      read = "{{ read }}"
    }
}
