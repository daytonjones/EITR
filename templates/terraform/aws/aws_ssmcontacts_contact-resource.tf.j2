resource "aws_ssmcontacts_contact" "example" {
    alias = "{{ alias }}"
    arn = "{{ arn }}"
    display_name = "{{ display_name }}"
    id = "{{ id }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
    type = "{{ type }}"
}