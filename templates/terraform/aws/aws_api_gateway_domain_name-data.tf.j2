data "aws_api_gateway_domain_name" "example" {
    domain_name = "{{ domain_name }}"
    domain_name_id = "{{ domain_name_id }}"
    id = "{{ id }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
