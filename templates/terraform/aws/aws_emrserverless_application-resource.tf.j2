resource "aws_emrserverless_application" "example" {
    architecture = "{{ architecture }}"
    arn = "{{ arn }}"
    id = "{{ id }}"
    name = "{{ name }}"
    release_label = "{{ release_label }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
    type = "{{ type }}"
}