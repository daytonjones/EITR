resource "aws_sesv2_configuration_set" "example" {
    arn = "{{ arn }}"
    configuration_set_name = "{{ configuration_set_name }}"
    id = "{{ id }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}