resource "aws_codeguruprofiler_profiling_group" "example" {
    arn = "{{ arn }}"
    compute_platform = "{{ compute_platform }}"
    id = "{{ id }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}