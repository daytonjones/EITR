resource "aws_api_gateway_usage_plan" "example" {
    arn = "{{ arn }}"
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    product_code = "{{ product_code }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}