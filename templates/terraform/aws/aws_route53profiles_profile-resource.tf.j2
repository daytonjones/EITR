resource "aws_route53profiles_profile" "example" {
    arn = "{{ arn }}"
    id = "{{ id }}"
    name = "{{ name }}"
    owner_id = "{{ owner_id }}"
    share_status = "{{ share_status }}"
    status = "{{ status }}"
    status_message = "{{ status_message }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}