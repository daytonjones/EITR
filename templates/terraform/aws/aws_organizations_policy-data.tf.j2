data "aws_organizations_policy" "example" {
    arn = "{{ arn }}"
    aws_managed = "{{ aws_managed }}"
    content = "{{ content }}"
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    policy_id = "{{ policy_id }}"
    type = "{{ type }}"
}