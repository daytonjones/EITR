data "aws_wafv2_ip_set" "example" {
    addresses = "{{ addresses }}"
    arn = "{{ arn }}"
    description = "{{ description }}"
    id = "{{ id }}"
    ip_address_version = "{{ ip_address_version }}"
    name = "{{ name }}"
    scope = "{{ scope }}"
}