resource "aws_dynamodb_global_table" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    region = "{{ region }}"
    replica {
      region_name = "{{ region_name }}"
    }
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
