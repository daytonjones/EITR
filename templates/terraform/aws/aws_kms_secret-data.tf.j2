data "aws_kms_secret" "example" {
    id = "{{ id }}"
    region = "{{ region }}"
    secret {
      context = {
        key = "{{ context_value }}"
      }
      grant_tokens = ["{{ grant_tokens }}"]
      name = "{{ name }}"
      payload = "{{ payload }}"
    }
}
