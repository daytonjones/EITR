data "aws_iam_session_context" "example" {
    arn = "{{ arn }}"
    id = "{{ id }}"
    issuer_arn = "{{ issuer_arn }}"
    issuer_id = "{{ issuer_id }}"
    issuer_name = "{{ issuer_name }}"
    session_name = "{{ session_name }}"
}