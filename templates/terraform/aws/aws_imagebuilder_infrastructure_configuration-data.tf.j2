data "aws_imagebuilder_infrastructure_configuration" "example" {
    arn = "{{ arn }}"
    id = "{{ id }}"
    region = "{{ region }}"
    resource_tags = {
        key = "{{ resource_tags_value }}"
      }
    tags = {
        key = "{{ tags_value }}"
      }
}
