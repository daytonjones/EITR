data "aws_backup_vault" "example" {
    arn = "{{ arn }}"
    id = "{{ id }}"
    kms_key_arn = "{{ kms_key_arn }}"
    name = "{{ name }}"
    recovery_points = "{{ recovery_points }}"
    tags = "{{ tags }}"
}