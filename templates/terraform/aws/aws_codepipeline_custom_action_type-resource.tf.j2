resource "aws_codepipeline_custom_action_type" "example" {
    arn = "{{ arn }}"
    category = "{{ category }}"
    id = "{{ id }}"
    owner = "{{ owner }}"
    provider_name = "{{ provider_name }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
    version = "{{ version }}"
}