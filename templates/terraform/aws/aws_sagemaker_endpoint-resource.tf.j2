resource "aws_sagemaker_endpoint" "example" {
    arn = "{{ arn }}"
    endpoint_config_name = "{{ endpoint_config_name }}"
    id = "{{ id }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}