data "aws_efs_access_point" "example" {
    access_point_id = "{{ access_point_id }}"
    arn = "{{ arn }}"
    file_system_arn = "{{ file_system_arn }}"
    file_system_id = "{{ file_system_id }}"
    id = "{{ id }}"
    owner_id = "{{ owner_id }}"
    posix_user = "{{ posix_user }}"
    root_directory = "{{ root_directory }}"
    tags = "{{ tags }}"
}