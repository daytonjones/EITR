data "aws_sesv2_dedicated_ip_pool" "example" {
    arn = "{{ arn }}"
    dedicated_ips = "{{ dedicated_ips }}"
    id = "{{ id }}"
    pool_name = "{{ pool_name }}"
    scaling_mode = "{{ scaling_mode }}"
    tags = "{{ tags }}"
}