resource "aws_glue_registry" "example" {
    description = "{{ description }}"
    id = "{{ id }}"
    region = "{{ region }}"
    registry_name = "{{ registry_name }}"
    tags = {
        key = "{{ tags_value }}"
      }
    tags_all = {
        key = "{{ tags_all_value }}"
      }
}
