resource "aws_macie2_invitation_accepter" "example" {
    administrator_account_id = "{{ administrator_account_id }}"
    id = "{{ id }}"
    region = "{{ region }}"
    timeouts {
      create = "{{ create }}"
    }
}
