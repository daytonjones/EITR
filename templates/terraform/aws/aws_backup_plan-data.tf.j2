data "aws_backup_plan" "example" {
    arn = "{{ arn }}"
    id = "{{ id }}"
    name = "{{ name }}"
    plan_id = "{{ plan_id }}"
    rule = "{{ rule }}"
    tags = "{{ tags }}"
    version = "{{ version }}"
}