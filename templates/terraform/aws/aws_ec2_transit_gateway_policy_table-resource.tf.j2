resource "aws_ec2_transit_gateway_policy_table" "example" {
    arn = "{{ arn }}"
    id = "{{ id }}"
    state = "{{ state }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
    transit_gateway_id = "{{ transit_gateway_id }}"
}