data "aws_apigatewayv2_export" "example" {
    api_id = "{{ api_id }}"
    export_version = "{{ export_version }}"
    id = "{{ id }}"
    include_extensions = {{ include_extensions }}
    output_type = "{{ output_type }}"
    region = "{{ region }}"
    specification = "{{ specification }}"
    stage_name = "{{ stage_name }}"
}
