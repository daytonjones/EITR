data "aws_efs_file_system" "example" {
    creation_token = "{{ creation_token }}"
    file_system_id = "{{ file_system_id }}"
    id = "{{ id }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
