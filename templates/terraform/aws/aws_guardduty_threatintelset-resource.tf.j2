resource "aws_guardduty_threatintelset" "example" {
    activate = "{{ activate }}"
    arn = "{{ arn }}"
    detector_id = "{{ detector_id }}"
    format = "{{ format }}"
    id = "{{ id }}"
    location = "{{ location }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}