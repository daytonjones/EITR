resource "aws_chimesdkvoice_sip_media_application" "example" {
    arn = "{{ arn }}"
    aws_region = "{{ aws_region }}"
    id = "{{ id }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}