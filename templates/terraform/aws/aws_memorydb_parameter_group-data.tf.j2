data "aws_memorydb_parameter_group" "example" {
    arn = "{{ arn }}"
    description = "{{ description }}"
    family = "{{ family }}"
    id = "{{ id }}"
    name = "{{ name }}"
    parameter = "{{ parameter }}"
    tags = "{{ tags }}"
}