data "aws_imagebuilder_components" "example" {
    id = "{{ id }}"
    owner = "{{ owner }}"
    region = "{{ region }}"
    filter {
      name = "{{ name }}"
      values = ["{{ values }}"]
    }
}
