resource "aws_sesv2_dedicated_ip_pool" "example" {
    arn = "{{ arn }}"
    id = "{{ id }}"
    pool_name = "{{ pool_name }}"
    scaling_mode = "{{ scaling_mode }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}