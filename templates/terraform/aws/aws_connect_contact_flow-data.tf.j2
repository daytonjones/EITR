data "aws_connect_contact_flow" "example" {
    contact_flow_id = "{{ contact_flow_id }}"
    id = "{{ id }}"
    instance_id = "{{ instance_id }}"
    name = "{{ name }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
    type = "{{ type }}"
}
