resource "aws_codebuild_report_group" "example" {
    arn = "{{ arn }}"
    created = "{{ created }}"
    delete_reports = "{{ delete_reports }}"
    id = "{{ id }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
    type = "{{ type }}"
}