data "aws_vpclattice_service_network" "example" {
    id = "{{ id }}"
    region = "{{ region }}"
    service_network_identifier = "{{ service_network_identifier }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
