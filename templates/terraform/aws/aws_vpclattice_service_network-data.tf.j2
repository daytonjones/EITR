data "aws_vpclattice_service_network" "example" {
    arn = "{{ arn }}"
    auth_type = "{{ auth_type }}"
    created_at = "{{ created_at }}"
    id = "{{ id }}"
    last_updated_at = "{{ last_updated_at }}"
    name = "{{ name }}"
    number_of_associated_services = "{{ number_of_associated_services }}"
    number_of_associated_vpcs = "{{ number_of_associated_vpcs }}"
    service_network_identifier = "{{ service_network_identifier }}"
    tags = "{{ tags }}"
}