resource "aws_route53recoveryreadiness_cell" "example" {
    arn = "{{ arn }}"
    cell_name = "{{ cell_name }}"
    cells = "{{ cells }}"
    id = "{{ id }}"
    parent_readiness_scopes = "{{ parent_readiness_scopes }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}