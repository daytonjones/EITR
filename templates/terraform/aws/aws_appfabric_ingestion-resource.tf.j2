resource "aws_appfabric_ingestion" "example" {
    app = "{{ app }}"
    app_bundle_arn = "{{ app_bundle_arn }}"
    ingestion_type = "{{ ingestion_type }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
    tenant_id = "{{ tenant_id }}"
}
