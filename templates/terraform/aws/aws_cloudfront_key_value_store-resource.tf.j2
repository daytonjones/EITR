resource "aws_cloudfront_key_value_store" "example" {
    arn = "{{ arn }}"
    comment = "{{ comment }}"
    etag = "{{ etag }}"
    id = "{{ id }}"
    last_modified_time = "{{ last_modified_time }}"
    name = "{{ name }}"
}