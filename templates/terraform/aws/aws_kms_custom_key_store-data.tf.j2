data "aws_kms_custom_key_store" "example" {
    custom_key_store_id = "{{ custom_key_store_id }}"
    custom_key_store_name = "{{ custom_key_store_name }}"
    id = "{{ id }}"
    region = "{{ region }}"
}
