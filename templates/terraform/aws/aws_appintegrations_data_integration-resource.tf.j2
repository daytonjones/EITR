resource "aws_appintegrations_data_integration" "example" {
    arn = "{{ arn }}"
    description = "{{ description }}"
    id = "{{ id }}"
    kms_key = "{{ kms_key }}"
    name = "{{ name }}"
    source_uri = "{{ source_uri }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}