data "aws_kms_ciphertext" "example" {
    context = {
        key = "{{ context_value }}"
      }
    id = "{{ id }}"
    key_id = "{{ key_id }}"
    plaintext = "{{ plaintext }}"
    region = "{{ region }}"
}
