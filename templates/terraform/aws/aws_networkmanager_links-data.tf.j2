data "aws_networkmanager_links" "example" {
    global_network_id = "{{ global_network_id }}"
    id = "{{ id }}"
    provider_name = "{{ provider_name }}"
    site_id = "{{ site_id }}"
    tags = {
        key = "{{ tags_value }}"
      }
    type = "{{ type }}"
}
