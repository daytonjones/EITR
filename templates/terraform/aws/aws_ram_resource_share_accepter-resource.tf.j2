resource "aws_ram_resource_share_accepter" "example" {
    id = "{{ id }}"
    region = "{{ region }}"
    share_arn = "{{ share_arn }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
    }
}
