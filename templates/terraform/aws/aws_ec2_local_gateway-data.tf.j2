data "aws_ec2_local_gateway" "example" {
    id = "{{ id }}"
    region = "{{ region }}"
    state = "{{ state }}"
    tags = {
        key = "{{ tags_value }}"
      }
    filter {
      name = "{{ name }}"
      values = ["{{ values }}"]
    }
    timeouts {
      read = "{{ read }}"
    }
}
