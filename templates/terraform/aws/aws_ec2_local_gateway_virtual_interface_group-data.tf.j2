data "aws_ec2_local_gateway_virtual_interface_group" "example" {
    id = "{{ id }}"
    local_gateway_id = "{{ local_gateway_id }}"
    local_gateway_virtual_interface_ids = "{{ local_gateway_virtual_interface_ids }}"
    tags = "{{ tags }}"
}