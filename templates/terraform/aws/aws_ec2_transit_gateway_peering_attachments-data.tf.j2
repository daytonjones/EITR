data "aws_ec2_transit_gateway_peering_attachments" "example" {
    id = "{{ id }}"
    region = "{{ region }}"
    filter {
      name = "{{ name }}"
      values = ["{{ values }}"]
    }
    timeouts {
      read = "{{ read }}"
    }
}
