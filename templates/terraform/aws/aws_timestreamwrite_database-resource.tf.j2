resource "aws_timestreamwrite_database" "example" {
    database_name = "{{ database_name }}"
    id = "{{ id }}"
    kms_key_id = "{{ kms_key_id }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
    tags_all = {
        key = "{{ tags_all_value }}"
      }
}
