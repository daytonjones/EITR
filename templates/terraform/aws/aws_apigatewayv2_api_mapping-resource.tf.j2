resource "aws_apigatewayv2_api_mapping" "example" {
    api_id = "{{ api_id }}"
    api_mapping_key = "{{ api_mapping_key }}"
    domain_name = "{{ domain_name }}"
    id = "{{ id }}"
    stage = "{{ stage }}"
}