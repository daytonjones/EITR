resource "aws_sagemaker_notebook_instance_lifecycle_configuration" "example" {
    arn = "{{ arn }}"
    id = "{{ id }}"
    name = "{{ name }}"
    on_create = "{{ on_create }}"
    on_start = "{{ on_start }}"
}