resource "aws_inspector2_member_association" "example" {
    account_id = "{{ account_id }}"
    id = "{{ id }}"
    region = "{{ region }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
    }
}
