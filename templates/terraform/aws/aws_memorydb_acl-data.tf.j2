data "aws_memorydb_acl" "example" {
    arn = "{{ arn }}"
    id = "{{ id }}"
    minimum_engine_version = "{{ minimum_engine_version }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    user_names = "{{ user_names }}"
}