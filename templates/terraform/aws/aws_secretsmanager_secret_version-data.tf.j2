data "aws_secretsmanager_secret_version" "example" {
    id = "{{ id }}"
    region = "{{ region }}"
    secret_id = "{{ secret_id }}"
    version_id = "{{ version_id }}"
    version_stage = "{{ version_stage }}"
}
