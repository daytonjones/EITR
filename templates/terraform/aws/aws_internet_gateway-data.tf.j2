data "aws_internet_gateway" "example" {
    arn = "{{ arn }}"
    attachments = "{{ attachments }}"
    id = "{{ id }}"
    internet_gateway_id = "{{ internet_gateway_id }}"
    owner_id = "{{ owner_id }}"
    tags = "{{ tags }}"
}