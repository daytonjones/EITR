resource "aws_dataexchange_revision" "example" {
    comment = "{{ comment }}"
    data_set_id = "{{ data_set_id }}"
    id = "{{ id }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
    tags_all = {
        key = "{{ tags_all_value }}"
      }
}
