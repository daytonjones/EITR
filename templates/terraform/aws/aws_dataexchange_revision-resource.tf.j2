resource "aws_dataexchange_revision" "example" {
    arn = "{{ arn }}"
    comment = "{{ comment }}"
    data_set_id = "{{ data_set_id }}"
    id = "{{ id }}"
    revision_id = "{{ revision_id }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}