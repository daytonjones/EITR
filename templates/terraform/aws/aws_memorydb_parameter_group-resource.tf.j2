resource "aws_memorydb_parameter_group" "example" {
    arn = "{{ arn }}"
    description = "{{ description }}"
    family = "{{ family }}"
    id = "{{ id }}"
    name = "{{ name }}"
    name_prefix = "{{ name_prefix }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}