data "aws_cloudformation_type" "example" {
    arn = "{{ arn }}"
    id = "{{ id }}"
    region = "{{ region }}"
    type = "{{ type }}"
    type_name = "{{ type_name }}"
    version_id = "{{ version_id }}"
}
