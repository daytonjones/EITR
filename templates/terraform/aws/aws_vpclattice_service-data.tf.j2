data "aws_vpclattice_service" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    region = "{{ region }}"
    service_identifier = "{{ service_identifier }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
