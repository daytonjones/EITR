resource "aws_sagemaker_app" "example" {
    app_name = "{{ app_name }}"
    app_type = "{{ app_type }}"
    arn = "{{ arn }}"
    domain_id = "{{ domain_id }}"
    id = "{{ id }}"
    space_name = "{{ space_name }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
    user_profile_name = "{{ user_profile_name }}"
}