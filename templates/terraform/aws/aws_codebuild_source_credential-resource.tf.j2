resource "aws_codebuild_source_credential" "example" {
    auth_type = "{{ auth_type }}"
    id = "{{ id }}"
    region = "{{ region }}"
    server_type = "{{ server_type }}"
    token = "{{ token }}"
    user_name = "{{ user_name }}"
}
