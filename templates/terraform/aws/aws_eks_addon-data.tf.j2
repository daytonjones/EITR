data "aws_eks_addon" "example" {
    addon_name = "{{ addon_name }}"
    cluster_name = "{{ cluster_name }}"
    id = "{{ id }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
