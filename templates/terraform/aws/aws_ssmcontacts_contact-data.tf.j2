data "aws_ssmcontacts_contact" "example" {
    alias = "{{ alias }}"
    arn = "{{ arn }}"
    display_name = "{{ display_name }}"
    id = "{{ id }}"
    tags = "{{ tags }}"
    type = "{{ type }}"
}