data "aws_eks_addon_version" "example" {
    addon_name = "{{ addon_name }}"
    id = "{{ id }}"
    kubernetes_version = "{{ kubernetes_version }}"
    most_recent = "{{ most_recent }}"
    version = "{{ version }}"
}