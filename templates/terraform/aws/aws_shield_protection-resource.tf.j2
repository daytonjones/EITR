resource "aws_shield_protection" "example" {
    arn = "{{ arn }}"
    id = "{{ id }}"
    name = "{{ name }}"
    resource_arn = "{{ resource_arn }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}