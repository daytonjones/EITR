data "aws_secretsmanager_secret_versions" "example" {
    arn = "{{ arn }}"
    include_deprecated = "{{ include_deprecated }}"
    name = "{{ name }}"
    secret_id = "{{ secret_id }}"
    versions = "{{ versions }}"
}