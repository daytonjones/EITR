resource "aws_schemas_discoverer" "example" {
    arn = "{{ arn }}"
    description = "{{ description }}"
    id = "{{ id }}"
    source_arn = "{{ source_arn }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}