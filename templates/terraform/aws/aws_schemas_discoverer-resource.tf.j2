resource "aws_schemas_discoverer" "example" {
    description = "{{ description }}"
    id = "{{ id }}"
    region = "{{ region }}"
    source_arn = "{{ source_arn }}"
    tags = {
        key = "{{ tags_value }}"
      }
    tags_all = {
        key = "{{ tags_all_value }}"
      }
}
