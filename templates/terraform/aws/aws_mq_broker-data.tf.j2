data "aws_mq_broker" "example" {
    broker_id = "{{ broker_id }}"
    broker_name = "{{ broker_name }}"
    id = "{{ id }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
