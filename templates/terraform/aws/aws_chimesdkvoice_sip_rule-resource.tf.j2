resource "aws_chimesdkvoice_sip_rule" "example" {
    disabled = "{{ disabled }}"
    id = "{{ id }}"
    name = "{{ name }}"
    trigger_type = "{{ trigger_type }}"
    trigger_value = "{{ trigger_value }}"
}