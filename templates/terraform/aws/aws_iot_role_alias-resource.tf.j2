resource "aws_iot_role_alias" "example" {
    alias = "{{ alias }}"
    arn = "{{ arn }}"
    credential_duration = "{{ credential_duration }}"
    id = "{{ id }}"
    role_arn = "{{ role_arn }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}