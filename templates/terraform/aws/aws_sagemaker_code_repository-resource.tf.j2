resource "aws_sagemaker_code_repository" "example" {
    arn = "{{ arn }}"
    code_repository_name = "{{ code_repository_name }}"
    id = "{{ id }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}