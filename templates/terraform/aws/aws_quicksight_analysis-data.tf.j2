data "aws_quicksight_analysis" "example" {
    analysis_id = "{{ analysis_id }}"
    aws_account_id = "{{ aws_account_id }}"
    id = "{{ id }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
