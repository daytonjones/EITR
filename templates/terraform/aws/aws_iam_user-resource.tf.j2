resource "aws_iam_user" "example" {
    arn = "{{ arn }}"
    force_destroy = "{{ force_destroy }}"
    id = "{{ id }}"
    name = "{{ name }}"
    path = "{{ path }}"
    permissions_boundary = "{{ permissions_boundary }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
    unique_id = "{{ unique_id }}"
}