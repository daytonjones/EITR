resource "aws_appsync_type" "example" {
    api_id = "{{ api_id }}"
    arn = "{{ arn }}"
    definition = "{{ definition }}"
    description = "{{ description }}"
    format = "{{ format }}"
    id = "{{ id }}"
    name = "{{ name }}"
}