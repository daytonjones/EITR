resource "aws_vpn_gateway" "example" {
    amazon_side_asn = "{{ amazon_side_asn }}"
    arn = "{{ arn }}"
    availability_zone = "{{ availability_zone }}"
    id = "{{ id }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
    vpc_id = "{{ vpc_id }}"
}