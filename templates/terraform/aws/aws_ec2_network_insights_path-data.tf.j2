data "aws_ec2_network_insights_path" "example" {
    id = "{{ id }}"
    network_insights_path_id = "{{ network_insights_path_id }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
    filter {
      name = "{{ name }}"
      values = ["{{ values }}"]
    }
}
