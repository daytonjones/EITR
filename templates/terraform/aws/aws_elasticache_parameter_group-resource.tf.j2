resource "aws_elasticache_parameter_group" "example" {
    arn = "{{ arn }}"
    description = "{{ description }}"
    family = "{{ family }}"
    id = "{{ id }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}