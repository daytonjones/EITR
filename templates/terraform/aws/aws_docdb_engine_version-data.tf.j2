data "aws_docdb_engine_version" "example" {
    engine = "{{ engine }}"
    id = "{{ id }}"
    parameter_group_family = "{{ parameter_group_family }}"
    preferred_versions = ["{{ preferred_versions }}"]
    region = "{{ region }}"
    version = "{{ version }}"
}
