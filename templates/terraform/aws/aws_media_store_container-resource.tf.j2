resource "aws_media_store_container" "example" {
    arn = "{{ arn }}"
    endpoint = "{{ endpoint }}"
    id = "{{ id }}"
    name = "{{ name }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}