resource "aws_account_region" "example" {
    account_id = "{{ account_id }}"
    enabled = {{ enabled }}
    id = "{{ id }}"
    region_name = "{{ region_name }}"
    timeouts {
      create = "{{ create }}"
      update = "{{ update }}"
    }
}
