resource "aws_shield_application_layer_automatic_response" "example" {
    action = "{{ action }}"
    resource_arn = "{{ resource_arn }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
