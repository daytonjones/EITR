resource "aws_oam_sink_policy" "example" {
    id = "{{ id }}"
    policy = "{{ policy }}"
    region = "{{ region }}"
    sink_identifier = "{{ sink_identifier }}"
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
      update = "{{ update }}"
    }
}
