data "aws_lb_listener" "example" {
    arn = "{{ arn }}"
    id = "{{ id }}"
    load_balancer_arn = "{{ load_balancer_arn }}"
    port = {{ port }}
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
    timeouts {
      read = "{{ read }}"
    }
}
