data "aws_ec2_managed_prefix_list" "example" {
    address_family = "{{ address_family }}"
    arn = "{{ arn }}"
    entries = "{{ entries }}"
    id = "{{ id }}"
    max_entries = "{{ max_entries }}"
    name = "{{ name }}"
    owner_id = "{{ owner_id }}"
    tags = "{{ tags }}"
    version = "{{ version }}"
}