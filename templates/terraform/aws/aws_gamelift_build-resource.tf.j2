resource "aws_gamelift_build" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    operating_system = "{{ operating_system }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
    tags_all = {
        key = "{{ tags_all_value }}"
      }
    version = "{{ version }}"
    storage_location {
      bucket = "{{ bucket }}"
      key = "{{ key }}"
      object_version = "{{ object_version }}"
      role_arn = "{{ role_arn }}"
    }
}
