data "aws_networkmanager_connections" "example" {
    device_id = "{{ device_id }}"
    global_network_id = "{{ global_network_id }}"
    id = "{{ id }}"
    ids = "{{ ids }}"
    tags = "{{ tags }}"
}