data "aws_connect_vocabulary" "example" {
    id = "{{ id }}"
    instance_id = "{{ instance_id }}"
    name = "{{ name }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
    vocabulary_id = "{{ vocabulary_id }}"
}
