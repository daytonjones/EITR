data "aws_quicksight_data_set" "example" {
    aws_account_id = "{{ aws_account_id }}"
    data_set_id = "{{ data_set_id }}"
    id = "{{ id }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
