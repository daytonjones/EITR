resource "aws_vpc_route_server_endpoint" "example" {
    region = "{{ region }}"
    route_server_id = "{{ route_server_id }}"
    subnet_id = "{{ subnet_id }}"
    tags = {
        key = "{{ tags_value }}"
      }
    timeouts {
      create = "{{ create }}"
      delete = "{{ delete }}"
    }
}
