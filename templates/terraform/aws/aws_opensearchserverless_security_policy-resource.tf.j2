resource "aws_opensearchserverless_security_policy" "example" {
    description = "{{ description }}"
    name = "{{ name }}"
    policy = "{{ policy }}"
    region = "{{ region }}"
    type = "{{ type }}"
}
