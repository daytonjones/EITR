data "aws_service_discovery_service" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    namespace_id = "{{ namespace_id }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
