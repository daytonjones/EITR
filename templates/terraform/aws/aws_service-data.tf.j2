data "aws_service" "example" {
    dns_name = "{{ dns_name }}"
    id = "{{ id }}"
    partition = "{{ partition }}"
    region = "{{ region }}"
    reverse_dns_name = "{{ reverse_dns_name }}"
    reverse_dns_prefix = "{{ reverse_dns_prefix }}"
    service_id = "{{ service_id }}"
    supported = "{{ supported }}"
}