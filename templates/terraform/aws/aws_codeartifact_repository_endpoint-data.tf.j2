data "aws_codeartifact_repository_endpoint" "example" {
    domain = "{{ domain }}"
    domain_owner = "{{ domain_owner }}"
    format = "{{ format }}"
    id = "{{ id }}"
    region = "{{ region }}"
    repository = "{{ repository }}"
}
