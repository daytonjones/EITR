data "aws_sesv2_configuration_set" "example" {
    configuration_set_name = "{{ configuration_set_name }}"
    id = "{{ id }}"
    region = "{{ region }}"
    tags = {
        key = "{{ tags_value }}"
      }
}
