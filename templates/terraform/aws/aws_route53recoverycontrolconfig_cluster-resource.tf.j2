resource "aws_route53recoverycontrolconfig_cluster" "example" {
    id = "{{ id }}"
    name = "{{ name }}"
    network_type = "{{ network_type }}"
    tags = {
        key = "{{ tags_value }}"
      }
    tags_all = {
        key = "{{ tags_all_value }}"
      }
}
