resource "aws_route53recoverycontrolconfig_cluster" "example" {
    arn = "{{ arn }}"
    cluster_endpoints = "{{ cluster_endpoints }}"
    id = "{{ id }}"
    name = "{{ name }}"
    status = "{{ status }}"
}