data "aws_api_gateway_sdk" "example" {
    id = "{{ id }}"
    parameters = {
        key = "{{ parameters_value }}"
      }
    region = "{{ region }}"
    rest_api_id = "{{ rest_api_id }}"
    sdk_type = "{{ sdk_type }}"
    stage_name = "{{ stage_name }}"
}
