data "aws_vpc_ipam_pool_cidrs" "example" {
    id = "{{ id }}"
    ipam_pool_id = "{{ ipam_pool_id }}"
    region = "{{ region }}"
    filter {
      name = "{{ name }}"
      values = ["{{ values }}"]
    }
    timeouts {
      read = "{{ read }}"
    }
}
