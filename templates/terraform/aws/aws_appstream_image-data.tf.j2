data "aws_appstream_image" "example" {
    arn = "{{ arn }}"
    most_recent = {{ most_recent }}
    name = "{{ name }}"
    name_regex = "{{ name_regex }}"
    region = "{{ region }}"
    type = "{{ type }}"
}
