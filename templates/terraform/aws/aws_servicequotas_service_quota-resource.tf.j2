resource "aws_servicequotas_service_quota" "example" {
    id = "{{ id }}"
    quota_code = "{{ quota_code }}"
    region = "{{ region }}"
    service_code = "{{ service_code }}"
    value = {{ value }}
}
