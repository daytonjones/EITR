resource "aws_apprunner_connection" "example" {
    arn = "{{ arn }}"
    connection_name = "{{ connection_name }}"
    id = "{{ id }}"
    provider_type = "{{ provider_type }}"
    status = "{{ status }}"
    tags = "{{ tags }}"
    tags_all = "{{ tags_all }}"
}