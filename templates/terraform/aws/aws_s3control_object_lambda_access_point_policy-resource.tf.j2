resource "aws_s3control_object_lambda_access_point_policy" "example" {
    account_id = "{{ account_id }}"
    id = "{{ id }}"
    name = "{{ name }}"
    policy = "{{ policy }}"
    region = "{{ region }}"
}
