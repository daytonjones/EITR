data "tfe_organization_membership" "example" {
    email = "{{ email }}"
    id = "{{ id }}"
    organization = "{{ organization }}"
    organization_membership_id = "{{ organization_membership_id }}"
    user_id = "{{ user_id }}"
    username = "{{ username }}"
}