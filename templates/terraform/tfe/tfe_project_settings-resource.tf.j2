resource "tfe_project_settings" "example" {
    default_agent_pool_id = "{{ default_agent_pool_id }}"
    default_execution_mode = "{{ default_execution_mode }}"
    project_id = "{{ project_id }}"
}
