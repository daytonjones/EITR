resource "tfe_organization_run_task" "example" {
    category = "{{ category }}"
    description = "{{ description }}"
    enabled = "{{ enabled }}"
    hmac_key = "{{ hmac_key }}"
    id = "{{ id }}"
    name = "{{ name }}"
    organization = "{{ organization }}"
    url = "{{ url }}"
}