resource "tfe_vault_oidc_configuration" "example" {
    address = "{{ address }}"
    auth_path = "{{ auth_path }}"
    encoded_cacert = "{{ encoded_cacert }}"
    namespace = "{{ namespace }}"
    organization = "{{ organization }}"
    role_name = "{{ role_name }}"
}
