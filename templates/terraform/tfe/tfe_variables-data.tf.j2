data "tfe_variables" "example" {
    env = "{{ env }}"
    id = "{{ id }}"
    terraform = "{{ terraform }}"
    variable_set_id = "{{ variable_set_id }}"
    variables = "{{ variables }}"
    workspace_id = "{{ workspace_id }}"
}