resource "tfe_organization_membership" "example" {
    email = "{{ email }}"
    id = "{{ id }}"
    organization = "{{ organization }}"
    user_id = "{{ user_id }}"
    username = "{{ username }}"
}