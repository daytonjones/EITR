data "tfe_team_project_access" "example" {
    access = "{{ access }}"
    id = "{{ id }}"
    project_access = "{{ project_access }}"
    project_id = "{{ project_id }}"
    team_id = "{{ team_id }}"
    workspace_access = "{{ workspace_access }}"
}