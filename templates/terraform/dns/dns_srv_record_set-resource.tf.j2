resource "dns_srv_record_set" "example" {
    name = "{{ name }}"
    ttl = {{ ttl }}
    zone = "{{ zone }}"
    srv {
      port = {{ port }}
      priority = {{ priority }}
      target = "{{ target }}"
      weight = {{ weight }}
    }
}
