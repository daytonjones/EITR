resource "kubernetes_config_map_v1_data" "example" {
    data = {
        key = "{{ data_value }}"
      }
    field_manager = "{{ field_manager }}"
    force = {{ force }}
    id = "{{ id }}"
    metadata {
      name = "{{ name }}"
      namespace = "{{ namespace }}"
    }
}
