data "kubernetes_mutating_webhook_configuration_v1" "example" {
    id = "{{ id }}"
    metadata {
      annotations = {
        key = "{{ annotations_value }}"
      }
      labels = {
        key = "{{ labels_value }}"
      }
      name = "{{ name }}"
    }
}
