data "kubernetes_config_map" "example" {
    id = "{{ id }}"
    immutable = {{ immutable }}
    metadata {
      annotations = {
        key = "{{ annotations_value }}"
      }
      labels = {
        key = "{{ labels_value }}"
      }
      name = "{{ name }}"
      namespace = "{{ namespace }}"
    }
}
