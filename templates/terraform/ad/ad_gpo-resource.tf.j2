resource "ad_gpo" "example" {
    description = "{{ description }}"
    dn = "{{ dn }}"
    domain = "{{ domain }}"
    id = "{{ id }}"
    name = "{{ name }}"
    numeric_status = "{{ numeric_status }}"
    status = "{{ status }}"
}