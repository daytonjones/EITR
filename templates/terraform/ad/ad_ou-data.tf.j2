data "ad_ou" "example" {
    description = "{{ description }}"
    dn = "{{ dn }}"
    id = "{{ id }}"
    name = "{{ name }}"
    ou_id = "{{ ou_id }}"
    path = "{{ path }}"
    protected = "{{ protected }}"
}