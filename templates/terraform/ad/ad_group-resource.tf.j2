resource "ad_group" "example" {
    category = "{{ category }}"
    container = "{{ container }}"
    description = "{{ description }}"
    dn = "{{ dn }}"
    id = "{{ id }}"
    name = "{{ name }}"
    sam_account_name = "{{ sam_account_name }}"
    scope = "{{ scope }}"
    sid = "{{ sid }}"
}