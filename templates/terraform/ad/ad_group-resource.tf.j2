resource "ad_group" "example" {
    category = "{{ category }}"
    container = "{{ container }}"
    description = "{{ description }}"
    id = "{{ id }}"
    name = "{{ name }}"
    sam_account_name = "{{ sam_account_name }}"
    scope = "{{ scope }}"
}
