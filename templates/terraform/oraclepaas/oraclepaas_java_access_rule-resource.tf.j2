resource "oraclepaas_java_access_rule" "example" {
    description = "{{ description }}"
    destination = "{{ destination }}"
    enabled = "{{ enabled }}"
    id = "{{ id }}"
    name = "{{ name }}"
    ports = "{{ ports }}"
    protocol = "{{ protocol }}"
    service_instance_id = "{{ service_instance_id }}"
    source = "{{ source }}"
}