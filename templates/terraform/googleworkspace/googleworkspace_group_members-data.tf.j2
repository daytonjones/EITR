data "googleworkspace_group_members" "example" {
    etag = "{{ etag }}"
    group_id = "{{ group_id }}"
    id = "{{ id }}"
    include_derived_membership = "{{ include_derived_membership }}"
    members = "{{ members }}"
}