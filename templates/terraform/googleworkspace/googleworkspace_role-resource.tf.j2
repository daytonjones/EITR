resource "googleworkspace_role" "example" {
    description = "{{ description }}"
    name = "{{ name }}"
    privileges {
      privilege_name = "{{ privilege_name }}"
      service_id = "{{ service_id }}"
    }
}
